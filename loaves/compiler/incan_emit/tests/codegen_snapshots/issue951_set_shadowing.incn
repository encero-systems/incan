"""Prove that an ordinary function named set keeps its source-defined identity."""


def set(values: List[str]) -> int:
    """Return the input cardinality without invoking the collection constructor."""
    return len(values)


def main() -> None:
    """Call the source-defined set function through generated Rust."""
    println(set(["alpha", "beta", "beta"]))
