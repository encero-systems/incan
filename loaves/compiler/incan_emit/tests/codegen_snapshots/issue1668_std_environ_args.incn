from std.environ import EnvironError, args


def subcommand(arguments: list[str]) -> str:
    if len(arguments) < 2:
        return "help"
    return arguments[1]


def main() -> Result[None, EnvironError]:
    arguments = args()?
    println(subcommand(arguments))
    for argument in arguments[1:]:
        println(argument)
    return Ok(None)
