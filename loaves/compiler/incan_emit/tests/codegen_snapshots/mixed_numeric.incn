def mixed_ops(x: int, y: float) -> float:
    a = x + y
    b = y + x
    c = x * y
    d = y / x
    e = x / y
    f = x - y
    return a + b + c + d + e + f


def main() -> None:
    result = mixed_ops(2, 3.5)
    println(result)
