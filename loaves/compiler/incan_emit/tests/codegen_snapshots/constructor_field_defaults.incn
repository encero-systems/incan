@derive(Debug)
model Settings:
    theme: str = "dark"
    font_size: int = 14
    auto_save: bool = true

def main() -> None:
    a = Settings()
    b = Settings(font_size=16)
    println(f"{a.theme} {b.font_size}")  # output: "dark 16"
