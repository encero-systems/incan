[package]
name = "native_constructor_forge"
version = "0.1.0"
edition = "2021"

[features]
admission = []
defaulted = []
mixed = []

[dependencies]
native_consumer_core = { path = "../native_items/target/lib" }
