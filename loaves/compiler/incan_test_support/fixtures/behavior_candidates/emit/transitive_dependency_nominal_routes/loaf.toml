[project]
name = "transitive_dependency_nominal_routes"
version = "0.1.0"

[project.scripts]
main = "src/main.incn"

[dependencies]
pricing = { path = "deps/pricing" }
catalog = { path = "deps/pricing/deps/catalog" }
