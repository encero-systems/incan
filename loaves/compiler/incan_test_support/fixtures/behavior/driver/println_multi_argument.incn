# behavior: println renders every argument, space-separated, rather than only the first
# retires: loaves/compiler/incan_driver/tests/replacement_backend_execution_tests.rs::both_backends_render_a_multi_argument_print_the_same_way
# expect-stdout:
#   count 3 true

def main() -> None:
    println("count", 3, true)
