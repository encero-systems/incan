# behavior: a value enum variant that carries a payload is refused at parse time
# retires: loaves/compiler/incan_emit/src/emit/decls/structures.rs::value_enum_payload_variant_is_rejected
# expect-diagnostic: INCAN-P0001

enum Bad(int):
    Payload(int) = 1


def main() -> None:
    println("unreachable")
