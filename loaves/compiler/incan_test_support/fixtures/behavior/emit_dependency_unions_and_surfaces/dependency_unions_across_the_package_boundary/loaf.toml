[project]
name = "dependency_unions_across_the_package_boundary"
version = "0.1.0"

[project.scripts]
main = "src/main.incn"

[dependencies]
querykit = { path = "deps/querykit" }
other = { path = "deps/other" }
