# behavior: std.fs Path is the filesystem path type: lexical joins, name, suffix and stem work, and a File handle type is importable beside it
# retires: loaves/compiler/incan_emit/tests/codegen_snapshot_tests.rs::test_std_fs_import_codegen
# expect-stdout:
#   std.fs import codegen
#   config/app.toml
#   app.toml .toml app config

from std.fs import Path, File

def consume_path(path: Path) -> None:
    return

def consume_file(file: File) -> None:
    return

def main() -> None:
    println("std.fs import codegen")
    config = Path("config").joinpath("app.toml")
    consume_path(config)
    println(config.0)
    println(f"{config.name()} {config.suffix()} {config.stem()} {config.parent().0}")
