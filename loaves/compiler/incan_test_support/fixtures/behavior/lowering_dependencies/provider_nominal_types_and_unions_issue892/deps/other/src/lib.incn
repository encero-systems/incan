pub model Left:
    pub value: int


pub model Right:
    pub label: str


pub type Choice = Left | Right


pub def choose(flag: bool) -> Choice:
    if flag:
        return Left(value=1)
    return Right(label="r")
