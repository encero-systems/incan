@requires(name: str)
trait Loggable:
    def log(self, msg: str) -> None:
        println(f"[{self.name}] {msg}")

model User with Loggable:
    name: str

def main() -> None:
    """The runnable example"""
    u = User(name="Ada")
    u.log("hello")
