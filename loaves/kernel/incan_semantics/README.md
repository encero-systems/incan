# `incan_semantics`

Ring: **kernel**

Surface-semantics contracts and registry interfaces.

## Moves here from

- `crates/incan_semantics_core/` only. `crates/incan_semantics_stdlib` is compiler implementation per `layering.md` and goes to `compiler/incan_provider`; the kernel must not carry stdlib policy.

## May depend on

`incan_lang`

This directory is a layout skeleton. It holds no code yet; `src/` is a placeholder for the conventional crate root.
