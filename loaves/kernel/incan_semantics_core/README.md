# `incan_semantics_core`

Ring: **kernel**

Surface-semantics contracts and registry interfaces.

## Moved here from

- `crates/incan_semantics_core/` only; it becomes `incan_semantics` when the crates are renamed (step 5 of #1478). `incan_semantics_stdlib` is compiler implementation per `layering.md` and lives in `compiler/`; the kernel must not carry stdlib policy.

## May depend on

`incan_lang`
