# `incan_vocab`

Ring: **kernel**

Vocabulary registration contract for companion crates, including the WASM desugar ABI constants.

## Moves here from

- `crates/incan_vocab/ (already independently versioned at 0.3.0; the precedent for ring versioning)`

## May depend on

none

Keeps its serde default: companions exchange JSON through the desugar ABI.

This directory is a layout skeleton. It holds no code yet; `src/` is a placeholder for the conventional crate root.
