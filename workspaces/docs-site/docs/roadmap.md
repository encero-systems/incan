# Incan Roadmap (Status-Focused)

This page tracks the implementation status and near-term planning (without being prescriptive about timelines).

Incan development is driven by RFCs (Request for Comments).

- An RFC captures a design proposal for a feature, including syntax, semantics, and implementation details.
- RFCs are not necessarily implemented in the order they are written.

See the [RFCs](RFCs/index.md) page for more information about RFCs.

## RFC status table

This table is autogenerated from the RFC files (it reads each RFC’s `**Status:** ...` line).

--8<-- "_snippets/tables/rfcs_index.md"

## Core Phases (overview)

- Core language + runtime
- Stdlib + tooling (fmt, test, LSP, VS Code extensions)
- Web backend (Axum)
- Web & product surfaces (server-authoritative apps with browser UI; optional WASM-heavy client track) — [RFC 003](RFCs/003_frontend_wasm.md)
- Rust interop

## Current Focus

- Language stability/feature freeze (core semantics + test surface):
    - [RFC 000] (core semantics) *Done*
    - [RFC 008] (const bindings) *Done*
    - Tests surface:
        - [RFC 001] (test fixtures) *In Progress*
        - [RFC 002] (parametrized tests) *Draft*
        - [RFC 004] (async fixtures) *Draft*
- Web & product surfaces ([RFC 003]): **Blocked** — Phase 1 (handlers, packaging, mutations, **interactive browser UI**, first browser target) before optional Phase 2 WASM/`jsx()`-first work; see RFC

## Ecosystem keystones (planned)

These are the cross-cutting capabilities that make Incan feel “capable” for real engineering work. This list is intentionally
kept high-level and status-oriented (RFCs will be added over time).

- Standard library contracts for real programs (HTTP, filesystem/paths, process, env, time, logging, config)
- Capability-based access model for IO/process/env/network (secure-by-default for tools)
- Interactive execution engine: `incan run -i` (expression-first) → eventual Jupyter/kernel interop → richer workspace UX
- Packaging/distribution story for tools and projects (reproducible builds, artifact creation)

## Status by Area (high-level)

- Core language: see [RFC 000] / [RFC 008]
- Tooling (build/run/fmt/test): see the CLI docs and [RFC 001]/[RFC 002]/[RFC 004]/[RFC 007] for the planned testing surface
- Rust interop: see [RFC 005] / [RFC 013] and the [Rust Interop guide](language/how-to/rust_interop.md)
- Web: see [Web Framework guide](language/tutorials/web_framework.md) (stabilization ongoing); product-surface direction and phased delivery in [RFC 003](RFCs/003_frontend_wasm.md)

## Upcoming (next)

- Web product surfaces per [RFC 003](RFCs/003_frontend_wasm.md) Phase 1 (typed handlers, packaging, mutations, browser UI)
- Test runner fixture execution (setup/teardown lifecycle)
- Python-style generators ([RFC 006]) — `yield` + `Iterator[T]` → Rust `gen` blocks
- Inline tests ([RFC 007]) — `@test` in source files, Rust-style proximity
- **Blocked / later (RFC 003 Phase 2 optional):** WASM/JSX-native parser & codegen, `--target wasm`, dev server + prod pipeline tuned for WASM, WebGPU-style 3D (see RFC appendix)

## Deferred / Later

The following items are intentionally deferred to later, and might be revisited in the future:

- SSR/SSG for frontend: Server-Side Rendering / Static Site Generation for the WASM/UI stack (render pages ahead of time
    or on the server, then hydrate).
- Desktop/mobile via wgpu: using the wgpu graphics stack to run Incan apps as native desktop/mobile apps (instead of
    browser-only).
- CRDT/collab features: real-time collaboration primitives (Conflict-free Replicated Data Types) for things like
    collaborative editing, shared state, etc.

### Guides

- Web framework guide: [Web framework](language/tutorials/web_framework.md)
- Rust interop guide: [Rust interop](language/how-to/rust_interop.md)
- Testing guide: [Testing](tooling/how-to/testing.md)

### Interested in contributing?

See the [Contributing](contributing/index.md) page for more information about contributing to Incan.

--8<-- "_snippets/rfcs_refs.md"
