# Test corpus inventory

!!! warning "Generated control-plane reference"

    Do not edit this page by hand. Regenerate it with `make test-inventory` from the tests in the tree and `scripts/test_inventory/dispositions.json`; `make test-inventory-check` fails when it is stale.

    Temporary. This page, the dispositions record and the `test-inventory` gate exist for the v0.6 cutover only: they retire with [#654](https://github.com/encero-systems/incan/issues/654) once no `retire` row remains and the durable corpus is frozen. Nothing here is a lasting contributor contract.

This is the control plane for the slice-7 cutover (issue [#1561](https://github.com/encero-systems/incan/issues/1561), test corpus). Every Rust test under `loaves/` and `workspaces/` has a disposition by what it proves and how, not by where it lives; every `.incn` fixture root is listed with its case count. A disposition is a recorded decision, the lane signals beside it are the mechanical evidence, and a `retire` row may be deleted only once it names a twin or records `dies`. How to classify a test, record a twin, or plan a split is recorded on the owning issue: [working the test corpus inventory](https://github.com/encero-systems/incan/issues/1561#issuecomment-5750194193).

## Summary

| Disposition | Tests | Files | Fixture cases |
|---|---:|---:|---:|
| keep | 3198 | 213 | 7 |
| re-point | 504 | 52 | 688 |
| retire | 1141 | 82 | 0 |
| unaffected | 1437 | 134 | 5 |
| unreviewed | 0 | 0 | 0 |
| **Total** | **6280** | **481** | **700** |

- Retire-class tests: 1141, of which twinned 467, dies 225, open 449 (neither yet).
- Retire-class files with open rows: 57 (a file whose retire tests are all twinned or recorded `dies` is done).
- Files whose test region exceeds the split threshold of 1500 lines: 18, of which 6 in the durable corpus (keep or re-point).
- Unreviewed files: 0.

## Dispositions

| Disposition | Meaning |
|---|---|
| `keep` | Asserts source meaning through the parser, typechecker, Body IR, formatter, LSP or semantics core, and never touches generated Rust. Survives the slice-7 cutover untouched. |
| `re-point` | Asserts program behavior (output, exit code, diagnostics of a run) but proves it by building or running generated Rust. The assertion stays; slice 7 changes the route. |
| `retire` | Asserts the shape of the generated Rust itself: snapshot text, `contains("fn ...")` on emitted source, emitter unit tests. Dies with #654, and only after its row names a twin or records `dies` with the reason. |
| `unaffected` | Oven, store, rustc, installer, stdlib runtime, layering guards and other tests the cutover does not touch. Listed so the total reconciles. |
| `unreviewed` | Nobody has read the file yet. The mechanical proposal is recorded in the notes when there is one; the maintainer works these rows through. |

## Twins and `dies`

A `retire` row leaves the corpus by naming what proves the behavior after the cutover in its `twin` field, or by recording that nothing user-observable is lost. `Twins` counts the first kind against the file's retire-class tests; `Dies` counts the second.

| `twin` | Meaning |
|---|---|
| `path::fn` | a `keep` or `re-point` test that proves the same behavior. |
| a fixture root | a declared `.incn` fixture root, by its bare path, when running that root's programs proves the behavior. |
| a behavior fixture | a file or directory under `loaves/compiler/incan_test_support/fixtures/behavior/<area>/`: an Incan program whose header declares its expected observables and names the tests it retires in `# retires:` lines. The gate refuses a fixture and a row that do not name each other. |
| `dies` | the test has no user-observable behavior to twin; the reason is recorded in a `dies` field beside it and the page shows it. Generated projects, `inspect rust` output and the build-report Cargo fields die with #654 (no Rust is generated at all any more); a data-structure invariant of a dying crate dies with the crate. |

## Lane signals

The collector counts these in the text of each test function and of the file-local helpers it calls. They are evidence, not the verdict: the disposition column is what the reviewer recorded. Helpers that live in a `#[path = "support/..."]` module outside the file are invisible to the scanner, so a test that drives the shadow comparison through such a helper shows only the lanes its own text carries. A `#[cfg_attr(..., test)]` attribute and a one-line `#[test] fn ...` are not counted; the tree has neither.

| Signal | Fires when the test |
|---|---|
| `codegen` | calls a codegen API (`IrCodegen`, `try_generate`, `generate_rust`, `emit_program`, `read_generated_rust`) or reads generated Rust (`target/incan/<project>/src/*.rs`, `incan --emit-rust`) |
| `snapshot` | asserts an `insta` snapshot |
| `generated_text` | asserts on generated Rust text (`contains("fn ")`, `contains("impl ")` and the like) |
| `build_run` | builds or runs generated Rust (`run_explicit_oven_bake`, `compare_source_observable`, a `cargo`/`rustc` command, or a CLI invocation such as `run_incan` / `incan_command` beside a `build`, `run`, `test` or `bake` subcommand or a generated-target read; `incan fmt`, `incan check`, `--help` and `--version` alone do not count) |
| `replacement` | uses the replacement route or Body IR (`replacement::`, `shadow_support`, `body_ir`, `lower_typed_body_ir`, `execute_free_function`) |
| `checker` | typechecks or reads diagnostics (`TypeChecker`, `check_str`, `CompileError`, `CompilationSession`) |
| `parser` | lexes or parses (`parser::parse`, `parse_str`, `lexer::lex`) |
| `legacy_ir` | lowers through the Rust-source backend's own IR (`AstLowering`, `lower_program`, `IrProgram`, `IrType`), which #654 removes with the emitter |
| `formatter` | formats source (`format_source`, `incan_format`) |
| `lsp` | drives the language server (`incan_lsp::`, `tower_lsp`, `lsp_types::`, hover and completion params) |

## Fixture roots

`.incn` fixtures are programs; the compiler suite, the example runner and the verified documentation examples run them as programs, so they are inventoried by root rather than per file.

| Root | Pattern | Cases | Disposition | Owner | Notes |
|---|---|---:|---|---|---|
| `examples` | `**/*.incn` | 91 | re-point | #1561 | typechecked and run by scripts/run_examples.sh (`make examples`); vocab and library examples are baked. |
| `loaves/compiler/incan_driver/src/replacement_compatibility/migration_baselines` | `**/*.incn` | 1 | unaffected | #1561 | frozen v0.5.0 migration baseline, decoded through the checked capability-metadata path (no generated Rust); retained only until the v0.5 replacement migration closes, per the compatibility inventory's retirement line, then removed from the collector. |
| `loaves/compiler/incan_driver/tests/fixtures` | `**/*.incn` | 20 | re-point | #1561 | driver integration fixtures (generated_rust_* artifact projects, callability, native consumer); their owner tests are retire-class. |
| `loaves/compiler/incan_emit/tests/codegen_snapshots` | `**/*.incn` | 180 | re-point | #1561 | snapshot corpus inputs considered as programs; the .snap outputs retire with codegen_snapshot_tests.rs. |
| `loaves/compiler/incan_test_support/fixtures` | `*.incn` | 12 | re-point | #1561 | top-level regression programs run by CLI integration tests (rfc023/rfc030/rfc064/rfc088 behavior, reflection, model traits). |
| `loaves/compiler/incan_test_support/fixtures/behavior/cli` | `<name>.incn or <name>/` | 31 | re-point | #1561 | behavior fixtures twinning the retire-class tests under loaves/toolchain/incan-cli (generated-text assertions after a build, IrCodegen smoke tests, an `--emit-rust`-only test), run by behavior_cli_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire test it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/cli_dependencies` | `<name>.incn or <name>/` | 4 | re-point | #1561 | behavior fixtures that are projects with in-fixture path dependencies (`[dependencies] <name> = { path = "deps/<name>" }`, reached through `pub::<name>`): the runner bakes every provider in dependency order before the run, with no Cargo authority, so a twin can prove what a consumer prints (or which diagnostic refuses it) across a package boundary. Run by behavior_cli_dependencies_tests.rs; a provider that would need Cargo (one that itself declares `[dependencies]`) fails its fixture at the suite's Cargo guard, so such fixtures stay parked. Each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/driver` | `<name>.incn or <name>/` | 3 | re-point | #1561 | behavior fixtures twinning incan_driver retire tests whose surviving observable is a program's output or a check-time diagnostic rather than the generated project they inspected; run by behavior_driver_tests.rs. Each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/harness` | `<name>.incn or <name>/` | 8 | re-point | #1561 | the behavior-fixture harness proving itself: one fixture per shape of the format (refused program with one code and with two, non-zero exit, exit code as the only observable, empty stdout, contained lines, module directory, project directory), run by behavior_harness_tests.rs. They twin nothing; they exist so a runner or route change is caught here before it is caught in a twin. Header refusals are unit tests of parse_header, not fixtures. |
| `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations` | `<name>.incn or <name>/` | 16 | re-point | #1561 | behavior fixtures twinning incan_ir retire tests about declarations: models, enums, generics, frozen collections, exact float widths, RFC 018 assertions, serde derive propagation, module-scoped traits (module fixtures) and two check-time refusals; run by behavior_lowering_tests.rs. Each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies` | `<name>.incn or <name>/` | 5 | re-point | #1561 | behavior fixtures twinning incan_ir retire tests whose observable crosses a package boundary: projects with in-fixture providers (`[dependencies] <name> = { path = "deps/<name>" }`) that the runner bakes first with no Cargo authority; run by behavior_lowering_tests.rs. Each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions` | `<name>.incn or <name>/` | 15 | re-point | #1561 | behavior fixtures twinning incan_ir retire tests about expressions: call shapes, method-kind classification, iterator and Result surfaces, trait dispatch by receiver shape, decorators, partials and the std JSON protocol; run by behavior_lowering_tests.rs. Each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds` | `<name>.incn or <name>/` | 7 | re-point | #1561 | behavior fixtures: exact-width float arithmetic and its finiteness check, and generic functions and models whose inferred bounds are satisfied by concrete types; twins of conversions.rs and trait_bound_inference.rs tests, run by behavior_ownership_tests.rs. A fixture is proved by running a program, so it is re-point and stays valid when the Rust-source backend is removed (#654); each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers` | `<name>.incn or <name>/` | 8 | re-point | #1561 | behavior fixtures: parameters the callee mutates, dict probes through them, loops over strings, bytes and enum lists, comprehension and list() sources, a bounded read through a buffer, self returned as a value, channel ends; twins of ownership.rs and conversions.rs planner tests, run by behavior_ownership_tests.rs. A fixture is proved by running a program, so it is re-point and stays valid when the Rust-source backend is removed (#654); each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries` | `<name>.incn or <name>/` | 7 | re-point | #1561 | behavior fixtures: strings crossing call, assignment, return, field and collection boundaries (literals, variables, consts, statics, frozen strings) and primitives that need no conversion; twins of conversions.rs and ownership.rs planner tests, run by behavior_ownership_tests.rs. A fixture is proved by running a program, so it is re-point and stays valid when the Rust-source backend is removed (#654); each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields` | `<name>.incn or <name>/` | 8 | re-point | #1561 | behavior fixtures: values passed and used again, fields read out of owners, loop bindings stored into results, generator sources consumed or reused, a field read through a web extractor wrapper; twins of conversions.rs, ownership.rs and implicit_borrowing planner tests, run by behavior_ownership_tests.rs. A fixture is proved by running a program, so it is re-point and stays valid when the Rust-source backend is removed (#654); each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/smoke` | `<name>.incn or <name>/` | 5 | re-point | #1561 | behavior fixtures: programs with their expected observables in the header, run by behavior_smoke_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings` | `<name>.incn or <name>/` | 39 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over lists, dicts, sets, comprehensions, iterators, strings and builtins; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching` | `<name>.incn or <name>/` | 18 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over enums, patterns, match, unions and `isinstance`; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections` | `<name>.incn or <name>/` | 28 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over functions, calls, decorators, aliases, partials and RFC 120 projections; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes` | `<name>.incn or <name>/` | 21 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over models, classes, fields, constructors and properties; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde` | `<name>.incn or <name>/` | 16 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over newtypes, validation, JSON and serde trait adoption; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib` | `<name>.incn or <name>/` | 32 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over stdlib module surfaces (math, fs, tempfile, testing, async, derives, registry, graph, uuid, regex, compression, traits, web); run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics` | `<name>.incn or <name>/` | 23 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over traits, supertraits, bounds, generics, protocol hooks and fallible iteration; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow` | `<name>.incn or <name>/` | 17 | re-point | #1561 | behavior fixtures twinning codegen snapshot tests over literals, operators, numerics, assignments, control flow, consts and statics; run by behavior_snapshots_tests.rs. A fixture is proved by running a program, so it is re-point today and stays valid after the route flips; each names the retire tests it twins in `# retires:` lines. |
| `loaves/compiler/incan_test_support/fixtures/invalid` | `**/*.incn` | 7 | keep | #1561 | diagnostics through the checker only (test_invalid_fixtures). |
| `loaves/compiler/incan_test_support/fixtures/oven_project_bake` | `**/*.incn` | 1 | re-point | #1561 | Incan project baked under Oven by the compiler suite; the program's route changes, the bake harness does not. |
| `loaves/compiler/incan_test_support/fixtures/oven_release_app_bake` | `**/*.incn` | 1 | re-point | #1561 | release-profile Oven bake of an Incan app. |
| `loaves/compiler/incan_test_support/fixtures/oven_release_bytes_io` | `**/*.incn` | 2 | re-point | #1561 | release-profile Oven bake exercising bytes I/O. |
| `loaves/compiler/incan_test_support/fixtures/oven_release_file_lock` | `**/*.incn` | 2 | re-point | #1561 | release-profile Oven bake exercising file locks. |
| `loaves/compiler/incan_test_support/fixtures/valid` | `**/*.incn` | 28 | re-point | #1561 | typechecked in bulk by test_valid_fixtures (keep) and run one by one as std surface programs by CLI tests (re-point). |
| `loaves/oven/oven_rustc/src/fixtures` | `**/*.incn` | 4 | unaffected | #1561 | Oven rustc fixtures. |
| `loaves/toolchain/incan-cli/tests/fixtures` | `**/*.incn` | 8 | re-point | #1561 | CLI integration fixtures (layering, package boundary facade, pub union consumer, vocab guardrails). |
| `workspaces/benchmarks` | `**/*.incn` | 9 | re-point | #1561 | benchmark programs built and timed by workspaces/benchmarks/run_all.sh (`make benchmarks`); the route changes, the timing harness does not. |
| `workspaces/docs-site/docs/_snippets/language/examples` | `verified_*.incn` | 8 | re-point | #1561 | verified documentation examples checked by scripts/check_docs_examples.sh. |
| `workspaces/oven/src` | `test_*.incn` | 11 | re-point | #1561 | Incan tests of the Oven release-policy project (workspaces/oven), run through `incan test`. |
| `workspaces/oven/tests/fixtures` | `**/*.incn` | 4 | re-point | #1561 | fixtures of the Oven release-policy project's tests. |

## Test files by crate

`Lines` is the file length; `Test lines` is the test region the split threshold applies to: the `#[cfg(test)]` modules when the file has any, otherwise the whole file. `Twins` is `twinned/retire-class` and `Dies` the number recorded `dies`, for files with retire-class tests. Per-test rows follow a file only when it carries per-test overrides.

### `loaves/compiler/incan_driver` (745 tests in 107 files: keep 362, re-point 138, retire 102, unaffected 143)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/compiler/incan_driver/src/backend/c_abi.rs` | 11 | 1074 | 333 | unaffected | - | - | - | #1561 | - | clang verification of checked C signatures; not the Rust backend. |
| `loaves/compiler/incan_driver/src/backend/project/cargo_toml.rs` | 22 | 1056 | 675 | retire | 0/22 | 22 | - | #1561 | run 20 | generated Cargo project shape; dies with the generated-project route |
| `loaves/compiler/incan_driver/src/backend/project/generator.rs` | 22 | 3250 | 1373 | retire (retire 22) | 3/22 | 19 | - | #1561 | codegen 5, text 9, run 20, checker 4 | generated Cargo project shape; dies with the generated-project route |
| `loaves/compiler/incan_driver/src/backend/project/lock_projection.rs` | 9 | 816 | 349 | retire | 0/9 | 9 | - | #1561 | - | generated Cargo project shape; dies with the generated-project route |
| `loaves/compiler/incan_driver/src/backend/project/plan.rs` | 2 | 229 | 23 | retire | 0/2 | 2 | - | #1561 | - | generated Cargo project shape; dies with the generated-project route |
| `loaves/compiler/incan_driver/src/backend/project/runner.rs` | 18 | 1031 | 449 | re-point | - | - | - | #1561 | codegen 2, run 14 | project runner: builds and runs the generated project; the run contract (profiles, rebuild triggers) is re-pointed at the replacement route. |
| `loaves/compiler/incan_driver/src/backend/project/tests/codegen_generator.rs` | 4 | 327 | 327 | retire | 0/4 | 4 | - | #1561 | codegen 4, text 1, run 4, checker 1, parser 3, legacy_ir 1 | codegen into a generated project; asserts generated Rust text. |
| `loaves/compiler/incan_driver/src/backend/project/tests/lock_payload.rs` | 1 | 26 | 26 | retire | 0/1 | 1 | - | #1561 | codegen 1, run 1 | generated Cargo project shape; dies with the generated-project route |
| `loaves/compiler/incan_driver/src/backend/shadow/abs_sum_profile_tests.rs` | 1 | 128 | 128 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/src/backend/shadow/enumerate_zip_tests.rs` | 5 | 164 | 164 | re-point | - | - | - | #1561 | run 3, replacement 4, checker 4, parser 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/src/backend/shadow/json_stringify_tests.rs` | 3 | 219 | 219 | re-point | - | - | - | #1561 | run 3, replacement 3 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/src/backend/shadow/legacy_oven.rs` | 1 | 669 | 59 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/src/backend/shadow/len_string_tests.rs` | 2 | 82 | 82 | re-point | - | - | - | #1561 | run 2, replacement 2, checker 2 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/src/backend/shadow/tests.rs` | 47 | 1253 | 1253 | re-point | - | - | - | #1561 | run 3, replacement 30 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. Result-report transport tests are the comparison harness itself. |
| `loaves/compiler/incan_driver/src/build/bake.rs` | 5 | 1137 | 172 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/caller_owned.rs` | 7 | 1044 | 315 | unaffected | - | - | - | #1561 | checker 4 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/inline_command.rs` | 5 | 129 | 75 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/library_exports.rs` | 12 | 933 | 559 | keep | - | - | - | #1561 | checker 8, parser 9 | library re-export resolution and Rust ABI query paths from checked declarations. |
| `loaves/compiler/incan_driver/src/build/library_outputs.rs` | 3 | 253 | 59 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/library_publication.rs` | 6 | 609 | 218 | unaffected | - | - | - | #1561 | checker 6 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/mod.rs` | 3 | 938 | 79 | unaffected | - | - | - | #1561 | replacement 2 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/output_materialization.rs` | 2 | 1273 | 411 | unaffected | - | - | - | #1561 | replacement 2 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/output_paths.rs` | 5 | 971 | 228 | unaffected | - | - | - | #1561 | codegen 1, run 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/output_selection.rs` | 1 | 1091 | 358 | unaffected | - | - | - | #1561 | replacement 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/oven_project.rs` | 6 | 1326 | 148 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/package_loafs.rs` | 2 | 1014 | 435 | unaffected | - | - | - | #1561 | text 1, checker 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/plan_authority.rs` | 4 | 1062 | 191 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/plan_selection.rs` | 6 | 1022 | 469 | unaffected | - | - | - | #1561 | codegen 1, run 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/prepare_project.rs` | 1 | 497 | 74 | retire | 0/1 | 1 | - | #1561 | codegen 1, run 1, checker 1 | prunes the generated project's Cargo dependencies; generated Cargo project shape; dies with the generated-project route |
| `loaves/compiler/incan_driver/src/build/provider_compilation.rs` | 6 | 1010 | 529 | unaffected | - | - | - | #1561 | checker 2 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/provider_metadata.rs` | 5 | 1111 | 266 | keep | - | - | - | #1561 | checker 3, parser 3 | provider operation metadata projected from checked declaration facts. |
| `loaves/compiler/incan_driver/src/build/publication.rs` | 1 | 798 | 63 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/replacement.rs` | 5 | 533 | 161 | keep | - | - | - | #1561 | replacement 4, checker 4, parser 1 | replacement build pipeline (session projection, exact numeric report). |
| `loaves/compiler/incan_driver/src/build/reuse.rs` | 1 | 679 | 35 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build/source_authority.rs` | 19 | 1846 | 1171 | unaffected | - | - | - | #1561 | codegen 1, checker 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/build_unit.rs` | 1 | 202 | 27 | unaffected | - | - | - | #1561 | checker 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/cargo_policy.rs` | 4 | 270 | 92 | retire (retire 4) | 2/4 | 2 | - | #1561 | - | Two tests die with the Cargo command line (arg ordering, `INCAN_CARGO_ARGS`); the `INCAN_LOCKED/FROZEN/OFFLINE` env defaults, the `--no-locked/--no-offline/--no-frozen` negations and frozen ⇒ locked+offline survive as Oven lock-policy inputs (maintainer ruling, 2026-09-20) and are twinned by re-point CLI tests. |
| `loaves/compiler/incan_driver/src/generated_cache.rs` | 18 | 1510 | 447 | retire | 0/18 | 18 | - | #1561 | - | generated-project cache identity and pruning; dies with the generated-project route. |
| `loaves/compiler/incan_driver/src/inspect/closure.rs` | 8 | 409 | 224 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/inspect/codegraph.rs` | 13 | 4549 | 849 | keep | - | - | - | #1561 | replacement 3, checker 9, parser 8 | codegraph projection from checked facts. |
| `loaves/compiler/incan_driver/src/lock/mod.rs` | 2 | 558 | 159 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/lock/registry_sources.rs` | 3 | 517 | 48 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/lock/resolution.rs` | 3 | 751 | 85 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/lock/rust_inspect.rs` | 2 | 536 | 97 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/lock/test_inputs.rs` | 2 | 200 | 91 | unaffected | - | - | - | #1561 | checker 2, parser 1 | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/lock/workspace.rs` | 1 | 587 | 45 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/modules.rs` | 23 | 2041 | 1166 | keep | - | - | - | #1561 | replacement 1, checker 19, parser 20 | module collection and Rust dependency use discovery through the parser. |
| `loaves/compiler/incan_driver/src/project.rs` | 9 | 407 | 105 | unaffected | - | - | - | #1561 | - | build orchestration over Oven (loafs, providers, publication, locks); not the Rust backend. |
| `loaves/compiler/incan_driver/src/replacement_compatibility.rs` | 12 | 3938 | 310 | keep | - | - | - | #1561 | replacement 12, formatter 10 | replacement compatibility inventory collector and validator. |
| `loaves/compiler/incan_driver/src/rust_inspect_workspace.rs` | 19 | 1885 | 867 | unaffected | - | - | - | #1561 | codegen 5, text 1, run 9, checker 1 | generated rust-inspect Cargo workspace that feeds the checker with Rust metadata; not the Rust backend. |
| `loaves/compiler/incan_driver/src/session.rs` | 4 | 911 | 175 | keep | - | - | - | #1561 | checker 4, parser 2 | CompilationSession analysis (provider plan reuse, feature projection). |
| `loaves/compiler/incan_driver/src/shadow_support.rs` | 2 | 252 | 28 | re-point | - | - | - | #1561 | checker 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/src/testing/discovery.rs` | 33 | 1973 | 829 | keep | - | - | - | #1561 | checker 32, parser 32 | test discovery through the parser. |
| `loaves/compiler/incan_driver/src/testing/module_graph.rs` | 3 | 412 | 128 | keep | - | - | - | #1561 | checker 1, parser 3 | test-runner module graph through the parser. |
| `loaves/compiler/incan_driver/src/tests/executable_session.rs` | 1 | 81 | 81 | keep | - | - | - | #1561 | replacement 1, checker 1 | canonical frames through a checked cycle. |
| `loaves/compiler/incan_driver/src/typecheck.rs` | 3 | 356 | 73 | keep | - | - | - | #1561 | checker 2 | typecheck over the import graph. |
| `loaves/compiler/incan_driver/tests/duration_artifact_tests.rs` | 1 | 67 | 67 | re-point | - | - | - | #1561 | run 1 | runs a program and checks the duration artifact. |
| `loaves/compiler/incan_driver/tests/emitted_symbol_projection_tests.rs` | 1 | 38 | 38 | retire | 0/1 | 1 | - | #1561 | - | RFC 120 emitted symbol projection and demangling; generated Rust shape. |
| `loaves/compiler/incan_driver/tests/fixtures/generated_rust_native_consumer/consumer/src/lib.rs` | 1 | 47 | 47 | retire | 0/1 | 1 | - | #1561 | - | fixture consumer of generated_rust_native_consumer_tests; follows its owner. |
| `loaves/compiler/incan_driver/tests/generated_cache_integration.rs` | 4 | 468 | 468 | retire (retire 4) | 0/4 | 4 | - | #1561 | run 4 | generated-project cache across builds; dies with the generated-project route. |
| `loaves/compiler/incan_driver/tests/generated_rust_artifact_tests.rs` | 5 | 567 | 567 | retire (retire 5) | 2/5 | 3 | - | #1561 | run 5, checker 2 | public generated-Rust artifact contract (RFC 120 projections, baseline generated projects); three rows die with the generated project, one is twinned by a refused-program project fixture, one stays open on a question the maintainer decides (the `.incnlib` manifest's fate; the provider bake its consumer-run twin would need now exists in the fixture runner). |
| `loaves/compiler/incan_driver/tests/generated_rust_audit_tests.rs` | 4 | 193 | 193 | retire | 0/4 | 4 | - | #1561 | - | public generated-Rust artifact contract (RFC 120 projections, native consumers, audit) |
| `loaves/compiler/incan_driver/tests/generated_rust_callability_artifact_tests.rs` | 1 | 258 | 258 | retire | 1/1 | 0 | - | #1561 | text 1, run 1, checker 1 | The generated Cargo manifests and the `transforms.rs`/`main.rs` text (fn-pointer parameters, qualified provider calls) die with #654; the surviving observable, the consumer run printing `2 3 4` (a `Callable` value passed across a `pub::` package boundary, which no other test runs), is the twin: a project fixture carrying the producer under `deps/callability_core`, which the runner bakes before `incan run` (harness-deps). |
| `loaves/compiler/incan_driver/tests/generated_rust_native_consumer_tests.rs` | 1 | 367 | 367 | retire | 0/1 | 1 | - | #1561 | run 1 | public generated-Rust artifact contract (RFC 120 projections, native consumers, audit) |
| `loaves/compiler/incan_driver/tests/parity_corpus_tests/corpus_proofs.rs` | 7 | 456 | 456 | re-point | - | - | - | #1561 | - | split of parity_corpus_tests.rs (the parity corpus: slice 7's own measurement instrument; the corpus support helper lives outside the file in `loaves/compiler/incan_driver/tests/support/parity_corpus.rs`, so the scanner sees only the `replacement` lane); corpus validation: the red-state schema proof, the green-state structural and behavioral proofs, the replacement rows' receipt-bound evidence and the CI summary. |
| `loaves/compiler/incan_driver/tests/parity_corpus_tests/paired_row_proofs.rs` | 15 | 857 | 857 | re-point | - | - | - | #1561 | replacement 6 | split of parity_corpus_tests.rs (the parity corpus: slice 7's own measurement instrument; the corpus support helper lives outside the file in `loaves/compiler/incan_driver/tests/support/parity_corpus.rs`, so the scanner sees only the `replacement` lane); the paired-comparison rows: these tests assert a green (or explicitly unavailable) legacy-vs-replacement shadow comparison through `shadow_support::compare_source_observable`, the same evidence class as `backend/shadow/**`: slice 7 (#1675) re-points that baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/parity_corpus_tests/rfc_120_proofs.rs` | 6 | 334 | 334 | re-point | - | - | - | #1561 | - | split of parity_corpus_tests.rs (the parity corpus: slice 7's own measurement instrument; the corpus support helper lives outside the file in `loaves/compiler/incan_driver/tests/support/parity_corpus.rs`, so the scanner sees only the `replacement` lane); the RFC 120 coverage and evidence proofs. |
| `loaves/compiler/incan_driver/tests/protected_builtin_binding_tests.rs` | 3 | 141 | 141 | keep | - | - | - | #1561 | checker 3, parser 3 | lex/parse/typecheck only. |
| `loaves/compiler/incan_driver/tests/protected_generic_binding_tests.rs` | 2 | 106 | 106 | keep | - | - | - | #1561 | checker 2, parser 2 | lex/parse/typecheck only. |
| `loaves/compiler/incan_driver/tests/replacement_abs_sum_profile_shadow_tests.rs` | 1 | 154 | 154 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_abs_sum_profile_tests.rs` | 4 | 199 | 199 | keep | - | - | - | #1561 | run 1, replacement 3, checker 3, parser 3 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/async_tasks_and_race.rs` | 10 | 467 | 467 | keep | - | - | - | #1561 | run 4, replacement 6 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); async tasks and `race`, direct and through the CLI. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/bodies_bindings_and_scalars.rs` | 16 | 546 | 546 | keep | - | - | - | #1561 | replacement 16 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); core body execution, bindings, projections and scalars. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/callables_and_generators.rs` | 19 | 772 | 772 | keep | - | - | - | #1561 | replacement 19 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); named calls, closures, partials, callable defaults and generators. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/cli_nominal_and_enum_values.rs` | 9 | 703 | 703 | keep | - | - | - | #1561 | run 9 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); CLI-driven nominal and enum programs with their receipts and refusals. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/cli_refusals_shadow_and_boundaries.rs` | 13 | 815 | 815 | keep | - | - | - | #1561 | run 13, replacement 1 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); CLI-driven refusals without a receipt, shadow requests that stay explicitly non-green, module and Rust interop boundaries. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/collections_strings_and_print.rs` | 18 | 497 | 497 | keep (keep 17, retire 1) | 1/1 | 0 | - | #1561 | codegen 1, run 1, replacement 17, parser 1 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); collections, strings, scalar JSON and `print`; one test compares against the legacy backend and retires to its behavior fixture. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/modules_and_sessions.rs` | 14 | 921 | 921 | keep | - | - | - | #1561 | run 10, replacement 4, checker 4, parser 4 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); the execution graph's cross-module resolution and the CLI-driven module, facade and session tests. |
| `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/nominal_and_enum_values.rs` | 19 | 995 | 995 | keep (keep 19) | - | - | - | #1561 | replacement 19 | split of replacement_backend_execution_tests.rs (Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep); source-local nominal models and enums through a direct callable, with the identity and layout refusals. |
| `loaves/compiler/incan_driver/tests/replacement_bool_truthiness_shadow_tests.rs` | 1 | 77 | 77 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_bool_truthiness_tests.rs` | 6 | 198 | 198 | keep | - | - | - | #1561 | run 1, replacement 5, checker 5, parser 5 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_collection_len_shadow_tests.rs` | 1 | 77 | 77 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_collection_len_tests.rs` | 4 | 153 | 153 | keep | - | - | - | #1561 | run 1, replacement 3, checker 3, parser 3 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_compatibility_registry_tests.rs` | 6 | 360 | 360 | keep | - | - | - | #1561 | - | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_enumerate_zip_boundary_tests.rs` | 10 | 728 | 728 | keep | - | - | - | #1561 | replacement 10, checker 10, parser 10 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_enumerate_zip_parity_cases.rs` | 4 | 162 | 162 | re-point | - | - | - | #1561 | run 4, replacement 4 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_enumerate_zip_shadow_tests.rs` | 1 | 128 | 128 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_enumerate_zip_tests.rs` | 15 | 552 | 552 | keep | - | - | - | #1561 | run 2, replacement 13, checker 13, parser 13 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_example_coverage.rs` | 1 | 215 | 215 | keep | - | - | - | #1561 | replacement 1, checker 1, parser 1 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_hashed_container_boundary_tests.rs` | 1 | 48 | 48 | keep | - | - | - | #1561 | replacement 1, checker 1, parser 1 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_hashed_execution_tests.rs` | 12 | 260 | 260 | keep | - | - | - | #1561 | run 1, replacement 11, checker 10, parser 10 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_hashed_shadow_tests.rs` | 1 | 81 | 81 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_isinstance_shadow_tests.rs` | 1 | 75 | 75 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_isinstance_tests.rs` | 8 | 334 | 334 | keep | - | - | - | #1561 | replacement 8, checker 8, parser 8 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_program_io_tests.rs` | 5 | 144 | 144 | keep | - | - | - | #1561 | run 5 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_scalar_conversion_shadow_tests.rs` | 7 | 358 | 358 | re-point | - | - | - | #1561 | run 7, replacement 7 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_scalar_conversion_tests.rs` | 16 | 803 | 803 | keep | - | - | - | #1561 | run 1, replacement 15, checker 15, parser 15 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_sorted_int_list_shadow_tests.rs` | 1 | 78 | 78 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_sorted_int_list_tests.rs` | 7 | 223 | 223 | keep | - | - | - | #1561 | run 1, replacement 6, checker 6, parser 6 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_stream_writer_tests.rs` | 7 | 239 | 239 | keep | - | - | - | #1561 | replacement 6, checker 6, parser 6 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_string_helper_execution_tests.rs` | 7 | 238 | 238 | keep | - | - | - | #1561 | run 1, replacement 7, checker 6, parser 6 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_string_helper_shadow_tests.rs` | 2 | 96 | 96 | re-point | - | - | - | #1561 | run 2, replacement 2 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_string_len_execution_tests.rs` | 4 | 96 | 96 | keep | - | - | - | #1561 | replacement 4, checker 4, parser 4 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/replacement_string_len_shadow_tests.rs` | 1 | 50 | 50 | re-point | - | - | - | #1561 | run 1, replacement 1 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/replacement_typed_numeric_tests.rs` | 13 | 581 | 581 | keep | - | - | - | #1561 | replacement 13, checker 13, parser 13 | Body IR lowering and replacement execution; the CLI-driven tests run the replacement route and stay keep. |
| `loaves/compiler/incan_driver/tests/shadow_comparison_tests.rs` | 9 | 451 | 451 | re-point | - | - | - | #1561 | run 9, replacement 9 | shadow comparison against the legacy Oven baseline; slice 7 (#1675) re-points the baseline to the frozen corpus receipts or retires the comparison with the legacy route. |
| `loaves/compiler/incan_driver/tests/stdlib_version_artifact_tests.rs` | 1 | 132 | 132 | retire | 0/1 | 1 | - | #1561 | codegen 1, parser 1 | asserts the stdlib version check inside the generated artifact. |

Per-test overrides in `loaves/compiler/incan_driver/src/backend/project/generator.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `generated_lock_is_discarded_when_its_manifest_no_longer_matches` | retire | `dies` | the generated project's Cargo.lock and the manifest witness that decides whether Cargo's `--locked` may reuse it; the generated project's Cargo.lock dies with #654. | build_run | - |
| `generated_consumer_rebinds_absent_private_sdk_cache_root_issue911` | retire | `dies` | rebinds a compiled artifact's Cargo path dependencies from an absent SDK cache root to the active one, asserts the projected Cargo.toml paths, and links the rebound Rust with direct rustc (#911); the compiled artifact's Cargo graph and the generated consumer that projects it die with #654. | codegen, generated_text, build_run, checker | - |
| `nested_compiled_artifacts_propagate_sdk_projection_issue911` | retire | `dies` | asserts that a nested compiled artifact's SDK Cargo path dependencies are re-projected through its parent's shadow (`.incan-sdk-rebound-ready`, artifact digest) and that the rebound Rust links with direct rustc (#911); the compiled artifact's Cargo graph dies with #654. | codegen, generated_text, build_run, checker | - |
| `sdk_projection_rejects_cargo_source_mismatch_before_cargo_issue911` | retire | `dies` | asserts that a compiled artifact whose Cargo.toml path or `default-features` disagrees with its checked `.incnlib` descriptor is refused before Cargo runs, and that the projected Cargo.toml normalizes `default-features = false` (#911); a Cargo-manifest consistency check of the compiled artifact, which dies with #654. | codegen, generated_text, build_run, checker | - |
| `test_is_stdlib_path` | retire | `dies` | predicate of the generator's `std` -> `__incan_std` Rust module renaming; a data-structure invariant of the dying generated-project crate. | - | - |
| `test_transform_stdlib_path` | retire | `dies` | the generator's `std` -> `__incan_std` Rust module renaming, which exists only to keep generated `mod std;` from shadowing Rust's `std`; a data-structure invariant of the dying generated-project crate. | - | - |
| `test_generate_multi_escapes_keyword_module_names` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/driver/module_named_like_host_keyword` | - | generated_text, build_run | asserted the `r#async` raw-identifier escape and `#[path]` attribute in generated module declarations; the twin imports a top-level and a nested module named `async` (soft keyword in Incan, hard in the host language) and reads their output. The retired test's `type` module is unreachable from source: `type` is a hard Incan keyword and the parser refuses it as a module path segment. |
| `test_generate_nested_escapes_keyword_submodule_names` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/driver/module_named_like_host_keyword` | - | generated_text, build_run | asserted the `r#async` raw-identifier escape and `#[path]` attribute in generated module declarations; the twin imports a top-level and a nested module named `async` (soft keyword in Incan, hard in the host language) and reads their output. The retired test's `type` module is unreachable from source: `type` is a hard Incan keyword and the parser refuses it as a module path segment. |
| `checked_namespace_facades_skip_children_fully_bound_by_the_parent_issue948` | retire | `dies` | asserts the generator's `public_namespace_facades` map, the plan for which `pub use child::*` lines a generated `mod.rs` carries so rustc sees no ambiguous glob re-export (#948); a generated-Rust facade plan. The user-facing namespace behavior is covered by the typechecker's pub_imports_namespaces_and_fields.rs (keep) and rfc031_pub_import_integration_tests.rs (re-point). | build_run, checker | - |
| `test_generate_nested_avoids_cargo_root_filenames_for_top_level_modules` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/driver/module_named_like_host_root_file` | - | build_run | asserted the `__incan_mod_main.rs`/`__incan_mod_lib.rs` renames that keep top-level modules named `main`/`lib` out of the host project's root filenames; the twin imports a sibling module named `lib` beside the entrypoint and reads its output. A module named `main` beside `main.incn` is the entrypoint itself, so only `lib` is reachable from a script. |

Per-test overrides in `loaves/compiler/incan_driver/src/cargo_policy.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `cargo_policy_resolves_env_defaults_and_frozen_implication` | retire | `loaves/toolchain/incan-cli/tests/cli_workspace_and_lock_tests.rs::build_lock_policy_env_defaults_refuse_a_missing_lock` | - | - | `INCAN_LOCKED=1` and `INCAN_FROZEN=1` (frozen ⇒ locked) refuse a project without `oven.lock` and create none; the `INCAN_CARGO_ARGS` half dies with the Cargo command line. |
| `cargo_policy_uses_cli_extra_args_before_env_extra_args` | retire | `dies` | `INCAN_CARGO_ARGS` ordering against CLI extra args on the Cargo command line; dies with #654. | - | - |
| `cargo_policy_cli_disable_flags_override_env_defaults` | retire | `loaves/toolchain/incan-cli/tests/cli_workspace_and_lock_tests.rs::build_no_flags_override_lock_policy_env_defaults` | - | - | `--no-locked` / `--no-frozen` negate the environment defaults on the command line. |
| `cargo_command_flags_order_policy_features_then_extra_args` | retire | `dies` | Cargo argument ordering (policy flags, then features, then extra args) on the generated Cargo command line; dies with #654. | - | - |

Per-test overrides in `loaves/compiler/incan_driver/tests/generated_cache_integration.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `explicitly_baked_project_reuses_release_json_authority_without_cargo` | retire | `dies` | the `sealed project output … no longer matches this source tree` warning after an edit dies (maintainer ruling, 2026-09-20: a stale bake is re-baked transparently, no explicit rebake — the DX requirement is recorded on #1142, slice 8, with its re-point CLI test). The other observables are carried: `reused sealed project Loaf` on `build --locked` after a bake by `rfc031_pub_import_integration_tests.rs`; `oven.lock is out of date` after a manifest edit by `cli_workspace_and_lock_tests.rs` and `cli_interop_target_tests.rs`; `incan test tests --fail-on-empty` on a baked `std.json` project (#1056) by no other test — folded into the #1142 requirement as well. | build_run | - |

Per-test overrides in `loaves/compiler/incan_driver/tests/generated_rust_artifact_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `generated_application_artifact_matches_baseline` | retire | `dies` | the generated application project compared with a baseline: required files (`Cargo.toml` and a `main.rs` under `src`), no Cargo.lock, `main.rs` fragments, and the Cargo `[package]` table (name, version, edition, license; `license-files` not becoming Cargo's `license-file`) with `incan_std_core`/`incan_derive` dependencies. Generated projects die with #654. | build_run | - |
| `generated_application_without_package_metadata_uses_compiler_defaults` | retire | `dies` | the generated Cargo.toml `[package]` version defaulting to the compiler's and carrying no license when `loaf.toml` declares none; a generated-manifest assertion, dies with #654. | build_run | - |
| `generated_library_and_pub_dependency_consumer_artifacts_match_baseline` | retire | `loaves/toolchain/incan-cli/tests/rfc031_pub_import_integration_tests.rs::private_pub_model_survives_facade_library_and_test_batch_boundaries_issue884` | - | build_run, checker | The generated `lib.rs`/`widgets.rs`/consumer `main.rs` fragments and both Cargo manifests die with #654. The `.incnlib` fields are pinned by re-point rows (`cli_workspace_and_lock_tests.rs`, `cli_provider_boundary_tests.rs`, `package_boundary_facade_tests.rs`, `cli_decorator_and_partial_tests.rs`, `package_executable_representation.rs`); the consumer run (a provider baked, a consumer importing a `pub model`, stdout asserted) is carried by the named re-point test. |
| `path_dependency_artifact_rebuilds_for_a_b_a_feature_projections` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli_dependencies/refused_feature_gated_export_of_dependency` | - | build_run, checker | twinned by a refused-program project fixture: a consumer importing a feature-gated `pub::` export of its in-fixture provider while the package feature is off is refused with INCAN-I0103 (`incan check` prepares an unbaked provider's metadata itself). The alpha/beta/alpha re-bake sequence and the generated `lib.rs` half die with #654; the `.incnlib` `active_features` and `fact_requirements` fields are pinned by re-point rows (`cli_codegraph_and_inspection_tests.rs`, `cli_workspace_and_lock_tests.rs`, `package_executable_representation.rs`). |
| `a_package_projects_a_call_into_its_own_sibling_module` | retire | `dies` | asserts the RFC 120 `__incan_v1_` projected wrapper name, not the source spelling `c.bumped()`, in a package's generated `lib.rs` (#1174 emitted-name recoverability); the emitted-name projection and the legacy `incan_ir` lowering that required it die with generated Rust. The package-calls-its-own-sibling behavior is reachable only through a `--lib`/SDK component build, which is not a run the fixture format can express. The surviving observable — a package whose library calls a sibling module's method builds — is carried by the SDK provider prewarm every suite root performs. | build_run | - |

Per-test overrides in `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/collections_strings_and_print.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `both_backends_render_a_multi_argument_print_the_same_way` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/driver/println_multi_argument.incn` | - | codegen, replacement, parser | compared the replacement executor's rendering of `println("count", 3, true)` with the IrCodegen placeholder count; the twin runs the program and reads `count 3 true` from its stdout, and the replacement-only half stays in the keep sibling replacement_executes_print_by_recording_its_output. |

Per-test overrides in `loaves/compiler/incan_driver/tests/replacement_backend_execution_tests/nominal_and_enum_values.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `replacement_refuses_a_nominal_pattern_after_its_exact_target_identity_is_removed` | keep | - | - | replacement | generated-text hit is a diagnostic string |

### `loaves/compiler/incan_emit` (928 tests in 57 files: keep 100, retire 826, unaffected 2)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/compiler/incan_emit/src/checked_program/borrowed_rust_enum.rs` | 3 | 181 | 181 | retire | 0/3 | 0 | - | #1561 | codegen 1, checker 3, parser 3 | checked-program codegen of borrowed Rust enums. |
| `loaves/compiler/incan_emit/src/checked_program/embedded_fragment.rs` | 1 | 130 | 130 | retire | 0/1 | 0 | - | #1561 | codegen 1, checker 1, parser 1, legacy_ir 1 | checked-program codegen of embedded fragments. |
| `loaves/compiler/incan_emit/src/checked_program/rust_supertrait_codegen.rs` | 2 | 76 | 76 | retire | 0/2 | 0 | - | #1561 | codegen 2, text 1, checker 1, parser 2 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/src/checked_program/rust_trait_receiver_codegen.rs` | 2 | 196 | 196 | retire | 0/2 | 0 | - | #1561 | codegen 2, text 1, checker 2, parser 2 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/src/checked_program/sdk_module_derives.rs` | 5 | 257 | 257 | retire (keep 2, retire 3) | 0/3 | 0 | - | #1561 | codegen 3, checker 5, parser 5 | SDK module derive requirements flowing into codegen; two tests assert the metadata round trip only. |
| `loaves/compiler/incan_emit/src/checked_program/tests.rs` | 5 | 497 | 497 | keep (keep 4, retire 1) | 0/1 | 0 | - | #1561 | codegen 1, replacement 1, checker 4, parser 4, legacy_ir 1 | checked-program facts (callable shapes, forwarding metadata, vocab refusal); one test drives codegen. |
| `loaves/compiler/incan_emit/src/codegen.rs` | 129 | 8040 | 5501 | retire (retire 129) | 6/129 | 0 | required | #1561 | codegen 122, text 52, checker 112, parser 82, legacy_ir 115 | IrCodegen entry point and emitter-side metadata; every test drives IrCodegen or emitter-owned merges (manifest type refs, native-union capture). Split with the retirement, not before. |
| `loaves/compiler/incan_emit/src/codegen/capability_bridge.rs` | 1 | 190 | 18 | retire | 0/1 | 0 | - | #1561 | parser 1 | codegen capability activation for generated projects. |
| `loaves/compiler/incan_emit/src/codegen/dependency_metadata.rs` | 6 | 1086 | 167 | retire | 0/6 | 0 | - | #1561 | checker 1, parser 5, legacy_ir 1 | which stdlib and provider items the generated project links; generated-project shape. |
| `loaves/compiler/incan_emit/src/conversions.rs` | 79 | 2805 | 1578 | retire (retire 79) | 50/79 | 8 | required | #1561 | legacy_ir 74 | Rust conversion policy (to_string/borrow/clone plans) for emission; every test drives `determine_conversion` on synthetic IR. Twinned by the ownership_* behavior fixtures and re-point interop programs per test; `rust::` programs are candidates; the token renderers and the unspellable `Unknown` states die. |
| `loaves/compiler/incan_emit/src/emit/decls/functions.rs` | 3 | 1997 | 115 | retire | 0/3 | 0 | - | #1561 | codegen 3, legacy_ir 3 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/decls/mod.rs` | 5 | 1225 | 237 | retire | 0/5 | 0 | - | #1561 | codegen 2, checker 2, legacy_ir 2 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/decls/structures.rs` | 8 | 1212 | 244 | retire | 0/8 | 0 | - | #1561 | codegen 8, text 7, legacy_ir 8, formatter 8 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/errors.rs` | 1 | 45 | 15 | retire | 0/1 | 0 | - | #1561 | - | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/expressions/builtins.rs` | 5 | 1087 | 174 | retire | 0/5 | 0 | - | #1561 | codegen 3, legacy_ir 4 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/expressions/calls.rs` | 29 | 2805 | 1278 | retire | 0/29 | 0 | - | #1561 | codegen 29, legacy_ir 29 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/expressions/indexing.rs` | 2 | 474 | 68 | retire | 0/2 | 0 | - | #1561 | codegen 2, legacy_ir 2 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/expressions/methods.rs` | 2 | 1533 | 60 | retire (retire 2) | 2/2 | 0 | - | #1561 | codegen 2, legacy_ir 2 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/expressions/mod.rs` | 52 | 4409 | 2798 | retire | 0/52 | 0 | required | #1561 | codegen 52, legacy_ir 52 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/expressions/structs_enums.rs` | 1 | 145 | 20 | retire | 0/1 | 0 | - | #1561 | codegen 1, legacy_ir 1 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/mod.rs` | 20 | 4605 | 952 | retire | 0/20 | 0 | - | #1561 | codegen 15, checker 4, legacy_ir 18 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/native_unions.rs` | 8 | 2072 | 1082 | retire | 0/8 | 0 | - | #1561 | codegen 6, checker 7, parser 7, legacy_ir 6 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/program.rs` | 10 | 4635 | 348 | retire | 0/10 | 0 | - | #1561 | codegen 10, text 2, checker 6, legacy_ir 10 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/statements.rs` | 8 | 1942 | 315 | retire | 0/8 | 0 | - | #1561 | codegen 7, text 4, legacy_ir 8 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/emit/types.rs` | 3 | 689 | 61 | retire | 0/3 | 0 | - | #1561 | codegen 3, legacy_ir 2 | emitter unit tests; frozen with the emitter under #1561, deleted by #654 once twinned. |
| `loaves/compiler/incan_emit/src/ownership.rs` | 46 | 2014 | 895 | retire (retire 46) | 36/46 | 3 | - | #1561 | legacy_ir 46 | emitter-side ownership and argument plans (borrow/clone/move for generated Rust) on synthetic IR. Twinned by the ownership_* behavior fixtures and re-point interop programs per test; `rust::` programs are candidates; the unspellable item and receiver types die. |
| `loaves/compiler/incan_emit/src/reference_shape.rs` | 3 | 107 | 65 | retire | 0/3 | 0 | - | #1561 | legacy_ir 3 | Rust reference-shape helper for emitted callbacks. |
| `loaves/compiler/incan_emit/src/replacement/executable_resolution_tests.rs` | 17 | 1102 | 1102 | keep | - | - | - | #1561 | codegen 1, replacement 16, checker 17, parser 17 | replacement route (executable resolution, provider preflight, source profile). |
| `loaves/compiler/incan_emit/src/replacement/hashed/tests.rs` | 15 | 314 | 314 | keep | - | - | - | #1561 | replacement 15 | replacement route (executable resolution, provider preflight, source profile). |
| `loaves/compiler/incan_emit/src/replacement/mod.rs` | 4 | 8103 | 262 | keep | - | - | - | #1561 | replacement 4 | replacement route (executable resolution, provider preflight, source profile). |
| `loaves/compiler/incan_emit/src/replacement/provider/tests.rs` | 15 | 1004 | 1004 | keep | - | - | - | #1561 | replacement 15, checker 15, parser 15 | replacement route (executable resolution, provider preflight, source profile). |
| `loaves/compiler/incan_emit/src/replacement/provider/tests/host_preflight_tests.rs` | 13 | 589 | 589 | keep | - | - | - | #1561 | replacement 13 | replacement route (executable resolution, provider preflight, source profile). |
| `loaves/compiler/incan_emit/src/replacement/source_profile.rs` | 4 | 272 | 128 | keep | - | - | - | #1561 | replacement 3, parser 3 | replacement route (executable resolution, provider preflight, source profile). |
| `loaves/compiler/incan_emit/src/selection.rs` | 13 | 970 | 323 | keep | - | - | - | #1561 | replacement 13 | backend selection policy (replacement vs legacy); survives as the replacement route's own selection. |
| `loaves/compiler/incan_emit/src/tests/lowering_through_emission.rs` | 2 | 332 | 332 | retire | 0/2 | 0 | - | #1561 | codegen 1, checker 2, parser 2, legacy_ir 2 | lowering through emission end to end. |
| `loaves/compiler/incan_emit/src/tests/mut_marker_publication.rs` | 1 | 63 | 63 | retire | 0/1 | 0 | - | #1561 | codegen 1, checker 1, parser 1 | asserts the emitter's native-union projection of a published function type whose parameter carries the `mut` marker (#1790); retires with the Rust-source backend (#654), whose publication path it pins. |
| `loaves/compiler/incan_emit/src/trait_bound_inference.rs` | 15 | 4404 | 703 | retire (retire 15) | 5/15 | 4 | - | #1561 | codegen 5, legacy_ir 15 | infers Rust trait bounds for generated generics. Twinned by the ownership_numerics_and_bounds fixtures (a bound is proved by the program running for concrete types); the absent-bound tests are open on a candidate that instantiates a non-`Clone` Rust type; the metadata-free, legacy-encoding and conservative-fallback tests die; the implementation-bound tests are open on #1280. |
| `loaves/compiler/incan_emit/tests/checked_empty_collection_constructor_tests.rs` | 14 | 675 | 675 | retire (keep 5, retire 9) | 1/9 | 0 | - | #1561 | codegen 9, snapshot 1, text 8, replacement 4, checker 14, parser 14 | empty-collection constructors: generated-text and snapshot assertions retire; the checked-type and Body IR aggregate assertions stay. |
| `loaves/compiler/incan_emit/tests/closure_local_call_codegen_tests.rs` | 4 | 208 | 208 | retire | 0/4 | 0 | - | #1561 | codegen 4, text 1, checker 4, parser 4 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/tests/codegen_snapshot_tests.rs` | 289 | 6776 | 6776 | retire (retire 289) | 254/289 | 18 | required | #1561 | codegen 289, snapshot 1, text 31, checker 7, parser 289 | insta snapshot corpus of generated Rust (289 tests): every test generated Rust from a `codegen_snapshots/*.incn` input or an inline source and asserted a snapshot or `contains(...)`. The programs are twinned by behavior fixtures under `fixtures/behavior/snapshots_*` (one fixture often retires several tests over the same program); a test with no runnable program (a fictional or unavailable Rust crate, a synthetic manifest or WASM desugarer, a server-only program) is dies; a program that needs a `rust::` bake or a provider bake under the suite, or that `incan check` accepts and the generated Rust refuses (a bug), is open with its candidate fixture in `fixtures/behavior_candidates/snapshots/` (a directory nothing runs; its README says why each waits). |
| `loaves/compiler/incan_emit/tests/construction_diagnostics_tests.rs` | 2 | 52 | 52 | keep | - | - | - | #1561 | checker 2, parser 2 | typechecker diagnostics for model construction. |
| `loaves/compiler/incan_emit/tests/constructor_argument_order_tests.rs` | 4 | 161 | 161 | retire | 4/4 | 0 | - | #1561 | codegen 1, checker 3, parser 4, legacy_ir 3 | argument sequencing asserted through the legacy IR and generated Rust; the CLI regression for issue 1462 runs the same program. |
| `loaves/compiler/incan_emit/tests/emitter_freeze_tests.rs` | 2 | 103 | 103 | unaffected | - | - | - | #1561 | - | emitter freeze gate (#1687); runs the fingerprint checker and goes with the emitter tree. |
| `loaves/compiler/incan_emit/tests/empty_list_comparison_operand_tests.rs` | 5 | 143 | 143 | keep (keep 3, retire 2) | 1/2 | 0 | - | #1561 | codegen 1, text 1, checker 4, parser 5, legacy_ir 1 | checker facts for empty-list operands; the lowered/generated assertions retire. |
| `loaves/compiler/incan_emit/tests/enumerate_value_codegen_tests.rs` | 1 | 71 | 71 | retire | 0/1 | 0 | - | #1561 | codegen 1, parser 1 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/tests/fallible_entrypoint_codegen_tests.rs` | 1 | 50 | 50 | retire | 0/1 | 0 | - | #1561 | codegen 1, snapshot 1, parser 1 | snapshot of the generated entrypoint. |
| `loaves/compiler/incan_emit/tests/generated_stdlib_version_tests.rs` | 2 | 24 | 24 | retire | 0/2 | 0 | - | #1561 | codegen 1 | the stdlib line the emitter declares in generated code; the version contract moves with the emitter. |
| `loaves/compiler/incan_emit/tests/implicit_borrowing_codegen_tests.rs` | 21 | 719 | 719 | retire (retire 21) | 2/21 | 3 | - | #1561 | codegen 21, text 1, checker 7, parser 21, legacy_ir 7 | implicit borrowing (#1455) asserted on generated Rust over `rust::toml_edit`/`rust::demo` programs. The uncallable `&T` reassignment programs and the unknown-contract program die; the `toml_edit` and `demo` programs are open on the clone-counting candidate project; two pure-Incan shapes are twinned by fixtures. |
| `loaves/compiler/incan_emit/tests/lowering_error_propagation.rs` | 1 | 33 | 33 | retire | 0/1 | 0 | - | #1561 | legacy_ir 1 | legacy IR lowering error propagation. |
| `loaves/compiler/incan_emit/tests/match_arm_ownership_codegen_tests.rs` | 4 | 151 | 151 | retire | 0/4 | 0 | - | #1561 | codegen 4, parser 4 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/tests/nested_list_loop_tests.rs` | 5 | 156 | 156 | keep (keep 3, retire 2) | 1/2 | 0 | - | #1561 | codegen 1, checker 4, parser 5, legacy_ir 1 | checker facts for nested empty lists (issue 1471); one generated-text and one legacy IR assertion retire. |
| `loaves/compiler/incan_emit/tests/qualified_type_annotation_codegen_tests.rs` | 3 | 167 | 167 | retire | 0/3 | 0 | - | #1561 | codegen 3, parser 3 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/tests/return_operand_ownership_tests.rs` | 5 | 205 | 205 | retire | 5/5 | 0 | - | #1561 | checker 5, parser 5, legacy_ir 5 | return-operand move/read decisions asserted through the legacy IR; the CLI regression for issue 1489 runs the loop-variable case, the other cases still need Body IR ownership twins. |
| `loaves/compiler/incan_emit/tests/stdlib_generated_rust_snapshot_tests.rs` | 14 | 434 | 434 | retire | 0/14 | 0 | - | #1561 | codegen 14, snapshot 13, parser 14 | insta snapshots of generated stdlib Rust. |
| `loaves/compiler/incan_emit/tests/string_helper_codegen_tests.rs` | 1 | 45 | 45 | retire | 0/1 | 0 | - | #1561 | codegen 1, snapshot 1, text 1, parser 1 | snapshot and generated text. |
| `loaves/compiler/incan_emit/tests/string_len_codegen_tests.rs` | 2 | 43 | 43 | retire | 0/2 | 0 | - | #1561 | codegen 2, parser 2 | asserts generated Rust text. |
| `loaves/compiler/incan_emit/tests/zip_alias_codegen_tests.rs` | 10 | 345 | 345 | retire | 0/10 | 0 | - | #1561 | codegen 7, parser 7, legacy_ir 3 | generated clone placement for zip aliases; three assignment-plan unit tests are emitter ownership plans and retire with them. |

Per-test overrides in `loaves/compiler/incan_emit/src/checked_program/sdk_module_derives.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `sdk_module_derives_metadata_round_trip_preserves_exact_membership` | keep | - | - | checker, parser | metadata round trip through the checker, no codegen |
| `sdk_module_derives_missing_membership_stays_rejected` | keep | - | - | checker, parser | checker rejection, no codegen |

Per-test overrides in `loaves/compiler/incan_emit/src/checked_program/tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `rust_trait_associated_call_uses_callable_shape_from_dependency_namespace_source` | retire | - | - | codegen, checker, parser | drives IrCodegen directly |

Per-test overrides in `loaves/compiler/incan_emit/src/codegen.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `string_membership_probe_borrows_loop_binding_used_later_in_branch_issue1057` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/smoke/membership_loop_binding.incn` | - | codegen, generated_text, checker, legacy_ir | twinned by the #1057 program run to completion: the loop binding is used after the membership probe, which is the observable half. Of the retired test's three assertions, the one a run cannot see, `!code.contains("name.clone()")` (the probe borrows rather than clones), is not user-observable and dies with #654; the twin stays. |
| `test_simple_function` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/smoke/simple_function_add.incn` | - | codegen, checker, legacy_ir | twinned by a program that calls the function and prints the sum. |
| `test_model_generation` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/smoke/model_fields.incn` | - | codegen, checker, legacy_ir | twinned by a program that builds the model and prints its fields. |
| `test_fstring_generation` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/smoke/fstring_greeting.incn` | - | codegen, checker, legacy_ir | twinned by a program that prints the interpolated greeting. |
| `test_struct_instantiation` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/smoke/model_fields.incn` | - | codegen, checker, legacy_ir | twinned by a program that builds the model with named arguments and prints each field. |
| `test_enum_generation` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/smoke/enum_variants.incn` | - | codegen, checker, legacy_ir | twinned by a program that matches each variant and prints its name. |

Per-test overrides in `loaves/compiler/incan_emit/src/conversions.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `string_addition_recognizes_canonical_rust_string_metadata_issue896` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_concatenation.incn` (the #896 library with a main: a str extended with a Rust `String` result and two results joined). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_call_skips_clone_for_mutable_list_param_issue244` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/mutable_list_parameter_changes_reach_the_caller.incn` | - | legacy_ir | twinned by a program that appends through a mut list parameter directly and through a forwarding helper, then searches and measures the caller's list. |
| `test_incan_call_moves_owned_mutable_rust_handle` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_handle_moved_into_a_mut_parameter.incn` (a Rust `Stdout` handed to an Incan `mut` parameter). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_call_keeps_string_conversion_for_mutable_string_param_issue244` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/mutable_string_parameter_from_literal_and_variable.incn` | - | legacy_ir | twinned by a program that passes a literal and a variable to a mut str parameter and prints the variable afterwards. |
| `test_incan_function_string_literal_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/string_literal_and_variable_to_str_parameter.incn` | - | legacy_ir | twinned by a program that passes a literal, a variable it keeps using and a variable at its last use to a str parameter and prints what each call received. |
| `test_incan_function_string_var_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/string_literal_and_variable_to_str_parameter.incn` | - | legacy_ir | twinned by a program that passes a literal, a variable it keeps using and a variable at its last use to a str parameter and prints what each call received. |
| `test_incan_function_string_var_read_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/string_literal_and_variable_to_str_parameter.incn` | - | legacy_ir | twinned by a program that passes a literal, a variable it keeps using and a variable at its last use to a str parameter and prints what each call received. |
| `test_incan_function_static_str_var_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/const_and_static_strings_to_parameters.incn` | - | legacy_ir | twinned by a program that passes str and int consts and statics to a str parameter and to a generic parameter and prints each value. |
| `test_incan_function_frozen_str_var_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. |
| `test_incan_function_frozen_str_literal_without_target_stays_frozen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. The literal without a target is `keep("policy")`, whose parameter is FrozenStr. |
| `test_incan_function_frozen_str_static_read_without_target_stays_frozen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. |
| `test_incan_function_static_str_var_to_generic` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/const_and_static_strings_to_parameters.incn` | - | legacy_ir | twinned by a program that passes str and int consts and statics to a str parameter and to a generic parameter and prints each value. |
| `test_assignment_frozen_str_var_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. |
| `test_incan_function_rust_path_value_to_string_param` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_error_value_to_a_str_parameter.incn` (a Rust `io::Error` handed to a str parameter prints its message). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_function_unknown_rust_payload_to_string_param` | retire | `dies` | an `Unknown`-typed value is the state of a Rust value whose metadata is absent; checked source cannot spell it. The resolved Rust-value case is the candidate `rust_error_value_to_a_str_parameter.incn`. | legacy_ir | - |
| `test_incan_function_local_struct_to_string_param_does_not_stringify_implicitly` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/model_value_refused_at_a_str_parameter.incn` | - | legacy_ir | twinned by a refused program: a model value at a str parameter is a type error (INCAN-T0001), so no implicit text conversion ever runs. |
| `test_incan_function_static_read_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/const_and_static_strings_to_parameters.incn` | - | legacy_ir | twinned by a program that passes str and int consts and statics to a str parameter and to a generic parameter and prints each value. |
| `test_incan_function_static_read_int_to_generic_stays_as_is` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/const_and_static_strings_to_parameters.incn` | - | legacy_ir | twinned by a program that passes str and int consts and statics to a str parameter and to a generic parameter and prints each value. |
| `test_incan_function_string_list_var_uses_owned_incan_semantics` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/string_list_passed_at_last_use_and_empty_literal.incn` | - | legacy_ir | twinned by a program that passes a list of strings at its last use and an empty list literal to a list parameter that measures them. |
| `test_incan_function_empty_list_to_string_list_skips_collect_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/string_list_passed_at_last_use_and_empty_literal.incn` | - | legacy_ir | twinned by a program that passes a list of strings at its last use and an empty list literal to a list parameter that measures them. |
| `test_incan_function_int_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/primitives_and_literals_as_arguments.incn` | - | legacy_ir | twinned by a program that passes an int, a float and a bool to functions, uses them again, and passes str literals to str methods. |
| `test_incan_function_float_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/primitives_and_literals_as_arguments.incn` | - | legacy_ir | twinned by a program that passes an int, a float and a bool to functions, uses them again, and passes str literals to str methods. |
| `test_incan_function_bool_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/primitives_and_literals_as_arguments.incn` | - | legacy_ir | twinned by a program that passes an int, a float and a bool to functions, uses them again, and passes str literals to str methods. |
| `test_incan_function_noncopy_var_last_use_moves` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/model_value_passed_then_used_again.incn` | - | legacy_ir | twinned by a program that passes a model value, uses it again, passes it twice inside one returned call and passes another at its last use, printing each result. |
| `test_incan_function_noncopy_var_non_last_use_clones` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/model_value_passed_then_used_again.incn` | - | legacy_ir | twinned by a program that passes a model value, uses it again, passes it twice inside one returned call and passes another at its last use, printing each result. |
| `test_assignment_static_str_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. A type-annotated local from a `static` str (`b: str = BASE`) fails to compile the same way as the int case (StaticBinding vs String, #1777); the fixture assigns it without the annotation. |
| `test_assignment_static_read_int_stays_as_is` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. The exact case, a type-annotated local from a `static` (`count: int = COUNT`), passes `incan check` and fails to compile (`expected i64, found StaticBinding<i64>`, #1777); the fixture assigns the static without the annotation. |
| `test_assignment_does_not_assume_opaque_rust_value_is_clone` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_value_assigned_to_a_new_name.incn` (a Rust `Stdin`, which cannot be copied, assigned to a second name and used through it). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_return_static_str_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. |
| `test_return_static_read_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. |
| `test_external_function_string_literal` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_parameters_by_value_and_in_a_field/` (`shout("literal")`: a str literal to a Rust function taking a `String`). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_string_var_borrow` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::build_inline_fstring_rust_interop_variants_issue716` | - | legacy_ir | twinned by the re-point program whose `fail_local` hands a str local to `raise_value_error(&str)` and builds; the local is lent. The metadata-free branch the test drives is where the policy lands without a Rust signature; the lend is what the program shows. |
| `test_external_function_string_method_call_result_unknown_target_stays_owned_issue1066` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_parameters_by_value_and_in_a_field/` (`shout(name.upper())`: a str method result handed to a Rust function taking a `String` passes by value, the variable it was made from still readable). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_string_expression_to_str_ref_borrows_issue716` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::build_inline_fstring_rust_interop_variants_issue716` | - | legacy_ir | twinned by the re-point program that hands inline and local f-strings to a Rust function taking `&str` and to a Rust enum variant holding a `String` (#716); the target-less half of the retired test is the metadata-free branch, which a runnable program does not take. |
| `test_external_function_as_slice_arg_does_not_double_borrow` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the re-point program whose `trait_by_value_decode_case` hands `encoded.as_slice()` to a Rust `decode` implemented for `&[u8]` and prints the outcome. |
| `test_external_function_as_ref_arg_does_not_double_borrow` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_trait_object_view_to_a_reference_parameter/` (an `Arc<dyn Array>` from an in-fixture crate viewed through `as_ref()` and handed to a Rust function over `&dyn Array`, twice; a second `&` would refuse the program at the Rust build). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_string_var_with_by_value_target_does_not_borrow` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_parameters_by_value_and_in_a_field/` (a str variable to a Rust function taking `String` by value, still usable afterwards). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_field_with_by_value_target_does_not_borrow` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_duration_fields_to_rust_parameters.incn` (a Rust `Duration` field handed to `saturating_add`, still readable afterwards). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_known_field_without_target_does_not_guess_borrow` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_duration_fields_to_rust_parameters.incn` (a Rust `Duration` field handed to `saturating_add` by value, still readable afterwards; the same program as the two typed-target rows). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_string_field_without_target_borrows_like_string_variable` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_parameters_by_value_and_in_a_field/` (`describe(label.0)`: a newtype's str field lent to a Rust function taking `&str`, the field readable afterwards). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_self_field_without_target_borrows_noncopy_receiver_field` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/channel_ends_unpacked_and_sent_through_a_wrapper.incn` | - | legacy_ir | twinned by a program that unpacks a channel into its two ends, sends two values through the sender wrapper and receives them in order. The stdlib `Sender` wrapper sends through its inner Rust value from methods taking self, twice, and is asked whether it is closed afterwards: the field is lent, never moved out of the receiver, which is what the test protects. |
| `test_external_function_unknown_field_without_target_keeps_adapter_borrow_fallback` | retire | `dies` | an `Unknown` field type is the state of a wrapper whose inner Rust type has no metadata; checked source cannot spell it. The resolved wrapper case (`Sender` in `std.async`) is twinned by `ownership_parameters_and_receivers/channel_ends_unpacked_and_sent_through_a_wrapper.incn`. | legacy_ir | - |
| `test_external_function_field_with_ref_target_borrows` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_duration_fields_to_rust_parameters.incn` (a Rust `Duration` field handed to `eq`). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_external_function_int_no_conversion` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_trait_object_method_arguments_borrow_by_metadata_issue832` | - | legacy_ir | twinned by the re-point program that hands a `usize` local to a Rust constructor (`InterleavedOwned.new(input_frames)`) and runs; a scalar passes as it is. The metadata-free branch the test drives is where the policy lands without a Rust signature; the value's passing is what the program shows. |
| `test_struct_field_string_literal` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/model_fields_from_literals_and_variables.incn` | - | legacy_ir | twinned by a program that builds a model from a str literal, an int literal and a str variable, prints the fields and uses the variable afterwards. |
| `test_field_read_through_a_transparent_wrapper_always_clones` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/web_extractor_field_read_at_three_sinks.incn` | - | legacy_ir | twinned by a program that reads a str field through Query and Json extractor wrappers into a model field, a list and a local, and hands a generic holder's field over at its last use, printing each. The wrapper's last use is the call site in each helper, as in the retired test. |
| `test_owned_storage_field_read_moves_only_out_of_a_last_use_local` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/field_read_out_of_a_local.incn` | - | legacy_ir | twinned by a program that reads a list field out of a local it keeps using, out of self across two calls, and out of a local at its last use, printing the locals and the results. The third arm, a local declared with `&Model`, is reachable only with a Rust-typed value (`&Model` is refused for Incan types); the self read stands in for a holder that must stay intact. |
| `test_struct_field_static_str_const_to_rust_string_targets` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::build_static_str_const_rust_string_struct_field` | - | legacy_ir | twinned by the re-point program that fills a Rust struct's `String` field from a `const OPTION_NAME: str` and builds. |
| `test_struct_field_string_literal_to_rust_string_target` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_parameters_by_value_and_in_a_field/` (a str literal into a Rust struct's `String` field, beside the const the re-point `build_static_str_const_rust_string_struct_field` already builds). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_unknown_struct_field_borrowed_string_const_materializes_owned_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. |
| `test_unknown_collection_element_preserves_frozen_string_without_target` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. |
| `test_struct_field_int_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/model_fields_from_literals_and_variables.incn` | - | legacy_ir | twinned by a program that builds a model from a str literal, an int literal and a str variable, prints the fields and uses the variable afterwards. |
| `test_struct_field_string_var_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/model_fields_from_literals_and_variables.incn` | - | legacy_ir | twinned by a program that builds a model from a str literal, an int literal and a str variable, prints the fields and uses the variable afterwards. |
| `reference_assignment_targets_take_precedence_over_owned_field_materialization` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Holder.view`: a field assigned to a local declared `&Item`, zero copies counted); a local declared with `&` is refused for Incan-native types, so only the Rust-typed form exists; the `&mut` and plain-typed target arms follow the same program. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_assignment_string_literal_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. |
| `test_assignment_int_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. |
| `exact_float_destinations_require_finite_values_without_rejecting_f32_widening` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/non_finite_exact_float_is_refused_at_runtime.incn` | - | legacy_ir | twinned by a program that widens a finite f32 into an f64, then assigns an overflowed float to an f64 and ends with the runtime refusal (exit 101) after printing what came before. The finite f32 widening is accepted before the overflowed float is refused. |
| `exact_float_arithmetic_plans_keep_width_and_validate_each_result` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/exact_float_arithmetic_keeps_its_width.incn` | - | legacy_ir | twinned by a program that prints f32 + f32 (0.3), f64 + f64, f32 mixed with float, f64 and int on either side, and a float at an f64 destination. |
| `mixed_f32_arithmetic_plans_widen_the_concrete_f32_operand` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/exact_float_arithmetic_keeps_its_width.incn` | - | legacy_ir | twinned by a program that prints f32 + f32 (0.3), f64 + f64, f32 mixed with float, f64 and int on either side, and a float at an f64 destination. |
| `test_return_string_literal_to_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. |
| `test_return_int_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/strings_and_ints_returned_and_assigned.incn` | - | legacy_ir | twinned by a program that returns a literal, a str const, a str static and an int from functions and assigns a literal, a const and a static to locals, printing each. |
| `test_return_call_context_noncopy_var_read_clones` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/model_value_passed_then_used_again.incn` | - | legacy_ir | twinned by a program that passes a model value, uses it again, passes it twice inside one returned call and passes another at its last use, printing each result. |
| `test_return_call_context_noncopy_var_move_stays_move` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/model_value_passed_then_used_again.incn` | - | legacy_ir | twinned by a program that passes a model value, uses it again, passes it twice inside one returned call and passes another at its last use, printing each result. |
| `test_return_call_context_frozen_str_target_preserves_frozen_literal` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/frozen_strings_at_parameters_fields_and_collections.incn` | - | legacy_ir | twinned by a program that prints a FrozenStr const, passes it to a str parameter, keeps it frozen through a FrozenStr parameter, extends a str local started from it, stores it in a model field and collects it into a list. |
| `test_incan_call_borrows_noncopy_var_for_ref_target` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`is_table_ref(document())`: a Rust value handed to a function whose parameter is declared `&Item`, zero copies counted); a `&Model` parameter is refused for Incan-native arguments, so only the Rust-typed form exists. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_return_call_context_borrows_noncopy_var_for_ref_target` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`check_ref`: the same call inside a return). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_call_clones_borrowed_as_ref_result_for_owned_nominal_target` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_box_as_ref_argument.incn` (a `Box.as_ref()` view handed to a function taking the value, twice, box still usable). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_call_clones_borrowed_as_ref_result_when_target_is_unknown` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_box_as_ref_argument.incn`; the target-less variant is the same call before the callee's signature is known. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_call_clones_interop_unwrapped_as_ref_result_when_target_is_unknown` | retire | `dies` | the `RustTypeUnwrap` coercion is an IR wrapper lowering inserts around a `rusttype`-declared value; the clone of an `as_ref()` result underneath it is the candidate `rust_box_as_ref_argument.incn`, and no source-level observable separates the wrapped from the plain result. | legacy_ir | - |
| `test_incan_call_clones_erased_receiver_as_ref_result_when_target_is_unknown` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_box_as_ref_argument.incn`; an erased receiver is the same box before its type is known. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_incan_call_clones_erased_receiver_as_ref_result_for_owned_nominal_target` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_box_as_ref_argument.incn`. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `test_assignment_clones_interop_unwrapped_string_field` | retire | - | - | legacy_ir | needs a `rusttype` wrapper over a crate struct with a `String` field, read into a local while the wrapper stays intact; no candidate: `rusttype` programs over crates belong with the `rust::` specification (#1337), and no runtime `rusttype` test exists in the tree to re-point at. |
| `test_assignment_moves_owned_tuple_unpack_field_without_clone` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/channel_ends_unpacked_and_sent_through_a_wrapper.incn` | - | legacy_ir | twinned by a program that unpacks a channel into its two ends, sends two values through the sender wrapper and receives them in order. `tx, rx = channel(2)` unpacks two ends that cannot be copied. |
| `test_method_arg_no_conversion` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/primitives_and_literals_as_arguments.incn` | - | legacy_ir | twinned by a program that passes an int, a float and a bool to functions, uses them again, and passes str literals to str methods. The method arguments are the str literals given to `startswith` and `endswith`. |
| `test_apply_none` | retire | `dies` | renders a conversion as Rust tokens (`value`); a data-structure invariant of the dying emitter. | - | - |
| `test_apply_to_string` | retire | `dies` | renders a conversion as Rust tokens (`.to_string()`); a data-structure invariant of the dying emitter. | - | - |
| `test_apply_borrow` | retire | `dies` | renders a conversion as Rust tokens (`&value`); a data-structure invariant of the dying emitter. | - | - |
| `test_apply_mut_borrow` | retire | `dies` | renders a conversion as Rust tokens (`&mut value`); a data-structure invariant of the dying emitter. | - | - |
| `test_apply_clone` | retire | `dies` | renders a conversion as Rust tokens (`.clone()`); a data-structure invariant of the dying emitter. | - | - |

Per-test overrides in `loaves/compiler/incan_emit/src/emit/expressions/methods.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `copy_payload_named_observer_is_borrowed_through_a_closure_issue1718` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/result_inspect_named_observer_copy_payload.incn` | - | codegen, legacy_ir | asserts the closure the emitter passes for a named observer over a Copy payload (#1718); the twin runs inspect and inspect_err over int payloads with named functions and reads what the observers print. |
| `non_copy_payload_named_observer_keeps_the_adapter_route` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/result_inspect_named_observer_copy_payload.incn` | - | codegen, legacy_ir | asserts the adapter or bare item the emitter passes for a named observer over a non-Copy payload; the twin runs inspect over a model payload with a named function and reads what the observer prints. |

Per-test overrides in `loaves/compiler/incan_emit/src/ownership.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `dict_lookup_key_plan_sees_through_borrowed_receivers` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/dict_lookups_through_a_mutable_parameter.incn` | - | legacy_ir | twinned by a program that probes and updates str- and int-keyed dicts through mut parameters, reuses a str probe, and builds a dict comprehension keyed by strings. Both a str-keyed and an int-keyed dict are probed and updated through the mut parameter (#1668). |
| `result_payload_read_of_a_loop_binding_materializes_the_owned_value` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/loop_binding_stored_into_a_result.incn` | - | legacy_ir | twinned by a program that returns a loop's row inside Ok while the caller keeps the whole list, and returns a local inside Ok at its last use. The rows are `list[list[str]]` and the caller measures them after the call (#1489). |
| `incan_call_string_literal_plans_owned_string` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_string_boundaries/string_literal_and_variable_to_str_parameter.incn` | - | legacy_ir | twinned by a program that passes a literal, a variable it keeps using and a variable at its last use to a str parameter and prints what each call received. |
| `mutable_list_param_requires_rust_mut_borrow` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/mutable_list_parameter_changes_reach_the_caller.incn` | - | legacy_ir | twinned by a program that appends through a mut list parameter directly and through a forwarding helper, then searches and measures the caller's list. |
| `argument_plan_mutable_list_param_reborrows_without_value_clone` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/mutable_list_parameter_changes_reach_the_caller.incn` | - | legacy_ir | twinned by a program that appends through a mut list parameter directly and through a forwarding helper, then searches and measures the caller's list. |
| `mutable_source_param_forwards_borrowed_rust_callback_value` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_callback_parameter_forwarded_to_a_mut_parameter/` (an in-fixture crate lends a mutable `Ui` to a closure twice; the closure forwards it to `def paint(mut ui: Ui)` and the host prints the count both calls changed); the `egui::Ui` of the retired test is the crate's `Ui` here. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_mutable_string_literal_materializes_then_reborrows` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/mutable_string_parameter_from_literal_and_variable.incn` | - | legacy_ir | twinned by a program that passes a literal and a variable to a mut str parameter and prints the variable afterwards. |
| `argument_plan_external_ref_param_borrows_once` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_concrete_reference_arguments_borrow_by_metadata_issue861` | - | legacy_ir | twinned by the re-point program that hands a Rust `Header` to a Rust method that only reads it (`view_value`) and prints the result; the `&thing` spelling dies with #654. |
| `argument_plan_checked_c_borrow_skips_owned_value_materialization` | retire | `loaves/toolchain/incan-cli/tests/rfc031_pub_import_integration_tests.rs::consumer_check_hides_owned_c_resources_behind_an_incan_facade` | - | legacy_ir | twinned by the re-point checked-C program that flushes and reads the descriptor of a C file resource through `c.BorrowedMut`/`c.Borrowed` symbols and runs to completion. |
| `argument_plan_external_inferred_generic_string_literal_stays_direct` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_method_into_bound_keeps_string_argument_inferable_issue804` | - | legacy_ir | twinned by the re-point program that hands a str literal to a Rust method generic over `Into<String>` and prints the encoded text. |
| `argument_plan_external_inferred_generic_read_string_clones` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_into_bound_string_variables/` (the #804 crate; a str variable encoded twice and printed afterwards). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_external_inferred_generic_moved_string_stays_direct` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_into_bound_string_variables/` (the same variable at its last use). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_external_list_element_into_is_value_plan` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_list_elements_converted_into_a_vec/` (a `list[str]` to a Rust `Vec<Name>` with `Name: From<String>`). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_external_list_element_into_skips_unresolved_source_elements` | retire | `dies` | a `list[Unknown]` element type is the state of a list whose element metadata is absent; checked source cannot spell it. The resolved list and the empty literal are the candidate `rust_list_elements_converted_into_a_vec/`. | legacy_ir | - |
| `argument_plan_external_list_element_into_skips_empty_list_literals` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_list_elements_converted_into_a_vec/` (`total_length([])`). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_external_buf_param_lends_incan_bytes_as_slice` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_buf_parameter_from_bytes/` (an Incan bytes value to a Rust `impl Buf` parameter, still usable afterwards). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_external_buf_param_lends_rust_vec_u8_as_slice` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the re-point program whose `cross_crate_decode_case` hands the `Vec<u8>` of `encode_to_vec()` to a `decode(impl prost::Buf)`. |
| `argument_plan_external_buf_param_lends_generic_rust_vec_u8_as_slice` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the same `cross_crate_decode_case`: the value is the same `Vec<u8>` whichever way the checker spells its type. |
| `rust_display_byte_buffer_detection_uses_shared_type_shape_parser` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the re-point program in which a `Vec<u8>` reaches a `Buf` parameter as a slice (`cross_crate_decode_case`) while a `Cursor<Vec<u8>>` is handed over by value (`by_value_decode_case`); the spelling table itself dies with #654. |
| `rust_display_buf_detection_uses_shared_type_shape_parser` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the re-point program whose `decode(impl prost::Buf)` and `decode(impl DecodeBuf)` parameters take the argument as a slice and by value; the spelling table itself dies with #654. |
| `rust_string_buffer_detection_is_owned_string_only` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/rust_string_concatenation.incn`, whose concatenations select the string-aware lowering by the Rust `String` spelling; the spelling table itself dies with #654. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `argument_plan_external_buf_param_keeps_explicit_as_slice_shape` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the re-point program whose `trait_by_value_decode_case` hands `encoded.as_slice()` to `decode`. |
| `argument_plan_external_buf_param_keeps_non_byte_buf_implementors_by_value` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_generic_interop_scenarios_share_one_project` | - | legacy_ir | twinned by the re-point program whose `by_value_decode_case` hands a `Cursor<Vec<u8>>` to `decode` by value. |
| `argument_plan_clone_bound_query_follows_shared_incan_arg_policy` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/generic_values_reused_across_calls.incn` | - | legacy_ir | twinned by a program that passes a generic value to a method twice from a generic function and passes a wrapper's generic field to a generic function while still reading the wrapper. `read_cursor` passes `other.cursor` to a generic function and reads it again. |
| `list_shared_receiver_borrows_plain_list` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/mutable_list_parameter_changes_reach_the_caller.incn` | - | legacy_ir | twinned by a program that appends through a mut list parameter directly and through a forwarding helper, then searches and measures the caller's list. |
| `plan_read_by_ref_receiver_borrows_owned_value` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::rust_std_io_trait_interop_borrows_receivers_and_propagates_results_issues878_888` | - | legacy_ir | twinned by the re-point program that calls `Read.by_ref(input)` on a `Stdin` value and builds. |
| `plan_read_by_ref_receiver_reborrows_refmut_guard` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/bytes_io_read_through_a_guard.incn` | - | legacy_ir | twinned by a program that reads bounded byte counts from a BytesIO buffer in sequence. `BytesIO.read` in `std.io` performs the bounded read through `Read.by_ref` on the `RefMut` guard of its cursor, which is the guarded receiver of the retired test. |
| `plan_read_by_ref_receiver_preserves_metadata_free_guard_fallback` | retire | `dies` | an `Unknown` receiver type is the state of a guard whose metadata is absent; checked source cannot spell it. The guard read is twinned by `ownership_parameters_and_receivers/bytes_io_read_through_a_guard.incn`. | legacy_ir | - |
| `dict_string_lookup_uses_as_ref_for_owned_probe` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/dict_lookups_through_a_mutable_parameter.incn` | - | legacy_ir | twinned by a program that probes and updates str- and int-keyed dicts through mut parameters, reuses a str probe, and builds a dict comprehension keyed by strings. |
| `for_loop_on_borrowable_string_list_uses_iter` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/for_loops_over_strings_bytes_and_enum_lists.incn` | - | legacy_ir | twinned by a program that loops over a list of strings, a str, a bytes value and a list of enum values and measures the lists afterwards. |
| `for_loop_on_string_iterates_owned_character_strings` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/for_loops_over_strings_bytes_and_enum_lists.incn` | - | legacy_ir | twinned by a program that loops over a list of strings, a str, a bytes value and a list of enum values and measures the lists afterwards. |
| `for_loop_on_bytes_iterates_incan_ints` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/for_loops_over_strings_bytes_and_enum_lists.incn` | - | legacy_ir | twinned by a program that loops over a list of strings, a str, a bytes value and a list of enum values and measures the lists afterwards. |
| `for_loop_on_user_enum_list_clones_items_when_not_mutating` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/for_loops_over_strings_bytes_and_enum_lists.incn` | - | legacy_ir | twinned by a program that loops over a list of strings, a str, a bytes value and a list of enum values and measures the lists afterwards. The loop body only reads each enum item and the list is measured afterwards. |
| `filtered_list_comprehension_uses_filter_map_clone_plan` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/comprehensions_and_list_constructor_sources.incn` | - | legacy_ir | twinned by a program with filtered and plain comprehensions over lists, a comprehension over a mut list parameter, and list() over a list, a mut parameter, a str, a frozen list, a frozen set and a dict. |
| `copy_list_comprehension_uses_copy_iteration_plans` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/comprehensions_and_list_constructor_sources.incn` | - | legacy_ir | twinned by a program with filtered and plain comprehensions over lists, a comprehension over a mut list parameter, and list() over a list, a mut parameter, a str, a frozen list, a frozen set and a dict. |
| `mutable_ref_comprehension_item_does_not_use_copied_plan` | retire | `dies` | a `RefMut(int)` comprehension item type is a list whose elements are `&mut int`, which no source spells; the comprehension over a mut list parameter strips the reference from the iterable and iterates `int` items, the copied plan that `copy_list_comprehension_uses_copy_iteration_plans` twins. | legacy_ir | - |
| `dict_comprehension_marks_noncopy_keys_for_clone` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/dict_lookups_through_a_mutable_parameter.incn` | - | legacy_ir | twinned by a program that probes and updates str- and int-keyed dicts through mut parameters, reuses a str probe, and builds a dict comprehension keyed by strings. |
| `owned_iterator_source_moves_last_use_list_var` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/generator_sources_consumed_and_reused.incn` | - | legacy_ir | twinned by a program with generator expressions over a list read again afterwards, a list at its last use and a one-shot literal, plus a comprehension over a generator variable, printing what each yields. |
| `owned_iterator_source_clones_reused_list_var` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/generator_sources_consumed_and_reused.incn` | - | legacy_ir | twinned by a program with generator expressions over a list read again afterwards, a list at its last use and a one-shot literal, plus a comprehension over a generator variable, printing what each yields. |
| `owned_iterator_source_moves_one_shot_list_expression` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/generator_sources_consumed_and_reused.incn` | - | legacy_ir | twinned by a program with generator expressions over a list read again afterwards, a list at its last use and a one-shot literal, plus a comprehension over a generator variable, printing what each yields. |
| `list_constructor_source_plans_follow_loop_iteration` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/comprehensions_and_list_constructor_sources.incn` | - | legacy_ir | twinned by a program with filtered and plain comprehensions over lists, a comprehension over a mut list parameter, and list() over a list, a mut parameter, a str, a frozen list, a frozen set and a dict. The `std::env::Args` source is run by the re-point `comprehension_over_rust_iterator_consumes_it_by_value_issue1490` program's `for argument in args()`; `list(bytes)`/`list(range)`/`list(generator)` follow `test_issue1464_list_constructor_codegen`'s twin. |
| `generator_sources_are_consumed_through_the_iterator_trait` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/list_and_comprehension_over_generator.incn` | - | legacy_ir | asserts the plans list(generator) and a comprehension over a generator take so the chain reaches the Iterator trait's adapters (#1464); the twin materializes generators through list(), comprehensions and the generator's own adapters and prints the results. |
| `opaque_comprehension_sources_are_consumed_by_value` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/generator_sources_consumed_and_reused.incn` | - | legacy_ir | twinned by a program with generator expressions over a list read again afterwards, a list at its last use and a one-shot literal, plus a comprehension over a generator variable at its last use, printing what each yields. The Rust-iterator arms (a call returning `std::env::Args`, and a reused parameter holding one) are run by the re-point `cli_rust_interop_tests.rs::comprehension_over_rust_iterator_consumes_it_by_value_issue1490`. The clone arm (a generator variable read again after the comprehension) has no fixture: the generator runtime type (`incan_std_core::iter::Generator`) has no `Clone`, so the clone that arm plans would fail to compile. |
| `external_call_string_var_borrows` | retire | `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs::build_inline_fstring_rust_interop_variants_issue716` | - | legacy_ir | twinned by the re-point program whose `fail_local` hands a str local to `raise_value_error(&str)` and builds; the local is lent. The `&s` spelling dies with #654. |
| `incan_call_field_backed_noncopy_arg_clones_for_by_value_methods_issue241` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/self_and_wrapped_fields_returned_as_values.incn` | - | legacy_ir | twinned by a program whose method returns self and whose wrapper field is passed to a function, with the receiver and the wrapper printed afterwards. The wrapper's field is passed to a by-value function and read again afterwards (#241). |
| `return_value_noncopy_self_read_clones_to_materialize_owned_result` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_parameters_and_receivers/self_and_wrapped_fields_returned_as_values.incn` | - | legacy_ir | twinned by a program whose method returns self and whose wrapper field is passed to a function, with the receiver and the wrapper printed afterwards. `snapshot` returns self and the receiver is read afterwards. |

Per-test overrides in `loaves/compiler/incan_emit/src/trait_bound_inference.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `implementation_bound_matching_is_canonical_and_instantiation_exact` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/bounds_from_a_generic_model_implementation.incn` passes `incan check` and fails to compile (a declared model bound `R with Clone` and the impl-inferred Display bound are not carried to the generic caller `consume[T](stream: Stream[T])`, E0277/E0599). Waits for #1280. The negative half (a different module or instantiation inherits nothing) has no observable. |
| `clone_dependencies_exclude_callable_signatures_and_concrete_values` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/generic_functions_over_a_non_clone_rust_value.incn` (`call_both` calls its callable twice and `label_twice` copies a str beside `T`, both instantiated with a Rust `Stdin`, which has no `Clone`; an over-inferred `T: Clone` would refuse the program at the Rust build). The same shapes run with copyable arguments in `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/generic_functions_reuse_callables_models_and_payloads.incn`, which proves nothing about an absent bound. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `repeated_clone_dependency_does_not_add_unrelated_parameters` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/generic_functions_over_a_non_clone_rust_value.incn` (`repeat_first[T, U]` copies its `T` value twice, so `T: Clone` is already inferred, and is instantiated with a `Stdin` at `U`; a bound leaking onto `U` would refuse the program at the Rust build). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `erased_clone_dependencies_retain_conservative_bounds` | retire | `dies` | an `Unknown`-typed clone dependency is the state of a value whose metadata is absent; checked source cannot spell it, and the conservative bound it adds is satisfied by every Incan type. | legacy_ir | - |
| `concrete_nominal_clone_does_not_bind_enclosing_generic_parameters` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/generic_functions_over_a_non_clone_rust_value.incn` (`copy_algorithm[T, U]` copies a concrete model beside two type parameters both instantiated with a `Stdin`; a bound leaking onto either would refuse the program at the Rust build). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `nominally_encoded_generic_clone_binds_only_the_matching_parameter` | retire | `dies` | a legacy IR encoding of a type parameter as a nominal (`Struct("T")`); no source spelling distinguishes it from the ordinary encoding, whose behavior `ownership_numerics_and_bounds/generic_functions_reuse_callables_models_and_payloads.incn` proves. | legacy_ir | - |
| `opaque_nominal_clone_in_impl_context_preserves_owner_bounds` | retire | `dies` | the conservative `T: Clone` this test asserts is observable only as the Rust build refusing an instantiation with a non-`Clone` Rust type, and a refusal at the Rust build is not a fixture observable (`expect-diagnostic` is a check-time refusal); the four rows that assert an absent bound are open on a program that builds with such a type, `generic_functions_over_a_non_clone_rust_value.incn`. | legacy_ir | - |
| `self_clone_binds_no_callable_own_parameters` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/generic_functions_over_a_non_clone_rust_value.incn` (`Codec.duplicate[T]` returns self, the receiver is read afterwards, and `T` is instantiated with a `Stdin`; a bound on the method's own `T` would refuse the program at the Rust build, #1489). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `result_payload_bounds_follow_the_struct_field_plan` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/generic_functions_reuse_callables_models_and_payloads.incn` | - | codegen, legacy_ir | twinned by a program of generic functions that call a callable twice, reuse a str beside a type parameter, copy a concrete model, return self from a generic method and return a field or a loop item inside Ok, all run with concrete arguments. `unwrap` returns a field of a last-use local inside Ok; `find` returns a loop item inside Ok (#1489). |
| `impl_owner_generic_bounds_are_written_to_impl_header` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/generic_model_method_formats_its_type_parameter.incn` | - | codegen, legacy_ir | twinned by a program whose generic model method interpolates the type parameter for int, str and float payloads. Where the bound is written (impl header or method) is a Rust spelling and dies with #654; the program proves the bound is inferred at all. |
| `generic_value_field_reflection_methods_infer_field_value_bound` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/reflection_over_a_generic_value.incn` | - | codegen, legacy_ir | twinned by a program whose generic function reads a named public field and the field items of the model it is given. `__field_value__` and `__field_items__` are called on a generic parameter. |
| `backend_clone_bounds_do_not_use_incan_policy_for_external_nominal_methods` | retire | `dies` | the absence of the bound is observable only by a generic function handing a non-`Clone` Rust value to a Rust-crate method that reads it, and such a method's own generic bound would have to reach the Incan caller first (the #1280 family), so no program can show it. The Incan-defined-method half is twinned by `ownership_numerics_and_bounds/generic_values_reused_across_calls.incn`; the absent-bound shapes over Incan callees are open on `generic_functions_over_a_non_clone_rust_value.incn`. | codegen, legacy_ir | - |
| `backend_clone_bounds_use_incan_policy_for_source_owned_methods` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/generic_values_reused_across_calls.incn` | - | codegen, legacy_ir | twinned by a program that passes a generic value to a method twice from a generic function and passes a wrapper's generic field to a generic function while still reading the wrapper. The method returns what it was given so both results are printed; a method whose body formats the value fails to compile (an inferred method bound is not carried to a generic caller, #1779). |
| `external_implementation_bounds_propagate_through_checked_trait_dispatch` | retire | - | - | legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/fallible_iteration_over_reader_chunks_from_a_bare_generic.incn` (`count_chunks[R](chunks: ReaderChunks[R])`, no bound of its own, iterates the stdlib's `FallibleIterator` adoption of `ReaderChunks[R with BinaryReader]`); the retired test asserts the implementation's bound reaches the bare generic caller, and a caller that writes the bound itself runs whether or not it does. Waits for #1280 (a generic caller inheriting the implementation's bound); if the checker is made to refuse the bare parameter instead, the candidate becomes an `expect-diagnostic` fixture. |
| `external_generic_bounds_do_not_rewrite_same_named_local_non_generic_function` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_numerics_and_bounds/local_function_named_like_a_generic_in_another_module` | - | legacy_ir | twinned by a module program whose own non-generic `timeout` runs beside a helper module declaring a generic `timeout`. The helper module's generic `timeout` is compiled with the program; the local one keeps its plain signature. |

Per-test overrides in `loaves/compiler/incan_emit/tests/checked_empty_collection_constructor_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `checked_empty_collection_constructors_lower_to_existing_aggregate_shapes_issue1247` | keep | - | - | replacement, checker, parser | checked result type or Body IR aggregate shape |
| `checked_empty_list_constructor_lowers_to_the_list_aggregate_issue1464` | keep | - | - | replacement, checker, parser | checked result type or Body IR aggregate shape |
| `mismatched_typed_empty_collection_constructor_contexts_remain_errors_issue1247` | keep | - | - | checker, parser | checked result type or Body IR aggregate shape |
| `shadowed_collection_constructors_do_not_gain_aggregate_lowering_issue1247` | keep | - | - | replacement, checker, parser | checked result type or Body IR aggregate shape |
| `missing_checked_collection_constructor_facts_do_not_guess_aggregate_lowering_issue1247` | keep | - | - | replacement, checker, parser | checked result type or Body IR aggregate shape |
| `checked_typed_empty_dict_reaches_existing_aggregate_emission_issue1247` | retire | `loaves/compiler/incan_emit/tests/checked_empty_collection_constructor_tests.rs::checked_empty_collection_constructors_lower_to_existing_aggregate_shapes_issue1247` | - | codegen, snapshot, checker, parser | snapshot of the emitted aggregate |

Per-test overrides in `loaves/compiler/incan_emit/tests/codegen_snapshot_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `test_pub_import_expressions_codegen` | retire | - | - | codegen, checker, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/pub_import_widgets/`, a project fixture whose `deps/widgets` package stands in for the test's synthetic manifest; the consumer imports the package's model, function and const by name and through the package module and prints `default` for each (expected `default`, `default`, `default`, `2` with the other two tests over the same program). waits for a Cargo-free bake under the suite: the in-fixture provider's bake goes through the compatibility publisher, which under the suite reaches Cargo; the candidate passes after a manual `incan oven bake --project deps/<name>`. The `widgets::make_widget` vs `widgets_core::make_widget` package-identity assertion dies with #654. |
| `test_pub_import_module_alias_codegen` | retire | - | - | codegen, checker, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/pub_import_widgets/` (shared with the other widgets imports): a package module alias calls the same function and prints `default`. waits for a Cargo-free bake under the suite: the in-fixture provider's bake goes through the compatibility publisher, which under the suite reaches Cargo; the candidate passes after a manual `incan oven bake --project deps/<name>`. The alias-projection spelling dies with #654. |
| `test_vocab_block_desugaring_codegen` | retire | `dies` | the program's `route` block is desugared through a WASM desugarer the test assembles in memory (a hand-written wat module plus a synthetic `routes` manifest); no package carries that artifact, so the program cannot run, and the assertion is the desugared Rust snapshot. The user-visible vocab behavior is proved by the CLI's vocab integration tests, which bake real vocab packages. | codegen, checker, parser | - |
| `test_vocab_helper_backed_desugaring_codegen` | retire | `dies` | the program's `where` block is desugared through a WASM desugarer the test assembles in memory beside a synthetic `query` manifest; no package carries that artifact, so the program cannot run, and the assertion is the desugared Rust snapshot. The user-visible vocab behavior is proved by the CLI's vocab integration tests. | codegen, checker, parser | - |
| `test_equivalent_helper_backed_keywords_codegen_identically` | retire | `dies` | asserts that two keywords registered by the same synthetic in-memory WASM desugarer generate byte-identical Rust; the program cannot run without that artifact and the equality is a generated-text comparison. | codegen, checker, parser | - |
| `test_basic_function_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/basic_function.incn` | - | codegen, parser | twinned by the program run to completion, printing the sum the retired snapshot pinned the Rust for. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_function_references_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/function_references.incn` | - | codegen, parser | twinned by the RFC 035 program with a main that prints each pair, the zero-argument result and the mapped list; the snapshot of the Rust dies. |
| `test_user_defined_decorators_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_decorators.incn` | - | codegen, parser | twinned by the RFC 036 program whose decorator prints `decorating label` once, before `main` prints its own marker and before two calls of the decorated name (`7`, `9`): the decorator runs once when the binding is initialized, not per call; the projection-count and `__incan_original_label` helper-name assertions are generated shape and die with #654. |
| `decorated_function_emits_one_source_projection_and_distinct_generated_helpers` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_decorators.incn` | - | codegen, generated_text, parser | twinned by the RFC 036 decorators program (decorator printed once, two calls of the replacement); the projection-count and `__incan_original_label` helper-name assertions are generated shape and die with #654. |
| `decorator_factory_calls_use_the_resolved_projection_for_free_and_method_wrappers` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/decorator_factory_on_function_and_method.incn` | - | codegen, parser | twinned by the program whose factory prints `preserving` once per decorated declaration, both before `main` starts, and prints both decorated results; the assertion that both initializers name the factory's projection is generated shape and dies with #654. |
| `ordinary_function_declaration_and_call_share_one_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/function_alias_and_partial.incn` | - | codegen, parser | twinned by one program calling the function directly, through its binding alias and through the partial; the projection and `fn compute` shape assertions die with #654. |
| `same_module_function_alias_calls_the_target_projection_without_a_second_definition` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/function_alias_and_partial.incn` | - | codegen, generated_text, parser | twinned by one program calling the function directly, through its binding alias and through the partial; the projection and `fn compute` shape assertions die with #654. |
| `partial_wrapper_declaration_and_call_share_one_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/function_alias_and_partial.incn` | - | codegen, parser | twinned by one program calling the function directly, through its binding alias and through the partial; the projection and `fn compute` shape assertions die with #654. |
| `cross_module_function_alias_imports_and_calls_the_provider_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/cross_module_function_alias` | - | codegen, generated_text, parser | twinned by a module fixture calling the aliased import; the `use crate::helpers::<projection>` shape assertion dies with #654. |
| `decorated_method_declaration_and_call_share_one_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_method_decorators.incn` | - | codegen, generated_text, parser | twinned by the RFC 036 method-decorators program (decorator printed once, two calls of the replacement); the projection-count and `__incan_original_label` assertions are generated shape and die with #654. |
| `ordinary_method_declaration_and_call_share_one_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/method_classmethod_staticmethod_property.incn` | - | codegen, parser | twinned by one program calling the instance method, classmethod, staticmethod and computed property; the projection spellings die with #654. |
| `public_inherent_methods_keep_source_spelled_rust_forwarders` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/public_model_static_method_and_property.incn` | - | codegen, parser | twinned by the program printing the static factory, instance method and property results; the source-spelled Rust forwarder assertions die with #654. |
| `classmethod_and_staticmethod_declarations_and_calls_share_their_projections` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/method_classmethod_staticmethod_property.incn` | - | codegen, parser | twinned by one program calling the instance method, classmethod, staticmethod and computed property; the projection spellings die with #654. |
| `static_factory_projection_coexists_with_same_named_instance_field` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/static_factory_named_like_field.incn` | - | codegen, parser | twinned by the program run to completion; the projection and `pub days: Days` shape assertions die with #654. |
| `type_owned_and_instance_owned_methods_with_one_spelling_project_separately` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/same_spelling_type_owned_and_instance_methods.incn` | - | codegen, parser | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `field_factory_and_accumulator_with_one_spelling_codegen_as_a_fluent_chain` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/same_spelling_type_owned_and_instance_methods.incn` | - | codegen, parser | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `newtype_associated_declaration_and_call_share_one_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/validated_newtype_hook_and_coercion.incn` | - | codegen, parser | twinned by one program calling the hook with a valid and an invalid value and coercing an int argument; the hook-projection spellings die with #654. |
| `validated_newtype_implicit_coercion_calls_the_hook_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/validated_newtype_hook_and_coercion.incn` | - | codegen, parser | twinned by one program calling the hook with a valid and an invalid value and coercing an int argument; the hook-projection spellings die with #654. |
| `method_alias_calls_the_target_projection_without_a_second_definition` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/method_alias_and_partial.incn` | - | codegen, generated_text, parser | twinned by one program calling a method alias and a method partial; the `fn short` presence/absence assertions are generated shape and die with #654. |
| `method_partial_uses_an_explicit_generated_wrapper_and_preserves_the_target_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/method_alias_and_partial.incn` | - | codegen, generated_text, parser | twinned by one program calling a method alias and a method partial; the `fn short` presence/absence assertions are generated shape and die with #654. |
| `computed_property_getter_and_access_share_one_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/method_classmethod_staticmethod_property.incn` | - | codegen, parser | twinned by one program calling the instance method, classmethod, staticmethod and computed property; the projection spellings die with #654. |
| `trait_method_keeps_abi_slot_and_exposes_one_recoverable_concrete_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/trait_method_and_adopted_default.incn` | - | codegen, generated_text, parser | twinned by one program calling an implemented and an adopted-default trait method on the concrete receivers, and the implemented one again through `describe[T with Labeled](x: T)` (two calls, `ready` and `bound`), so the trait slot is reached through a bound as well as directly; the ABI-slot and projection assertions die with #654. |
| `trait_targeted_method_overloads_keep_distinct_recoverable_projections` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/newtype_trait_targets.incn` | - | codegen, parser | twinned by the same RFC 043 program; the distinct-projection count assertion is generated shape and dies with #654. |
| `a_type_owned_call_with_explicit_type_arguments_projects_like_its_declaration` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/classmethod_with_explicit_type_arguments.incn` | - | codegen, parser | twinned by the program instantiating the generic classmethod for int and str; the turbofish projection assertion dies with #654. |
| `a_derived_method_keeps_its_source_spelling_instead_of_a_wrapper_that_does_not_exist` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/derived_default_associated_function.incn` | - | codegen, parser | twinned by the program printing the derived default; the `Point::default()` spelling assertion dies with #654. |
| `adopted_default_method_exposes_a_recoverable_projection_beside_the_trait_slot` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/trait_method_and_adopted_default.incn` | - | codegen, generated_text, parser | twinned by one program calling an implemented and an adopted-default trait method on the concrete receivers, and the implemented one again through `describe[T with Labeled](x: T)` (two calls, `ready` and `bound`), so the trait slot is reached through a bound as well as directly; the ABI-slot and projection assertions die with #654. |
| `rust_extern_wrapper_is_projected_but_delegated_rust_symbol_is_not` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/rust_extern_delegation.incn` | - | codegen, generated_text, parser | twinned by the program calling the stdlib extern through its source wrapper and aborting on a failing assertion; the projection and `incan_std_testing::fail_t` symbol assertions die with #654. |
| `reexport_chain_preserves_the_provider_projection_without_alias_guessing` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/reexport_chain` | - | codegen, generated_text, parser | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `test_decorated_variadic_function_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/decorated_variadic_function.incn` | - | codegen, parser | twinned by the program printing the decorated variadic result for both keyword branches. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_user_defined_method_decorators_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_method_decorators.incn` | - | codegen, parser | twinned by the RFC 036 program whose method decorator prints `decorating label` once, before `main` prints its own marker and before two calls of the decorated method (`8`, `10`); the projection-count and `__incan_original_label` assertions are generated shape and die with #654. |
| `test_user_defined_mutable_method_decorators_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_mutable_method_decorators.incn` | - | codegen, parser | twinned by the program whose decorator prints `decorating bump` once, before `main` starts and before two successive bumps through the decorated mutating method (`3`, `5`). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc070_result_combinators_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/result_combinators.incn` | - | codegen, parser | twinned by one RFC 070 program using map, and_then, inspect with a callable object and with a named function over a non-Copy payload, and unwrap inside `direct[E](result: Result[int, E]) -> int`, generic over an unbounded `E`. The Copy-payload named-observer inspect (`.inspect(observe_int)`) is not in the twin: the build passes the fn item where the helper wants `fn(&T)` (E0308), so it never compiled. The `crate::__incan_std::result::*` helper assertions die with #654. |
| `test_rfc070_result_unwrap_codegen_does_not_require_debug_err` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/result_combinators.incn` | - | codegen, parser | twinned by the RFC 070 combinators program: `direct[E](result: Result[int, E]) -> int` unwraps an Ok payload with `E` unbounded, called with a `Result[int, PlainError]` (`9`); a regression to Rust's `.unwrap()`, which needs `E: Debug`, would fail to compile that generic function. The assertions on the lowered spelling (an explicit `match result { Ok(v) => v, Err(_) => panic!... }` and no `result.unwrap()`) die with #654. |
| `test_rfc070_result_inspect_non_copy_observer_borrows_payload` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/result_combinators.incn` | - | codegen, generated_text, parser | twinned by one RFC 070 program using map, and_then, inspect with a callable object and with a named function over a non-Copy payload, and unwrap inside `direct[E](result: Result[int, E]) -> int`, generic over an unbounded `E`. The Copy-payload named-observer inspect (`.inspect(observe_int)`) is not in the twin: the build passes the fn item where the helper wants `fn(&T)` (E0308), so it never compiled. The `crate::__incan_std::result::*` helper assertions die with #654. |
| `test_dict_operations_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/dict_operations.incn` | - | codegen, parser | twinned by the program run to completion, keys in any order. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_model_struct_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/model_struct.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_uppercase_var_field_access_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/uppercase_var_field_access.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_model_with_alias_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/model_with_alias.incn` | - | codegen, parser | twinned by the program constructing the model through its alias names and printing every field; only `type` and `status` are read through their aliases as well as their field names (the other fields have none). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_model_with_serde_alias_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/model_with_serde_alias.incn` | - | codegen, parser | twinned by the program printing the JSON with alias keys and reading it back. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_model_alias_expressions_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/model_alias_expressions.incn` | - | codegen, parser | twinned by the RFC 021 program printing alias construction, alias field access, self access and mixed access. The constructor-pattern half (`Account(type="premium") =>`) is covered by `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/partial_constructor_pattern.incn` (#1707, #1708). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_model_alias_self_access_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/model_alias_expressions.incn` | - | codegen, parser | twinned by the alias-expressions program, whose `get_type` method reads `self.type` and is printed. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_web_route_extractors_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/web_route_extractors.incn` | - | codegen, parser | twinned by an async program awaiting both route handlers directly (they return str because the build cannot return `int` from a handler); the framework-leakage and routing-module assertions die with #654. |
| `test_std_web_routing_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/web_route_extractors.incn` | - | codegen, parser | twinned by the web-route fixture importing std.web routing; the `raise_runtime_misuse` helper is observable when it fires (a route fixture expecting it would declare `expect-exit: 101`), and its and the `panic!("decorator marker` absence assertions are generated shape that dies with #654. |
| `imported_stdlib_static_method_defaults_expand_at_call_site_issue500` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/imported_stdlib_defaults.incn` | - | codegen, parser | twinned by one program calling `Time.from_fields` and `OrdinalMapError.invalid_key_record` with their defaults omitted; `App.run()` itself is not called because it binds a server. The expanded-argument spelling assertions die with #654. |
| `imported_stdlib_associated_function_defaults_expand_in_generated_rust` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/imported_stdlib_defaults.incn` | - | codegen, parser | twinned by one program calling `Time.from_fields` and `OrdinalMapError.invalid_key_record` with their defaults omitted; `App.run()` itself is not called because it binds a server. The expanded-argument spelling assertions die with #654. |
| `test_web_route_extractors_nested_module_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/web_route_nested_module` | - | codegen, parser | twinned by a module fixture whose nested `api/routes.incn` declares the handlers (with `{id}` path segments so the handlers are extractable) and whose main awaits them. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_web_route_private_nested_module_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/web_route_private_nested_module` | - | codegen, generated_text, parser | twinned by a module fixture that builds and runs with the private handler and model in the nested module; the retention and visibility assertions are generated shape and die with #654. `App.run` is not called because it binds a server. |
| `test_async_main_runtime_bootstrap_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_tasks_and_time.incn` | - | codegen, parser | twinned by one async program using spawn, spawn_blocking, yield_now, sleep, sleep_ms, timeout and timeout_ms (the expired deadline prints `error.message()`, `operation timed out`) and timeout_join_ms, whose `TimedOut(handle)` outcome is awaited afterwards for the task's value (`still running`, `3`); `Duration.*` and `JoinHandle.abort()` have no fixture. The stdlib module snapshots and the async-main bootstrap shape die with #654. |
| `test_web_route_codegen_no_framework_crate_leakage` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/web_route_extractors.incn` | - | codegen, parser | twinned by an async program awaiting both route handlers directly (they return str because the build cannot return `int` from a handler); the framework-leakage and routing-module assertions die with #654. |
| `test_literals_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/literals.incn` | - | codegen, parser | twinned by the program printing every literal. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_operators_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/operators.incn` | - | codegen, parser | twinned by the program printing every operator result including the #1066 call-result probe. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_user_defined_operators_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/user_defined_operators.incn` | - | codegen, parser | twinned by the program printing every dunder-driven operator result. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc068_protocol_hooks_lower_to_method_calls` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/protocol_hooks.incn` | - | codegen, parser | twinned by the RFC 068 program driving every hook and printing the results; the `__bool__()`/`__len__()` call-spelling assertions die with #654. |
| `test_fallible_iteration_protocol_propagates_next_errors_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/fallible_iteration_protocol.incn` | - | codegen, parser | twinned by one program looping a concrete (`drain_concrete(fail_at)`, run to exhaustion and run into `broken at 2`) and a trait-typed fallible iterator, propagating the first Err through `?` on both receivers; the `.__next__()?` spelling assertions die with #654. |
| `test_fallible_iteration_protocol_lowers_trait_typed_receiver_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/fallible_iteration_protocol.incn` | - | codegen, parser | twinned by one program looping a concrete (`drain_concrete(fail_at)`, run to exhaustion and run into `broken at 2`) and a trait-typed fallible iterator, propagating the first Err through `?` on both receivers; the `.__next__()?` spelling assertions die with #654. |
| `test_source_callable_bound_preserves_nominal_trait_with_native_callable_blanket_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/callable_bound_closure_and_model.incn` | - | codegen, parser | twinned by the program printing both applications; the nominal-bound and `Callable1::<i64,String,>::__call__` spelling assertions die with #654. |
| `test_std_fallible_loop_preserves_qualified_trait_dispatch_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_fallible_iterator_loop.incn` | - | codegen, parser | twinned by one program looping and collecting a stdlib FallibleIterator adopter; the qualified-dispatch and `&mut self` receiver assertions die with #654. |
| `generic_fallible_iterator_consumer_inherits_implementation_bounds_issue1280` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/generic_fallible_iterator_consumer.incn` | - | codegen, parser | twinned by the #1280 program draining the generic adopter through both consumer functions; the `<R: Clone>` bound-spelling assertions die with #654. |
| `compiled_provider_consumer_inherits_manifest_implementation_bounds_issue1280` | retire | - | - | codegen, checker, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/compiled_provider_fallible_stream/`, a project fixture whose `deps/streams` package declares a generic FallibleIterator model `Stream[R]`; the consumer's `drain[R](stream: Stream[R])` loops it with `?` and prints the count, `2`. waits for a Cargo-free bake under the suite: the in-fixture provider's bake goes through the compatibility publisher, which under the suite reaches Cargo; the candidate passes after a manual `incan oven bake --project deps/<name>`. The `<R: Clone>` bound-spelling assertion dies with #654. |
| `test_std_fallible_adapter_loop_inherits_qualified_trait_dispatch_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_fallible_adapter_loop.incn` | - | codegen, parser | twinned by the program consuming a BytesIO in 16-byte chunks through map_err and ?; the qualified `FallibleIterator::<Vec<u8>,String,>::__next__` dispatch assertion dies with #654. |
| `test_fallible_iterator_adapter_chain_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/fallible_iterator_adapter_chain.incn` | - | codegen, parser | twinned by the program run to completion; the adapter-chain, `impl FallibleStream<U,String> + use<U>` and `let mut stream` shape assertions die with #654. |
| `test_projected_fallible_terminal_uses_mutable_receiver` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_fallible_iterator_loop.incn` | - | codegen, parser | twinned by one program looping and collecting a stdlib FallibleIterator adopter; the qualified-dispatch and `&mut self` receiver assertions die with #654. |
| `test_mixed_numeric_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/mixed_numeric_and_float_display.incn` | - | codegen, parser | twinned by one program printing the mixed arithmetic total and every float display position; the `float_to_string` count and `format!("{:?}")` assertions die with #654. |
| `test_float_display_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/mixed_numeric_and_float_display.incn` | - | codegen, parser | twinned by one program printing the mixed arithmetic total and every float display position; the `float_to_string` count and `format!("{:?}")` assertions die with #654. |
| `test_std_math_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_math.incn` | - | codegen, parser | twinned by the program printing each constant and function beside the original NaN-poisoned total. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_std_fs_import_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_fs_path.incn` | - | codegen, parser | twinned by the program using the imported Path lexically; the `pub use crate::__incan_std::fs::Path` vs web Path assertions die with #654. |
| `test_issue1668_std_environ_args_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_environ_args.incn` | - | codegen, parser | twinned by the #1668 program run without arguments; the `crate::__incan_std::environ::args()` spelling assertions die with #654. |
| `test_issue1668_str_encode_bytes_decode_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/str_encode_bytes_decode.incn` | - | codegen, generated_text, parser | twinned by the #1668 program printing every encode/decode result including the strict-mode refusal; the `from_utf8`/`from_utf8_lossy` assertions die with #654. |
| `test_issue1668_dict_contains_key_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/dict_contains_key_and_mut_dict_params.incn` | - | codegen, parser | twinned by one program for both #1668 dict cases; the `contains_key`/`insert`/`AsRef<str>` spelling assertions die with #654. |
| `test_issue1668_mut_dict_param_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/dict_contains_key_and_mut_dict_params.incn` | - | codegen, parser | twinned by one program for both #1668 dict cases; the `contains_key`/`insert`/`AsRef<str>` spelling assertions die with #654. |
| `test_issue1668_mut_str_match_arm_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/mut_str_match_arm.incn` | - | codegen, parser | twinned by the #1668 program printing every arm; the `.to_string()` assignment assertions die with #654. |
| `test_issue1668_dict_keys_outside_comprehension_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/dict_keys_and_values_outside_comprehensions.incn` | - | codegen, parser | twinned by one program for #1668 keys/values and #1461 sorted keys; the `.keys().cloned().collect::<Vec<_>>()` assertions die with #654. |
| `test_issue1461_sorted_dict_keys_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/dict_keys_and_values_outside_comprehensions.incn` | - | codegen, generated_text, parser | twinned by one program for #1668 keys/values and #1461 sorted keys; the `.keys().cloned().collect::<Vec<_>>()` assertions die with #654. |
| `test_issue1464_list_constructor_codegen` | retire | - | - | codegen, generated_text, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/list_constructor_sources.incn`: `list()` over dict views, lists, sets, a mutable parameter, a dict, a str, nothing, bytes, a range, frozen lists, an iterator, a reused source and the process arguments (`rust::std::env`), printing the lengths `2 2 2 3 2 2 3 0 2 3 1 1 4 2` and `1`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The `list(generator)` and `FrozenList([...])` cases are not in the candidate: the build emits `.collect::<Vec<_>>()` on the runtime Generator and a `FrozenList(...)` tuple constructor that does not exist. |
| `test_issue1490_comprehension_over_rust_iterator_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/comprehension_over_rust_iterator.incn`: list and dict comprehensions iterate a Rust iterator of process arguments directly and print `1` four times. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The comprehension over a Generator source is not in the candidate (the same `.collect::<Vec<_>>()` legacy gap). |
| `test_std_tempfile_import_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_tempfile.incn` | - | codegen, parser | twinned by the program creating each temporary kind and printing the spool state; the `pub use` re-export assertions die with #654. |
| `test_function_calls_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/function_calls.incn` | - | codegen, parser | twinned by the program printing every call result the snapshot recorded (positional, default, builtin, method, chained, list and dict calls). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_variadic_calls_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/variadic_calls.incn` | - | codegen, parser | twinned by the program printing the three rest-parameter sums and their total instead of returning the total from main. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_collections_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/collections.incn` | - | codegen, parser | twinned by the program printing every index, slice, concatenation and extension result. The bare `Tuple`-annotated literal is dropped: a bare `Tuple` annotation is refused (#1717, fixture `cli/refused_bare_tuple_annotation.incn`). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue633_question_mark_list_comprehension_codegen_uses_loop` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/question_mark_in_list_comprehension.incn` | - | codegen, parser | twinned by the #633 program with a succeeding and a failing list; the loop-vs-`.map(...?)` lowering assertion dies with #654. |
| `test_list_repeat_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/list_repeat.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc088_iterator_adapters_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/iterator_adapters.incn` | - | codegen, parser | twinned by the RFC 088 program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue950_953_iterator_adapter_sources_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/iterator_adapter_sources.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue951_set_constructor_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/set_constructor.incn` | - | codegen, parser | twinned by the #951 program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue963_set_add_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/set_add_and_shadowing.incn` | - | codegen, parser | twinned by one program for #963 and #951 shadowing. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue969_storable_sha256_hasher_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/storable_sha256_hasher.incn` | - | codegen, parser | twinned by the #969 program printing a matching and a mismatching digest. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue951_set_shadowing_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/set_add_and_shadowing.incn` | - | codegen, parser | twinned by one program for #963 and #951 shadowing. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue1116_builtin_len_shadowing_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/builtin_len_shadowing.incn` | - | codegen, parser | twinned by the #1116 program run to completion; the `fn len(value: i64)` shape assertion dies with #654. |
| `builtin_json_stringify_evaluates_its_operand_once` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/json_stringify_builtin.incn` | - | codegen, parser | twinned by one program printing the results of both spellings, `None` and the widest int; the `let __incan_json_value = &(...)` binding assertions die with #654. |
| `builtin_json_stringify_gives_untyped_none_a_concrete_rust_type` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/json_stringify_builtin.incn` | - | codegen, parser | twinned by one program printing the results of both spellings, `None` and the widest int; the `let __incan_json_value = &(...)` binding assertions die with #654. |
| `builtin_json_stringify_preserves_the_incan_int_width` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/json_stringify_builtin.incn` | - | codegen, parser | twinned by one program printing the results of both spellings, `None` and the widest int; the `let __incan_json_value = &(...)` binding assertions die with #654. |
| `test_issue950_builtin_zip_only_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/builtin_zip_only.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_empty_list_string_arg_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/empty_list_string_arg.incn` | - | codegen, parser | twinned by the program printing the length instead of returning it from main. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_generic_model_field_access_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/generic_model_field_access.incn` | - | codegen, parser | twinned by the program printing the field for an int and a str instantiation. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue1370_phantom_type_param_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/phantom_type_param.incn` | - | codegen, generated_text, parser | twinned by the #1370 program run to completion; the PhantomData marker, derive-list, HasFieldInfo and literal-count assertions are generated shape and die with #654. |
| `test_lowercase_types_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/lowercase_types.incn` | - | codegen, parser | twinned by the program printing the annotated values; the tuple is read by index because the build does not bind names in a tuple pattern over a `tuple[...]`-typed scrutinee. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_assignments_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/assignments.incn` | - | codegen, parser | twinned by the program printing the final values of every assignment form. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_control_flow_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/control_flow.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_pattern_alternation_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/pattern_alternation.incn` | - | codegen, parser | twinned by the program printing both alternation forms for every alternative. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc049_if_let_while_let_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/if_let_while_let.incn` | - | codegen, parser | twinned by the RFC 049 program printing both helpers for Some and None. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_returns_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/returns.incn` | - | codegen, parser | twinned by the program printing every return-form result. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_loops_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/loops.incn` | - | codegen, parser | twinned by the program printing each loop's sequence on one line (a local accumulator per test function). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_match_statements_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/match_statements.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue492_rust_result_match_does_not_clone_scrutinee` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_result_match_scrutinee.incn`: matches a Rust `read_dir` Result and each entry Result in a loop over a temporary directory holding one file, printing `only.txt`, then `missing` for a missing directory. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The `entry_result.clone()` absence assertion dies with #654. |
| `test_type_annotations_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/type_annotations.incn` | - | codegen, parser | twinned by the program printing the annotated values. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc029_union_types_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/union_types.incn` | - | codegen, parser | twinned by the RFC 029 program calling every narrowing function with each member type; the `isinstance(` absence assertion dies with #654. |
| `isinstance_alias_target_uses_the_typecheckers_resolved_target_in_native_lowering` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/isinstance_explicit_builtin_and_aliases.incn` | - | codegen, parser | twinned by the program calling every function with each member type, through helper functions that build the FrozenStr unions because the build does not coerce a const into a union at a call. The `matches!`/`match value` lowering-shape assertions die with #654. |
| `test_issue501_option_union_isinstance_codegen_no_raw_call` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/newtype_union_isinstance.incn` | - | codegen, parser | twinned by one program for #501 and #502, building the optional union through helper functions because the build does not coerce a literal into `Option[union]` at a call; the `isinstance(` and `=> {}` absence assertions die with #654. |
| `test_issue502_exhaustive_independent_isinstance_branches_codegen_has_no_unit_fallback` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/newtype_union_isinstance.incn` | - | codegen, parser | twinned by the same program's `normalize_path_like`, called for both members; the `=> {}` absence assertion dies with #654. |
| `test_issue457_461_cross_module_union_codegen_uses_crate_wrapper` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/cross_module_union` | - | codegen, parser | twinned by a module fixture passing a producer's union to a consumer and a literal to the consumer; the crate-level `__IncanUnion` wrapper assertions die with #654. |
| `test_string_operations_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/string_operations.incn` | - | codegen, parser | twinned by the program printing every string result. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue236_non_string_join_codegen` | retire | `dies` | declares a `rusttype` over `rust::mail::Dataset`, a crate that exists nowhere (the checker treats `rust::mail` as an opaque foreign path); the program can be checked and emitted but never built or run, and the assertion is the snapshot of the delegating `join` call. The checker facts about `rusttype` declarations are keep-class in the `rust_*` typechecker modules (#1337). | codegen, parser | - |
| `test_issue244_recursive_mut_list_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/recursive_mut_list_and_mut_str_params.incn` | - | codegen, parser | twinned by one program for both #244 cases, printing the visited flags, the drained queue and the mut str; the `&mut` vs `.clone()` assertions die with #654. |
| `test_issue244_mut_str_param_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/recursive_mut_list_and_mut_str_params.incn` | - | codegen, parser | twinned by one program for both #244 cases, printing the visited flags, the drained queue and the mut str; the `&mut` vs `.clone()` assertions die with #654. |
| `test_issue241_field_backed_method_arg_clone_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/field_backed_method_arg.incn` | - | codegen, parser | twinned by the program merging two wrappers and printing all three labels; the clone-vs-borrow assertion dies with #654. |
| `test_issue364_filtered_list_comp_borrow_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/filtered_comprehensions.incn` | - | codegen, parser | twinned by one program for #364, the dict-comprehension predicate and #602 copy hot paths; the `filter_map`/`copied` shape assertions die with #654. |
| `test_rfc006_generator_expression_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/generator_expression_and_yield.incn` | - | codegen, parser | twinned by one program collecting a nested generator expression and a yield-based generator; the `Generator::new`/`spawn`/`yield_value` shape assertions die with #654. |
| `test_rfc006_generator_function_yield_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/generator_expression_and_yield.incn` | - | codegen, parser | twinned by one program collecting a nested generator expression and a yield-based generator; the `Generator::new`/`spawn`/`yield_value` shape assertions die with #654. |
| `test_issue366_clone_self_string_field_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/clone_self_string_field.incn` | - | codegen, parser | twinned by the program printing the copy and the untouched original; the `.clone()` site assertion dies with #654. |
| `test_filtered_dict_comp_predicate_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/filtered_comprehensions.incn` | - | codegen, parser | twinned by one program for #364, the dict-comprehension predicate and #602 copy hot paths; the `filter_map`/`copied` shape assertions die with #654. |
| `test_issue602_comprehension_copy_hotpaths_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/filtered_comprehensions.incn` | - | codegen, parser | twinned by one program for #364, the dict-comprehension predicate and #602 copy hot paths; the `filter_map`/`copied` shape assertions die with #654. |
| `test_issue602_owned_iterator_source_hotpaths_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/owned_iterator_sources.incn` | - | codegen, parser | twinned by the #602 program run to completion; the `ListIterator{items: ...}`/`.clone().into_iter()` assertions die with #654. |
| `test_functions_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/functions.incn` | - | codegen, parser | twinned by the program run to completion; the doubled list and the tuple are printed instead of the original placeholder lines. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_classes_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/classes.incn` | - | codegen, parser | twinned by the program run to completion, which also calls `Counter.reset`, `Stack.is_empty`, `Calculator.subtract`/`multiply` and reads `Calculator.name` and `Person.age` through methods. `Stack.pop` takes `mut self` in the twin: a mutation inside a plain `self` method is refused (#1723, INCAN-T0102, fixture `cli/refused_mutation_through_plain_self.incn`). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue246_class_field_visibility_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/class_field_visibility.incn` | - | codegen, parser | twinned by the program printing the private backing field through its method and the public field directly. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_generic_methods_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/generic_methods.incn` | - | codegen, parser | twinned by the program printing every generic method result. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue731_generic_method_defaults_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/generic_method_defaults.incn` | - | codegen, parser | twinned by the #731 program printing positional, named and defaulted calls. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_explicit_call_site_generics_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/explicit_call_site_generics.incn` | - | codegen, parser | twinned by the RFC 054 program printing explicitly instantiated calls; the turbofish shape dies with #654. |
| `test_models_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/models.incn` | - | codegen, parser | twinned by the program run to completion, with every declared field read (the Option fields, the nested collections, `Point` and `ComplexModel`, which the retired program never constructed) and the json-derived model printed. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_power_receiver_parenthesization_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/power_operator_receivers.incn` | - | codegen, parser | twinned by the program printing both power results; the `.powf` parenthesization assertions die with #654. |
| `exact_float_boundaries_emit_finite_guards_without_changing_ordinary_float` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/exact_float_finite_guards.incn` | - | codegen, parser | twinned by one program observing exact f32/f64 results and ordinary NaN, ending with a NaN reaching an exact f64 return (exit 101); the `require_finite_*` count assertions die with #654. |
| `exact_float_arithmetic_is_validated_before_every_observable_use` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/exact_float_finite_guards.incn` | - | codegen, parser | twinned by one program observing exact f32/f64 results and ordinary NaN, ending with a NaN reaching an exact f64 return (exit 101); the `require_finite_*` count assertions die with #654. |
| `exact_float_public_and_rust_ingress_is_guarded_before_observation` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/exact_float_ingress_and_aggregates.incn` | - | codegen, parser | twinned by one program calling the real `incan_std_core::num::require_finite_f32` extern, observing exact f32/f64 values through println, str() and f-strings (`2.5`, `3`, `2.5`; an integral f32 prints `4` where the ordinary float path prints `nan`, so Display and `float_to_string` are told apart), reading scalars from a model field, an `Option[f32]` field and a list, and aborting (exit 101) when `parsed_exact` converts a parsed NaN `float` into its exact `f64` return, before the callee is entered. The pub-parameter ingress guards the test asserted (`require_finite_f32(left)` on entry) are reachable only from a Rust caller: an Incan caller cannot hand a NaN to an exact parameter without tripping an earlier conversion, so the fixture cannot observe them and the guard-placement assertions die with #654. |
| `exact_float_scalars_extracted_from_aggregates_are_guarded_before_use` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/exact_float_ingress_and_aggregates.incn` | - | codegen, parser | twinned by one program calling the real `incan_std_core::num::require_finite_f32` extern, observing exact f32/f64 values through println, str() and f-strings (`2.5`, `3`, `2.5`; an integral f32 prints `4` where the ordinary float path prints `nan`, so Display and `float_to_string` are told apart), reading scalars from a model field, an `Option[f32]` field and a list, and aborting (exit 101) when `parsed_exact` converts a parsed NaN `float` into its exact `f64` return, before the callee is entered. The pub-parameter ingress guards the test asserted (`require_finite_f32(left)` on entry) are reachable only from a Rust caller: an Incan caller cannot hand a NaN to an exact parameter without tripping an earlier conversion, so the fixture cannot observe them and the guard-placement assertions die with #654. `consume_exact` is replaced by that real extern because no such symbol exists. |
| `mixed_f32_arithmetic_widens_operands_for_f64_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/mixed_f32_arithmetic.incn` | - | codegen, parser | twinned by the program printing every widened result; the `(left) as f64` operand assertions die with #654. |
| `test_rfc046_computed_properties_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/computed_properties.incn` | - | codegen, generated_text, parser | twinned by the RFC 046 program printing the property sum and the trait-required property; the `pub fn dollars(&self)` shape assertion dies with #654. |
| `test_list_pop_clone_only_model_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/list_pop_and_clone_of_models.incn` | - | codegen, parser | twinned by one program draining a model list through pop() and copying one through clone(); the source list is not `mut` (only the copy is appended to) and stays readable after the call. The stdlib-helper routing assertions die with #654. |
| `test_list_clone_model_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/list_pop_and_clone_of_models.incn` | - | codegen, parser | twinned by one program draining a model list through pop() and copying one through clone(); the source list is not `mut` (only the copy is appended to) and stays readable after the call. The stdlib-helper routing assertions die with #654. |
| `test_issue380_len_comparison_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/len_comparison.incn` | - | codegen, parser | twinned by the #380 program printing both comparisons; the `::std::convert::identity(... as i64)` assertions die with #654. |
| `test_issue383_loop_helper_shared_string_list_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/shared_string_list_helpers.incn` | - | codegen, parser | twinned by one program for both #383 cases; the `xs.clone()`/`name.clone()` assertions die with #654. |
| `test_issue383_dict_comp_reuses_noncopy_key_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/shared_string_list_helpers.incn` | - | codegen, parser | twinned by one program for both #383 cases; the `xs.clone()`/`name.clone()` assertions die with #654. |
| `test_for_in_list_enum_equality_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/for_in_list_enum_equality.incn` | - | codegen, parser | twinned by the #195 program printing a hit and a miss; the `.iter().cloned()` assertion dies with #654. |
| `test_issue372_imported_enum_loop_ownership_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/imported_enum_loop_ownership` | - | codegen, parser | twinned by a module fixture looping over the imported enum list; the `.iter().cloned()` assertion dies with #654. |
| `test_issue886_imported_private_source_class_constructor_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/imported_private_source_class_constructor` | - | codegen, parser | twinned by a module fixture constructing the twice re-exported class from the consumer and printing its public and defaulted fields; the provider-bridge constructor spelling dies with #654. |
| `test_issue964_imported_private_source_model_constructor_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/imported_private_source_model_constructor` | - | codegen, parser | twinned by a module fixture constructing the twice re-exported model from the consumer and printing its public and defaulted fields; the public-field bridge spelling dies with #654. |
| `test_issue377_imported_sum_shadows_builtin_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/imported_helper_named_like_builtin` | - | codegen, generated_text, parser | twinned by a module fixture calling the imported `sum`; the `.iter().sum::<i64>()` avoidance assertion dies with #654. |
| `test_traits_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/traits.incn` | - | codegen, parser | twinned by the program run to completion. `Resizable.resize` takes `mut self` in the twin: a field assignment inside a plain `self` method is refused (#1723, INCAN-T0102, fixture `cli/refused_mutation_through_plain_self.incn`). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_trait_supertraits_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/trait_supertraits.incn` | - | codegen, parser | twinned by the RFC 042 program run to completion; the `impl<T: Clone>` shape assertions die with #654. |
| `test_trait_supertrait_assignability_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/trait_supertrait_assignability.incn` | - | codegen, parser | twinned by the RFC 042 program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_enums_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/enums.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_enum_methods_traits_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/enum_methods_and_traits.incn` | - | codegen, parser | twinned by the RFC 050 program calling the trait method through the trait, on a variant and on the associated constructor's result; the impl-block shape assertions die with #654. |
| `test_rfc043_newtype_trait_targets_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/newtype_trait_targets.incn` | - | codegen, parser | twinned by the RFC 043 program dispatching both targeted methods through their bounds; the `impl ToInt for Value`/`type Output` shape assertions die with #654. |
| `test_rfc043_imported_trait_associated_type_codegen` | retire | `dies` | declares an associated type for `rust::demo::Assoc`, a crate that exists nowhere; the program can be checked and emitted but never built or run, and the assertion is the `impl Assoc for Boxed { type Item = i64; }` snapshot. | codegen, parser | - |
| `test_rfc043_rust_derive_passthrough_codegen` | retire | - | - | codegen, generated_text, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_derive_passthrough.incn`: a model carrying pass-through Rust derives is constructed and its field printed, `1`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The `#[derive(serde::Serialize, Default, ...)]` list assertion dies with #654; `PayloadKey.default()` is not visible to the checker for a `@rust.derive(Default)`, so the candidate prints a constructed value only. |
| `test_value_enums_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/value_enums.incn` | - | codegen, parser | twinned by the RFC 032 program printing wire values and parses, including a rejected value. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_trailing_match_statement_is_returned_not_discarded` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/trailing_match_expression.incn` | - | codegen, generated_text, parser | twinned by one program for the three trailing-match forms; the `let _ = match` / `return match` shape assertions die with #654. |
| `test_match_arms_convert_to_the_match_result_type` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/trailing_match_expression.incn` | - | codegen, parser | twinned by one program for the three trailing-match forms (`one`, `one`, `other`); the assertions on the `let _ = match` / `return match` shapes and on the arm value being converted to the match result type (`"one".to_string()` is the dying assertion) die with #654. |
| `test_trailing_match_statement_in_unit_function_stays_a_statement` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/trailing_match_expression.incn` | - | codegen, parser | twinned by one program for the three trailing-match forms; the `let _ = match` / `return match` shape assertions die with #654. |
| `test_patterns_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/patterns.incn` | - | codegen, parser | twinned by the program run to completion. The tuple arm with a str literal element (`(_, "answer")` printing `second is answer`) is replaced by `(42, _)` printing `first is forty-two`; a str literal inside a tuple pattern is covered by `snapshots_enums_and_matching/tuple_str_literal_pattern.incn` (#1707). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_param_mut_unused_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/param_mut_unused.incn` | - | codegen, parser | twinned by the program with a main that prints the sum; the `mut`-marking assertion is generated shape. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_imports_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_std_imports.incn`: plain, aliased and nested imports of Rust standard-library items coexist in one module and its functions print `basic imports work`, `aliased imports work`, `nested imports work`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The `use` statement snapshot dies with #654. |
| `test_builtins_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/builtins.incn` | - | codegen, parser | twinned by the program run to completion; the `list_min_copy`/`list_max_copy` helper-routing assertions die with #654. |
| `test_pub_const_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/consts_and_statics.incn` | - | codegen, parser | twinned by one RFC 008 program printing every const and the static model's field, plus the public and private float consts. The tuple const is printed element by element (`x 42`): the retired program's `print(T)` emitted `println!("{}", T)` on a tuple, which never compiled (tuples have no Display). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_consts_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/consts_and_statics.incn` | - | codegen, parser | twinned by one RFC 008 program printing every const and the static model's field, plus the public and private float consts. The tuple const is printed element by element (`x 42`): the retired program's `print(T)` emitted `println!("{}", T)` on a tuple, which never compiled (tuples have no Display). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_descriptor_const_with_empty_frozen_list_codegen_issue1001` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/descriptor_const_with_empty_frozen_list.incn` | - | codegen, parser | twinned by the #1001 program printing the const comparison and fields; the `FrozenList::new(&[])` const-shape assertions die with #654. |
| `test_rfc052_module_static_storage_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/module_static_storage.incn` | - | codegen, parser | twinned by the RFC 052 program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc052_pub_static_codegen` | retire | - | - | codegen, checker, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/pub_import_widgets/` (shared with the widgets imports): the consumer appends to the package's `SHARED_ITEMS` static through a live alias and prints its length, `2`. waits for a Cargo-free bake under the suite: the in-fixture provider's bake goes through the compatibility publisher, which under the suite reaches Cargo; the candidate passes after a manual `incan oven bake --project deps/<name>`. The static-projection spelling dies with #654. |
| `test_const_str_chain_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/const_str_chain_and_bytes.incn` | - | codegen, parser | twinned by one program printing the chained str consts and the bytes/FrozenBytes lengths. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_const_bytes_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/const_str_chain_and_bytes.incn` | - | codegen, parser | twinned by one program printing the chained str consts and the bytes/FrozenBytes lengths. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_inferred_reassign_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/inferred_reassign.incn` | - | codegen, parser | twinned by the program run to completion. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rust_interop_associated_functions_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_interop_associated_functions.incn`: associated functions of imported Rust types are called (a random Uuid renders as 36 characters, a typed HashMap starts empty, `Instant.now()` is taken), printing `36` and `0`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The untyped `HashMap.new()` binding is not in the candidate: it is refused (#1720). |
| `test_issue806_rust_receiver_turbofish_codegen` | retire | `dies` | imports `rust::rubato::Fft`, an external crate the offline suite does not carry; the program cannot be built, and the assertion is the `Fft::new::<f32>` turbofish snapshot. | codegen, parser | - |
| `test_rust_associated_call_in_elif_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_associated_call_in_elif.incn`: an imported Rust associated function called inside an elif branch runs like one in the if branch, printing `true`, `false`, `false`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). |
| `test_issue367_result_ok_string_literal_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/result_ok_string_literal.incn` | - | codegen, parser | twinned by the #367 program run to completion; the `.to_string()` vs `Ok::<String, String>("...")` assertions die with #654. |
| `test_issue367_result_ok_string_literal_emits_owned_strings` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/result_ok_string_literal.incn` | - | codegen, parser | twinned by the #367 program run to completion; the `.to_string()` vs `Ok::<String, String>("...")` assertions die with #654. |
| `test_issue880_map_err_string_literal_closure_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/map_err_string_literal_closure.incn` | - | codegen, parser | twinned by the #880 program printing every mapped error; the closure and `T: Clone` spelling assertions die with #654. |
| `test_issue880_map_err_string_literal_closure_emits_owned_error` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/map_err_string_literal_closure.incn` | - | codegen, parser | twinned by the #880 program printing every mapped error; the closure and `T: Clone` spelling assertions die with #654. |
| `test_issue374_enum_constructor_match_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/enum_constructor_match.incn` | - | codegen, parser | twinned by the #374 program printing every variant's arm. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue389_for_tuple_unpack_enumerate_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/enumerate_tuple_unpack.incn` | - | codegen, parser | twinned by one program for #389 and #483 printing the bindings built each way; the `(idx as i64, value.clone())` assertions die with #654. |
| `test_issue483_list_comp_tuple_unpack_enumerate_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/enumerate_tuple_unpack.incn` | - | codegen, parser | twinned by one program for #389 and #483 printing the bindings built each way; the `(idx as i64, value.clone())` assertions die with #654. |
| `test_fixed_call_unpack_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/fixed_call_unpack.incn` | - | codegen, parser | twinned by the program printing each unpacked call's result. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_collection_literal_spread_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/collection_literal_spread.incn` | - | codegen, parser | twinned by the program printing the spread results; the `__incan_list.extend` shape assertions die with #654. |
| `test_issue391_list_str_append_literal_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/list_str_append_literal.incn` | - | codegen, parser | twinned by the program printing the appended list; the `.to_string()` vs `.clone()` assertion dies with #654. |
| `test_rust_interop_field_access_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_interop_field_access.incn`: a constant read from an imported Rust module alias prints its value, `3.141592653589793`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). |
| `test_issue217_rust_enum_match_bindings_codegen` | retire | `dies` | matches a `rusttype` over `rust::mail::Sender`, a crate that exists nowhere; the program can be checked and emitted but never built or run, and the assertion is the snapshot of the composed variant path. The checker facts are keep-class in the `rust_*` typechecker modules (#1337). | codegen, parser | - |
| `test_issue459_rust_enum_pattern_import_codegen` | retire | `dies` | the `substrait` crate the program matches against is a stub the test writes to a temporary directory for `rust_inspect`; no fixture layout carries a Rust crate, and the assertion is that the `use ::substrait::proto::rel::RelType;` line survives pruning (generated shape). If the harness grows a local-Rust-crate fixture layout, the pattern behavior can be twinned then. | codegen, generated_text, parser | - |
| `test_issue1491_rust_struct_variant_pattern_codegen` | retire | `dies` | imports `rust::cfg_expr::Predicate`, an external crate the offline suite does not carry; the program cannot be built, and the assertion is the named-field variant pattern snapshot. | codegen, parser | - |
| `test_issue1493_empty_list_comparison_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/empty_list_equality.incn` | - | codegen, generated_text, parser | twinned by one program for #1493 and #1476; the `Vec::<String>::new()` spelling assertions die with #654. |
| `test_issue1462_named_constructor_evaluation_order_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/named_constructor_evaluation_order.incn` | - | codegen, parser | twinned by the program printing the argument evaluation order and the filled fields; the `__incan_ctor_arg_N` assertion dies with #654. |
| `test_issue1489_loop_variable_returned_owned_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/loop_variable_returned_owned.incn` | - | codegen, parser | twinned by the #1489 program run to completion plus a miss; the `row.clone()`/`.to_string()` return assertions die with #654. |
| `test_issue1489_method_type_param_clone_bound_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/method_type_param_clone_bound.incn` | - | codegen, parser | twinned by the #1489 program calling every generic method and function on filled and empty inputs; the `Clone` bound placement is generated shape. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_issue1489_web_extractor_field_read_clones_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/web_extractor_field_reads.incn` | - | codegen, parser | twinned by an async program awaiting the three handlers with a constructed Query; the clone-site assertions die with #654. |
| `test_issue1494_collection_method_literal_arguments_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/collection_method_literal_arguments.incn` | - | codegen, parser | twinned by the #1494 program run to completion; the `Deque::<Unknown>` absence assertion dies with #654. |
| `test_issue1476_empty_list_equality_operands_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/empty_list_equality.incn` | - | codegen, generated_text, parser | twinned by one program for #1493 and #1476; the `Vec::<String>::new()` spelling assertions die with #654. |
| `test_rfc041_std_rust_capability_bounds_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_rust_capability_bounds.incn` | - | codegen, parser | twinned by the RFC 041 program with every `std.rust` marker: Send, Sync and Static over ordinary values, a Static-bounded identity, and Fn, FnMut and FnOnce each accepting a named function (#1716: the callable markers once emitted `Fn<i64>`, which rustc refuses). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc041_rusttype_interop_codegen` | retire | `dies` | declares `parse` and `as_str` with `...` bodies on a `rusttype` over `rust::std::string::String`; the legacy route projects both onto the Rust type, where `String::as_str` exists but `parse` does not (E0599), so the program never compiled and cannot run. The interop-clause checker facts are keep-class in the `rust_*` typechecker modules (#1337). | codegen, parser | - |
| `test_rfc041_rusttype_rebinding_codegen` | retire | `dies` | declares a `rusttype` with a method alias over `rust::mail::Sender`, a crate that exists nowhere; the program can be checked and emitted but never built or run. The rebinding checker facts are keep-class in the `rust_*` typechecker modules (#1337). | codegen, parser | - |
| `test_rfc041_interop_from_try_codegen` | retire | `dies` | declares an `interop: from str try` clause over `rust::mail::EmailAddress`, a crate that exists nowhere; the program can be checked and emitted but never built or run. The interop-clause checker facts are keep-class in the `rust_*` typechecker modules (#1337). | codegen, parser | - |
| `test_rfc041_interop_into_via_codegen` | retire | `dies` | declares `parse` with a `...` body on a `rusttype` over `rust::std::string::String` and passes the value to `rust::std::env::var`; the projected `parse` has no Rust symbol (E0599), so the program never compiled and cannot run. The interop-clause checker facts are keep-class in the `rust_*` typechecker modules (#1337). | codegen, parser | - |
| `test_rfc041_capability_bounds_full_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_rust_capability_bounds.incn` | - | codegen, parser | twinned by the RFC 041 program with every `std.rust` marker: Send, Sync and Static over ordinary values, a Static-bounded identity, and Fn, FnMut and FnOnce each accepting a named function (#1716: the callable markers once emitted `Fn<i64>`, which rustc refuses). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc041_structural_coercion_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/structural_coercion.incn` | - | codegen, parser | twinned by the RFC 041 program printing the values passed through. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rfc041_rust_coercions_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_duration_coercion.incn`: an f32 literal coerces into an imported Rust Duration constructor and reads back as `1.5` seconds. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). Same program shape as `behavior_candidates/cli/rust_f32_argument_coercion.incn` for the CLI's coercion smoke test. |
| `test_rfc041_emit_rust_path_type_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_path_type_return.incn`: a function returning an imported Rust HashMap keyed by Rust String builds an empty map, printing `0`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). |
| `test_rfc041_emit_static_bound_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_rust_capability_bounds.incn` | - | codegen, parser | twinned by the RFC 041 program with every `std.rust` marker: Send, Sync and Static over ordinary values, a Static-bounded identity, and Fn, FnMut and FnOnce each accepting a named function (#1716: the callable markers once emitted `Fn<i64>`, which rustc refuses). The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_titlecase_var_not_type_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/titlecase_var_not_type.incn`: a TitleCase variable holding an imported Rust value is a value, so a method call on it renders it (a Uuid's length, `36`). waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The value-vs-path call spelling assertion dies with #654. |
| `test_constructor_field_defaults_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/constructor_field_defaults.incn` | - | codegen, parser | twinned by the program printing defaulted and overridden fields. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_newtype_checked_construction_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_checked_construction.incn` | - | codegen, parser | twinned by the RFC 017 program printing every construction and the non-canonical helper's accept and refuse; the `.expect("validated newtype construction failed")` assertions die with #654. |
| `test_newtype_implicit_coercion_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_implicit_coercion.incn` | - | codegen, generated_text, parser | twinned by the program run to completion; the `RetryAttempts(` shape assertion dies with #654. |
| `test_validated_newtype_json_deserialization_uses_canonical_hook` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_json_validation.incn` | - | codegen, generated_text, parser | twinned by the program deserializing a valid and an over-long id and a generic newtype; the `impl<'de> serde::Deserialize<'de>` shape assertions die with #654. |
| `test_constrained_newtype_generated_validation_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/constrained_newtype_validation.incn` | - | codegen, parser | twinned by the program printing two accepted values and aborting on a violating one (exit 101); the `raise_constraint_error` shape assertions die with #654. |
| `test_user_defined_panic_function_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/panic_function_name.incn` | - | codegen, parser | twinned by the program run to completion; the `panic!` macro-avoidance assertion is generated shape. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_newtype_builder_methods_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_builder_methods.incn` | - | codegen, parser | twinned by one program for the builder methods and the user-provided `__into_response__` override. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_newtype_with_override_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_builder_methods.incn` | - | codegen, parser | twinned by one program for the builder methods and the user-provided `__into_response__` override. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_newtype_axum_response_codegen` | retire | `dies` | the program's own docstring says its generated Rust may not compile: a newtype over `rust::axum::response::Response` with a `pass`-bodied placeholder constructor; the assertion is the generated `impl axum::response::IntoResponse` delegation, which dies with #654. | codegen, parser | - |
| `test_newtype_generic_json_codegen` | retire | `dies` | a generic newtype over `rust::axum::Json[T]` with no runnable use (main prints a marker only, and the test's own docstring calls it a codegen test); the assertion is the generated trait-delegation shape, which dies with #654. | codegen, parser | - |
| `test_newtype_generic_simple_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_generic.incn` | - | codegen, parser | twinned by one program for the generic newtype's method, static builder and tuple duplicate, whose pair is printed element by element (`42 42`): the retired program's `print(boxed.duplicate())` emitted `println!("{}", ...)` on a tuple, which never compiled. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_newtype_generic_builder_methods_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_generic.incn` | - | codegen, parser | twinned by one program for the generic newtype's method, static builder and tuple duplicate, whose pair is printed element by element (`42 42`): the retired program's `print(boxed.duplicate())` emitted `println!("{}", ...)` on a tuple, which never compiled. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_newtype_web_response_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/newtype_web_response.incn` | - | codegen, parser | twinned by the program run to completion with the json model printed; the retired test asserted only that the imports compiled. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rust_extern_delegation_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/rust_extern_delegation.incn` | - | codegen, parser | twinned by the program calling the stdlib extern through its source wrapper and aborting on a failing assertion; the projection and `incan_std_testing::fail_t` symbol assertions die with #654. |
| `rust_extern_method_projection_preserves_rust_abi_symbol` | retire | `dies` | the program's only behavior is `App.run`, bound through `rust.module("incan_std_web")`, which starts a web server; the assertion is that the projected wrapper delegates to the unprojected `incan_std_web::run` ABI symbol, a generated-symbol spelling that dies with #654. | codegen, parser | - |
| `test_std_testing_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_testing_assertions.incn` | - | codegen, parser | twinned by one program using the std.testing assertion surface, the assert statement forms of the assert_surface program, and every model-constructor comparison assertion form; the stdlib module snapshot and the `syn::parse_file` validity check are generated shape. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_std_async_task_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_tasks_and_time.incn` | - | codegen, parser | twinned by one async program using spawn, spawn_blocking, yield_now, sleep, sleep_ms, timeout and timeout_ms (the expired deadline prints `error.message()`, `operation timed out`) and timeout_join_ms, whose `TimedOut(handle)` outcome is awaited afterwards for the task's value (`still running`, `3`); `Duration.*` and `JoinHandle.abort()` have no fixture. The stdlib module snapshots and the async-main bootstrap shape die with #654. |
| `test_std_async_time_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_tasks_and_time.incn` | - | codegen, parser | twinned by one async program using spawn, spawn_blocking, yield_now, sleep, sleep_ms, timeout and timeout_ms (the expired deadline prints `error.message()`, `operation timed out`) and timeout_join_ms, whose `TimedOut(handle)` outcome is awaited afterwards for the task's value (`still running`, `3`); `Duration.*` and `JoinHandle.abort()` have no fixture. The stdlib module snapshots and the async-main bootstrap shape die with #654. |
| `test_std_async_channel_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_channels_and_sync.incn` | - | codegen, parser | twinned by one async program using a bounded channel and a Mutex; the stdlib module snapshots die with #654. |
| `test_std_async_sync_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_channels_and_sync.incn` | - | codegen, parser | twinned by one async program using a bounded channel and a Mutex; the stdlib module snapshots die with #654. |
| `test_std_async_race_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_race.incn` | - | codegen, parser | twinned by one async program using race/arm and the race-for expression; the stdlib module snapshot and the race-for lowering snapshot die with #654. |
| `test_race_for_expression_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_async_race.incn` | - | codegen, parser | twinned by one async program using race/arm and the race-for expression; the stdlib module snapshot and the race-for lowering snapshot die with #654. |
| `test_awaitable_bound_lowers_to_runtime_trait_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/awaitable_bound_join_handle.incn` | - | codegen, parser | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `test_std_derives_comparison_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_derives_surface.incn` | - | codegen, parser | twinned by one program deriving Eq, Ord, Hash, Clone, Copy, Default and Debug on a model and observing `==`, `!=`, `<`, `>`, `<=`, `>=` (`true false`, `true true`, `false true false`), Dict keying, `{:?}` rendering, `default()` and a copy; the stdlib module snapshot dies with #654. |
| `test_std_derives_copying_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_derives_surface.incn` | - | codegen, parser | twinned by one program deriving Eq, Ord, Hash, Clone, Copy, Default and Debug on a model and observing `==`, `!=`, `<`, `>`, `<=`, `>=` (`true false`, `true true`, `false true false`), Dict keying, `{:?}` rendering, `default()` and a copy; the stdlib module snapshot dies with #654. |
| `test_std_derives_string_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_derives_surface.incn` | - | codegen, parser | twinned by one program deriving Eq, Ord, Hash, Clone, Copy, Default and Debug on a model and observing `==`, `!=`, `<`, `>`, `<=`, `>=` (`true false`, `true true`, `false true false`), Dict keying, `{:?}` rendering, `default()` and a copy; the stdlib module snapshot dies with #654. |
| `test_std_derives_collection_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_derives_collection_adapters.incn` | - | codegen, generated_text, parser | twinned by `std_derives_collection_adapters.incn`: a model adopting the stdlib `Iterator` drives a for loop (`3`) and a model adopting the stdlib `FallibleIterator` drives `.map(Double())`, `.filter(Above(threshold=2))` and `.collect()` with `@derive(Clone)` callable models (`2 4 6`), which is what the `StoredMapFn: Clone + Callable1<T, Output>` and `filter<Predicate: Clone + Callable1<Output, bool>>` bound assertions were about; those spellings and the module snapshot die with #654. The same adopter in a module that also imports `Clone` from `std.derives.copying` is #1727 (fixed), covered by `snapshots_stdlib/std_derives_collection_adapters_with_imported_clone.incn`. |
| `test_std_serde_json_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_serde_json_import.incn` | - | codegen, parser | twinned by the RFC 024 program printing and reading back JSON; the stdlib module snapshot and the `stringify_or_raise`/`parse_or_error` helper assertions die with #654. |
| `test_std_serde_json_import_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_serde_json_import.incn` | - | codegen, parser | twinned by the RFC 024 program printing and reading back JSON; the stdlib module snapshot and the `stringify_or_raise`/`parse_or_error` helper assertions die with #654. |
| `test_std_registry_import_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_functions.incn` | - | codegen, parser | twinned by one RFC 113 program describing a function with a summary descriptor and one with a type-token descriptor and printing the loaded entries; the registry lowering snapshots die with #654. |
| `test_std_registry_methods_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_methods.incn` | - | codegen, parser | twinned by the RFC 113 program printing the loaded method entry's qualified name; the registry lowering snapshot dies with #654. |
| `test_std_registry_subjects_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_subjects.incn` | - | codegen, parser | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `test_std_registry_type_token_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_functions.incn` | - | codegen, parser | twinned by one RFC 113 program describing a function with a summary descriptor and one with a type-token descriptor and printing the loaded entries; the registry lowering snapshots die with #654. |
| `test_std_graph_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_graph.incn` | - | codegen, parser | twinned by one RFC 047 program printing graph, DAG and multigraph query results; the stdlib module snapshot and the `__incan_new()` constructor assertions die with #654. |
| `test_std_compression_modules_compile_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_compression.incn` | - | codegen, parser | twinned by the std.compression surface program (the same program `valid/std_compression_surface.incn` runs under the CLI); the eleven module compilations are covered by its imports. |
| `test_std_graph_import_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_graph.incn` | - | codegen, generated_text, parser | twinned by one RFC 047 program printing graph, DAG and multigraph query results; the stdlib module snapshot and the `__incan_new()` constructor assertions die with #654. |
| `test_std_uuid_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_uuid.incn` | - | codegen, parser | twinned by the RFC 060 program run to completion with canonical strings printed; the `pub struct UUID(pub u128)` and `uuid::Uuid` absence assertions die with #654. |
| `test_std_uuid_import_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_uuid.incn` | - | codegen, parser | twinned by the RFC 060 program run to completion with canonical strings printed; the `pub struct UUID(pub u128)` and `uuid::Uuid` absence assertions die with #654. |
| `test_std_regex_import_constructor_hook_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_regex.incn` | - | codegen, parser | twinned by the program matching through a flagged constructor and refusing an invalid pattern; the `Regex::__incan_new(...)` hook-spelling assertions die with #654. |
| `test_std_serde_with_serialize_trait_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/serialize_trait_adoption.incn` | - | codegen, parser | twinned by one program printing to_json through adoption, through a derive and through a `json.Serialize` bound, beside an ordinary method, and decoding a `@derive(json)` model back through `from_json` (`{"value":3}` prints `3`); the impl-shape assertions die with #654. |
| `test_newtype_local_serde_named_traits_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/local_serde_named_marker_traits.incn` | - | codegen, parser | twinned by the #1431 program run to completion; the empty-impl and `serde::` absence assertions die with #654. |
| `test_facade_reexported_std_serde_json_traits_keep_their_protocol` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/facade_reexported_serde_traits` | - | codegen, parser | twinned by a module fixture with a `pub from std.serde.json import` facade; the forwarded-derive and `use crate::facade::` assertions die with #654. |
| `test_newtype_with_serialize_trait_forwards_rust_derive` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/newtype_with_serialize_trait.incn` | - | codegen, generated_text, parser | twinned by the program printing the newtype's JSON; the forwarded `#[derive(serde::Serialize)]` assertion dies with #654. |
| `test_with_serialize_keeps_ordinary_methods_inherent` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/serialize_trait_adoption.incn` | - | codegen, parser | twinned by one program printing to_json through adoption, through a derive and through a `json.Serialize` bound, beside an ordinary method, and decoding a `@derive(json)` model back through `from_json` (`{"value":3}` prints `3`); the inherent-vs-trait impl-shape assertions die with #654. |
| `test_qualified_source_trait_dispatch_does_not_double_borrow_self` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/qualified_json_trait_dispatch.incn` | - | codegen, parser | twinned by the program printing the encoded model; the `json::Serialize::to_json(self)` spelling assertion dies with #654. |
| `test_direct_json_trait_import_keeps_canonical_owner_across_modules_issue946` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/direct_json_trait_import_across_modules` | - | codegen, parser | twinned by a module fixture for #946; the canonical-owner path assertion dies with #654. |
| `test_rfc024_module_derive_json_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/serialize_trait_adoption.incn` | - | codegen, parser | twinned by one program printing to_json through adoption, through a derive and through a `json.Serialize` bound, beside an ordinary method, and decoding a `@derive(json)` model back through `from_json` (`{"value":3}` prints `3`). The `serde::Serialize,serde::Deserialize` forwarding the test asserted is observable as `to_json` and `from_json` both working on `Derived`; the `impl json::Serialize for Payload` impl-shape assertion and the snapshot die with #654. |
| `test_rfc024_partial_alias_derive_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/partial_alias_derive.incn` | - | codegen, parser | twinned by the RFC 024 program that imports `std.serde.json.Serialize` as `JsonSerialize` and uses the alias in `@derive` and as a bound, printing `{"value":1}` (#1712: the dispatch path once spelled the alias into the stdlib path). The retired assertion on the emitted `impl JsonSerialize for Payload` text dies with #654. |
| `test_rfc024_user_module_serde_format_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/user_module_derivables` | - | codegen, parser | twinned by a module fixture for both RFC 024 user-module derivables; the deduplicated `serde::Serialize` count and impl-shape assertions die with #654. |
| `test_rfc024_user_module_pure_incan_derivable_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/user_module_derivables` | - | codegen, parser | twinned by a module fixture for both RFC 024 user-module derivables; the retired assertion is `!contains("serde::Serialize")` (a pure-Incan derivable must not pull a serde derive in) plus the impl shape, generated text that dies with #654. |
| `test_multi_instantiation_trait_methods_codegen_trait_impls_only` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/multi_instantiation_trait_methods.incn` | - | codegen, parser | twinned by one program dispatching the model's and the enum's same-named methods by annotated result type; the `impl Convert<i64> for` and inherent-duplicate assertions die with #654. |
| `test_std_json_value_indexing_emits_checked_helpers` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/json_value_indexing.incn` | - | codegen, parser | twinned by the program indexing parsed objects and arrays; the `__getitem__` call-spelling assertions die with #654. |
| `test_issue815_generic_index_adoption_emits_trait_impl` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/index_trait_adoption.incn` | - | codegen, parser | twinned by one program for both #815 adoptions; the `impl Index<...> for` shape assertions die with #654. |
| `test_issue815_self_returning_index_adoption_uses_owner_type` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/index_trait_adoption.incn` | - | codegen, parser | twinned by one program for both #815 adoptions; the `impl Index<...> for` shape assertions die with #654. |
| `test_enum_multi_instantiation_trait_methods_codegen_trait_impls_only` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/multi_instantiation_trait_methods.incn` | - | codegen, parser | twinned by one program dispatching the model's and the enum's same-named methods by annotated result type; the `impl Convert<i64> for` and inherent-duplicate assertions die with #654. |
| `test_std_traits_ops_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_traits_surface.incn` | - | codegen, parser | twinned by one program adopting ops (Add, Neg, Sub: `30 -5 18`), Error, Index, Callable1 and convert traits. `std.traits.prelude` is not importable by a program: `from std.traits.prelude import Error` is refused with `Unknown stdlib module std.traits.prelude` (the hint's own known-modules list names it) and the `from std.traits.prelude import *` form documented in `workspaces/docs-site/docs/language/reference/stdlib/traits.md` is a parse error, so the prelude's compilation is covered by the traits it re-exports and the docs claim is wrong (#1728). The stdlib module snapshots die with #654. |
| `test_std_traits_error_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_traits_surface.incn` | - | codegen, parser | twinned by one program adopting ops (Add, Neg, Sub: `30 -5 18`), Error, Index, Callable1 and convert traits. `std.traits.prelude` is not importable by a program: `from std.traits.prelude import Error` is refused with `Unknown stdlib module std.traits.prelude` (the hint's own known-modules list names it) and the `from std.traits.prelude import *` form documented in `workspaces/docs-site/docs/language/reference/stdlib/traits.md` is a parse error, so the prelude's compilation is covered by the traits it re-exports and the docs claim is wrong (#1728). The stdlib module snapshots die with #654. |
| `test_std_traits_indexing_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_traits_surface.incn` | - | codegen, parser | twinned by one program adopting ops (Add, Neg, Sub: `30 -5 18`), Error, Index, Callable1 and convert traits. `std.traits.prelude` is not importable by a program: `from std.traits.prelude import Error` is refused with `Unknown stdlib module std.traits.prelude` (the hint's own known-modules list names it) and the `from std.traits.prelude import *` form documented in `workspaces/docs-site/docs/language/reference/stdlib/traits.md` is a parse error, so the prelude's compilation is covered by the traits it re-exports and the docs claim is wrong (#1728). The stdlib module snapshots die with #654. |
| `test_std_traits_callable_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_traits_surface.incn` | - | codegen, parser | twinned by one program adopting ops (Add, Neg, Sub: `30 -5 18`), Error, Index, Callable1 and convert traits. `std.traits.prelude` is not importable by a program: `from std.traits.prelude import Error` is refused with `Unknown stdlib module std.traits.prelude` (the hint's own known-modules list names it) and the `from std.traits.prelude import *` form documented in `workspaces/docs-site/docs/language/reference/stdlib/traits.md` is a parse error, so the prelude's compilation is covered by the traits it re-exports and the docs claim is wrong (#1728). The stdlib module snapshots die with #654. |
| `test_std_traits_prelude_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_traits_surface.incn` | - | codegen, parser | twinned by one program adopting ops (Add, Neg, Sub: `30 -5 18`), Error, Index, Callable1 and convert traits. `std.traits.prelude` is not importable by a program: `from std.traits.prelude import Error` is refused with `Unknown stdlib module std.traits.prelude` (the hint's own known-modules list names it) and the `from std.traits.prelude import *` form documented in `workspaces/docs-site/docs/language/reference/stdlib/traits.md` is a parse error, so the prelude's compilation is covered by the traits it re-exports and the docs claim is wrong (#1728). The stdlib module snapshots die with #654. |
| `test_std_traits_convert_compiled_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_traits_surface.incn` | - | codegen, parser | twinned by one program adopting ops (Add, Neg, Sub: `30 -5 18`), Error, Index, Callable1 and convert traits. `std.traits.prelude` is not importable by a program: `from std.traits.prelude import Error` is refused with `Unknown stdlib module std.traits.prelude` (the hint's own known-modules list names it) and the `from std.traits.prelude import *` form documented in `workspaces/docs-site/docs/language/reference/stdlib/traits.md` is a parse error, so the prelude's compilation is covered by the traits it re-exports and the docs claim is wrong (#1728). The stdlib module snapshots die with #654. |
| `test_std_traits_convert_usage_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_traits_convert_usage.incn` | - | codegen, parser | twinned by the program printing From and TryFrom results including a refusal. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_assert_surface_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_testing_assertions.incn` | - | codegen, parser | twinned by one program using the std.testing assertion surface, the assert statement forms of the assert_surface program, and every model-constructor comparison assertion form; the stdlib module snapshot and the `syn::parse_file` validity check are generated shape. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `model_constructor_comparison_assertion_forms_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_testing_assertions.incn` | - | codegen, parser | twinned by one program using the std.testing assertion surface, the assert statement forms of the assert_surface program, and every model-constructor comparison assertion form; the stdlib module snapshot and the `syn::parse_file` validity check are generated shape. |
| `test_rust_allow_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/rust_allow_lint_suppressions.incn` | - | codegen, parser | twinned by the RFC 057 program calling every suppressed declaration; the `#[allow(...)]` attribute snapshot dies with #654. |
| `test_trait_bound_inference_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/trait_bound_inference.incn` | - | codegen, parser | twinned by one program calling the inferred-bound functions for int, float and str. The inferred `/` and `%` cases are not in the twin: the build lowers them through `py_div`/`py_mod`, whose sealed numeric bounds a type parameter cannot satisfy, so they never compiled. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_trait_bound_explicit_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/trait_bound_explicit.incn` | - | codegen, parser | twinned by the RFC 023 program calling both explicitly bounded functions. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rust_supertrait_imported_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_supertrait_imported.incn`: a source trait with an imported Rust Display supertrait is adopted by a model with `__str__`, and the trait's default method renders through it, `release`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The absolute-path supertrait spelling assertion dies with #654. |
| `test_rust_trait_import_without_metadata_codegen` | retire | `dies` | the program is accepted only in the test's metadata-free codegen context: under a baked project the checker resolves `value.borrow()` through Rust inspection and refuses the `&PathBuf` return (a type mismatch against the trait's own type), so there is no runnable form. The assertion is that the `use ::std::borrow::Borrow;` line survives pruning, generated shape that dies with #654. | codegen, generated_text, parser | - |
| `test_rust_trait_import_without_metadata_alias_codegen` | retire | `dies` | the program is accepted only in the test's metadata-free codegen context: under a baked project the checker resolves `value.borrow()` through Rust inspection and refuses the `&PathBuf` return, so there is no runnable form. The assertion is that the aliased `use` survives pruning, generated shape that dies with #654. | codegen, generated_text, parser | - |
| `test_rust_trait_import_without_metadata_unused_codegen` | retire | - | - | codegen, parser | open: candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/snapshots/rust_trait_import_without_metadata_unused.incn`: a Rust extension trait imported without inspected metadata and never called leaves the program runnable, printing `kept`. waits for a Cargo-free bake under the suite: a `rust::` program needs source-current project inspection authority, which only an explicit bake of its project records, and that bake runs Rust inspection through Cargo (`no source-current project inspection authority` under the suite otherwise); the candidate passes standalone (`incan oven bake` + `incan run`). The import-pruning assertion is generated shape and dies with #654. |
| `test_trait_bound_default_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/trait_bound_default.incn` | - | codegen, generated_text, parser | twinned by the #1374 program printing the defaulted fields; the `T: Default` bound-spelling assertion dies with #654. |
| `test_ordinal_key_builtin_impls_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/ordinal_key_builtin_impls.incn` | - | codegen, parser | twinned by the program printing the ordinal bytes lengths and encodings for every key type; the `impl OrdinalKey for` shape assertions die with #654. |
| `test_ordinal_map_str_fast_lookup_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/ordinal_map_str_lookup.incn` | - | codegen, parser | twinned by the program run to completion with a refused key; the `__incan_require_str_fast` fast-path assertions die with #654. |
| `test_imported_stdlib_value_fragment_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/imported_stdlib_value_fragment.incn` | - | codegen, parser | twinned by the program printing the byte length; the `::ordinal_key_byte(7)` fragment assertion dies with #654. |
| `test_trait_bound_inference_more_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/trait_bound_inference.incn` | - | codegen, parser | twinned by the merged inference program (f-string, dict key, +, -, *, clone, transitive and combined bounds). The inferred `/` and `%` cases are not in the twin: the build lowers them through `py_div`/`py_mod`, whose sealed numeric bounds a type parameter cannot satisfy, so they never compiled. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_loop_expressions_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_values_and_control_flow/loop_expressions.incn` | - | codegen, parser | twinned by the program printing the loop-expression values. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_generic_bounds_return_type_codegen` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/generic_bounds_return_type.incn` | - | codegen, parser | twinned by the #196 program upcasting a bounded model through both functions. The retired snapshot (the program's whole generated Rust) dies with #654. |
| `test_rust_generic_bounds` | retire | `loaves/toolchain/incan-cli/tests/integration_tests.rs::imported_rust_generic_bounds_remain_native_obligations` | - | codegen, snapshot, parser | twinned by the CLI test that writes the same `valid/rust_generic_bounds.incn` program into a project with a serde dependency, bakes it and runs it (re-point); the snapshot of its Rust dies with #654. |

Per-test overrides in `loaves/compiler/incan_emit/tests/empty_list_comparison_operand_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `lowered_empty_list_operand_carries_the_element_type` | retire | - | - | checker, parser, legacy_ir | legacy IR shape |
| `generated_rust_names_the_element_type_on_the_empty_operand` | retire | `loaves/toolchain/incan-cli/tests/cli_language_regression_tests.rs::empty_list_equality_operands_build_with_json_cohort_issue1476` | - | codegen, generated_text, parser | asserts generated Rust text |

Per-test overrides in `loaves/compiler/incan_emit/tests/implicit_borrowing_codegen_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `private_enum_classifier_infers_shared_parameter` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Holder.observe` calls `is_table(self.item)`; the counter stays at 0, which is the only observable of `!self.item.clone()`). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `private_classifier_borrow_flows_through_a_helper` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`inspect` -> `classify` -> `is_table` with the counter at 0). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `escaping_classifier_keeps_its_callable_abi` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`callback()` returns `is_table_callback`, a classifier that only escapes, and the returned callable is called on a fresh tree with no copy counted; the classifier the direct calls lend to is a separate function, since one function cannot keep both ABIs). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `unknown_rust_receiver_does_not_authorize_borrowing` | retire | `dies` | the program imports `rust::demo`, a crate that exists nowhere; the test proves the inference declines when the receiver's contract is unknown, which no runnable program can show (a runnable program has the contract). | codegen, parser | - |
| `overlapping_call_arguments_keep_the_owned_helper_abi` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`observe_pair` calls `classify_pair(item, item)`; overlapping arguments keep the helper's owned parameters, so the first `item`, not at its last use, is copied whole: three counts, one tree); the `rust::demo` crate of the retired test is the fixture's counting crate here. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `escaping_cursor_keeps_its_owned_value` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`select_owned` returns its cursor; the returned cursor is an escape, so the parameter and the cursor stay owned and the tree moves through with no copy counted); the `rust::demo` crate of the retired test is the fixture's counting crate here. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `mutating_pattern_payload_keeps_the_owned_parameter` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`clear` calls the crate's `clear(&mut self)` on its cursor, then reports zero children with no copy counted; a mutating receiver contract keeps the parameter owned). The retired test mutates through a match payload whose Rust map type has no Incan spelling, so the mutation is a `&mut self` method on the cursor instead; the `rust::demo` crate of the retired test is the fixture's counting crate here. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `crate_visible_helpers_keep_owned_entry_points_and_borrow_local_calls` | retire | - | - | codegen, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Holder.observe` over a private helper, counter at 0); the `__incan_std` module context and the shared-function name it asserts are Rust spellings that die with #654. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `proven_receiver_child_infers_a_borrowed_helper_and_cursor` | retire | - | - | codegen, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`observe`/`traverse` walk `get` children through a cursor with the counter at 0, the crate's real receiver contracts standing in for the test's synthetic ones). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `inferred_method_helper_preserves_owned_public_abi` | retire | - | - | codegen, generated_text, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Reader.select`/`read`: a method walking a parameter through a cursor, one counted copy for its explicit clone). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `inline_tree_cursor_borrows_a_field_without_annotations` | retire | - | - | codegen, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Holder.walk` starts its cursor from `self.item`, counter at 0). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `later_owner_mutation_preserves_the_cursor_snapshot` | retire | - | - | codegen, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Holder.snapshot` keeps the tree it read before replacing the field; the three counted copies are the whole tree the read materializes). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `unrelated_return_lifetime_does_not_authorize_cursor_assignment` | retire | - | - | codegen, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`take_child` assigns the owned child that `child()` returns; the two counted copies are that child's subtree, the cursor stays owned). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `inferred_traversal_compiles_and_runs_without_any_tree_clones` | retire | - | - | codegen, checker, parser, legacy_ir | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (the retired test's program and clone-counting crate as a project fixture with a `[rust-dependencies]` path crate; its consumer's counts, `observe` 0, `Reader.read` 1, `Holder.snapshot` +3, are the first three counts of the fixture, and the fixture's README derives the rest). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `explicit_reference_local_borrows_a_field_without_cloning` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Holder.view`: `current: &Item = self.item`, counter at 0). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `owned_reassignment_materializes_a_borrowed_generic_and_infers_clone_bound` | retire | `dies` | `replace[T](first: T, second: &T)` cannot be called from Incan source: a value where `&T` is expected is refused (`expected '&T', found 'str'`), and values typed with `&` exist only for Rust types, so the body is unreachable; its assertions (`<T:Clone,`, `second.clone()`) are Rust spellings. | codegen, parser | - |
| `borrowed_reassignment_preserves_reference_shape_without_clone_bound` | retire | `dies` | `replace[T](first: &T, second: &T)` cannot be called from Incan source (a value where `&T` is expected is refused), so the body is unreachable; its assertions (`current=second;`, no `T:Clone`) are Rust spellings. | codegen, parser | - |
| `owned_reassignment_preserves_a_field_source` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/field_assigned_into_a_reassigned_local.incn` | - | codegen, parser | twinned by a program whose method overwrites a local with a str field of self and returns it while the field stays readable. The `current=self.name.clone()` spelling dies with #654; the program proves the field is returned and still readable. |
| `opaque_rust_child_reassignment_does_not_add_a_second_borrow` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`traverse_ref(item: &Item, ...)` reassigns its cursor to each `get` child, counter at 0). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `callback_in_a_trait_default_keeps_the_owned_abi` | retire | - | - | codegen, parser | candidate `loaves/compiler/incan_test_support/fixtures/behavior_candidates/ownership/implicit_borrowing_clone_counter/` (`Factory.callback`, a trait default, returns `is_table_callback`; a model adopting the trait hands the callable a fresh tree with no copy counted); the `rust::demo` crate of the retired test is the fixture's counting crate here. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `comprehension_shadowing_does_not_rewrite_an_unrelated_binding` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/ownership_values_and_fields/comprehension_binding_shadowing_a_parameter.incn` | - | codegen, parser | twinned by a program whose comprehension binding is spelled like the enclosing parameter; the match that follows still sees the parameter. The `rust::demo` enum is a local enum here; the `!contains("item:&Item")` spelling dies with #654. |

Per-test overrides in `loaves/compiler/incan_emit/tests/nested_list_loop_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `nested_list_empty_holes_retain_checked_string_types` | retire | - | - | checker, parser, legacy_ir | legacy IR shape; the checked-type assertions (`List[List[str]]` on the empty holes) are the keep-class content the twin must carry. |
| `nested_list_loop_emits_owned_strings_without_caller_annotation` | retire | `loaves/toolchain/incan-cli/tests/cli_language_regression_tests.rs::nested_empty_first_list_runs_without_a_caller_annotation_issue1471` | - | codegen, parser | asserts generated Rust text |

### `loaves/compiler/incan_format` (191 tests in 9 files: keep 191)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/compiler/incan_format/src/config.rs` | 24 | 275 | 206 | keep | - | - | - | #1561 | - | formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/formatter/tests.rs` | 11 | 327 | 327 | keep | - | - | - | #1561 | parser 7, formatter 11 | formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/tests/comments_and_blank_lines.rs` | 20 | 488 | 488 | keep | - | - | - | #1561 | formatter 18 | split of incan_format/src/lib.rs; formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/tests/declarations.rs` | 28 | 755 | 755 | keep | - | - | - | #1561 | formatter 28 | split of incan_format/src/lib.rs; formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/tests/entry_points_imports_and_spacing.rs` | 27 | 455 | 455 | keep | - | - | - | #1561 | checker 1, parser 1, formatter 21 | split of incan_format/src/lib.rs; formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/tests/expressions_and_calls.rs` | 23 | 631 | 631 | keep | - | - | - | #1561 | parser 1, formatter 23 | split of incan_format/src/lib.rs; formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/tests/statements_and_patterns.rs` | 14 | 281 | 281 | keep | - | - | - | #1561 | formatter 14 | split of incan_format/src/lib.rs; formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/src/writer.rs` | 37 | 565 | 389 | keep | - | - | - | #1561 | - | formatter; no emit/driver dependency. Reviewed at crate level. |
| `loaves/compiler/incan_format/tests/property_tests.rs` | 7 | 411 | 385 | keep | - | - | - | #1561 | parser 4, formatter 6 | formatter; no emit/driver dependency. Reviewed at crate level. |

### `loaves/compiler/incan_frontend` (1778 tests in 86 files: keep 1778)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/compiler/incan_frontend/src/api_metadata.rs` | 17 | 3768 | 944 | keep | - | - | - | #1561 | checker 17, parser 17 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/body_ir.rs` | 2 | 1094 | 49 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/async_and_race.rs` | 14 | 390 | 390 | keep | - | - | - | #1561 | replacement 1 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/calls_and_arguments.rs` | 37 | 996 | 996 | keep | - | - | - | #1561 | parser 1 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/closures_comprehensions_and_generators.rs` | 28 | 797 | 797 | keep | - | - | - | #1561 | parser 1 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/control_flow_and_loops.rs` | 21 | 670 | 670 | keep | - | - | - | #1561 | replacement 1 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/for_patterns.rs` | 16 | 482 | 482 | keep | - | - | - | #1561 | replacement 3, checker 6, parser 7 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/identities_and_imports.rs` | 17 | 1027 | 1027 | keep | - | - | - | #1561 | replacement 12, checker 12, parser 12 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/input_contract_and_refusals.rs` | 7 | 298 | 298 | keep | - | - | - | #1561 | replacement 5, checker 5, parser 5 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/methods_and_defaults.rs` | 31 | 1005 | 1005 | keep | - | - | - | #1561 | replacement 1, checker 1, parser 1 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/operators_literals_and_assignment.rs` | 38 | 859 | 859 | keep | - | - | - | #1561 | - | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/patterns_and_assertions.rs` | 24 | 803 | 803 | keep | - | - | - | #1561 | replacement 7, checker 2, parser 2 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/body_ir/tests/provider_plans.rs` | 14 | 627 | 627 | keep | - | - | - | #1561 | replacement 13, checker 13, parser 13 | split of body_ir/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/compiler_stack.rs` | 3 | 113 | 41 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/contract_metadata.rs` | 5 | 517 | 89 | keep | - | - | - | #1561 | parser 2, formatter 3 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/hir.rs` | 7 | 540 | 328 | keep | - | - | - | #1561 | checker 7, parser 7 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/library_exports.rs` | 6 | 2120 | 179 | keep | - | - | - | #1561 | checker 3 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/library_manifest/artifact.rs` | 16 | 1677 | 937 | keep | - | - | - | #1561 | checker 5 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/library_manifest/published_layout.rs` | 2 | 287 | 47 | keep | - | - | - | #1561 | checker 2 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/library_manifest/tests/export_round_trips.rs` | 23 | 1170 | 1170 | keep | - | - | - | #1561 | checker 21 | split of library_manifest/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/library_manifest/tests/formats_and_metadata.rs` | 18 | 572 | 572 | keep | - | - | - | #1561 | checker 17 | split of library_manifest/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/library_manifest/tests/identity_graph.rs` | 7 | 926 | 926 | keep | - | - | - | #1561 | checker 7 | split of library_manifest/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/library_manifest/tests/identity_validation.rs` | 9 | 1084 | 1084 | keep | - | - | - | #1561 | checker 9, parser 1 | split of library_manifest/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/library_manifest/tests/vocab_and_scoped_surfaces.rs` | 19 | 733 | 733 | keep | - | - | - | #1561 | checker 19 | split of library_manifest/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/library_manifest/type_projection.rs` | 1 | 502 | 69 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/library_manifest_index.rs` | 10 | 1387 | 468 | keep | - | - | - | #1561 | checker 8 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/module.rs` | 33 | 1659 | 913 | keep | - | - | - | #1561 | parser 5 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/provider/features.rs` | 11 | 1919 | 578 | keep | - | - | - | #1561 | checker 10 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/provider/plan.rs` | 15 | 2791 | 697 | keep | - | - | - | #1561 | checker 13 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/provider/sdk.rs` | 13 | 1305 | 270 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/provider/stdlib_sources.rs` | 2 | 223 | 71 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/resolved_type_subst.rs` | 1 | 221 | 46 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/rust_type_display.rs` | 7 | 754 | 171 | keep | - | - | - | #1561 | parser 6 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/surface_semantics.rs` | 3 | 138 | 48 | keep | - | - | - | #1561 | parser 2 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/symbols.rs` | 16 | 3089 | 462 | keep | - | - | - | #1561 | checker 10 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/testing_markers.rs` | 7 | 871 | 164 | keep | - | - | - | #1561 | checker 1, parser 7 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/typechecker/check_expr/calls/rust_boundary.rs` | 43 | 2526 | 1425 | keep | - | - | - | #1561 | text 2, checker 43 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/typechecker/collect/stdlib_imports.rs` | 1 | 4834 | 32 | keep | - | - | - | #1561 | checker 1 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/typechecker/identity_surface_tests.rs` | 8 | 284 | 284 | keep | - | - | - | #1561 | checker 8, parser 8 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/typechecker/stdlib_loader.rs` | 31 | 3079 | 1113 | keep | - | - | - | #1561 | parser 30 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/async_and_iteration.rs` | 53 | 1005 | 1005 | keep | - | - | - | #1561 | checker 40 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/bounds_and_derives.rs` | 39 | 1165 | 1165 | keep | - | - | - | #1561 | checker 21, parser 11 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/calls_decorators_and_builtins.rs` | 52 | 1208 | 1208 | keep | - | - | - | #1561 | checker 28, parser 9 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/canonical_identity/declarations_and_scopes.rs` | 26 | 995 | 995 | keep | - | - | - | #1561 | checker 5 | split of typechecker/canonical_identity_tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/canonical_identity/duplicates_and_collisions.rs` | 12 | 605 | 605 | keep | - | - | - | #1561 | checker 11 | split of typechecker/canonical_identity_tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/canonical_identity/imports_and_dependencies.rs` | 21 | 921 | 921 | keep | - | - | - | #1561 | checker 20 | split of typechecker/canonical_identity_tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/canonical_identity/references_calls_and_patterns.rs` | 18 | 758 | 758 | keep | - | - | - | #1561 | checker 9 | split of typechecker/canonical_identity_tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/capabilities.rs` | 20 | 553 | 553 | keep | - | - | - | #1561 | checker 20, parser 9 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/capability_requirements.rs` | 18 | 898 | 898 | keep | - | - | - | #1561 | checker 18, parser 3 | typechecker refusals of types lacking a capability the program needs (#1754 automatic derives, #1758 set elements and dict keys, #1772 a function value passed as a task); no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/checked_facts_and_registries.rs` | 21 | 986 | 986 | keep | - | - | - | #1561 | checker 20, parser 2 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/collections_strings_and_bytes.rs` | 48 | 1045 | 1045 | keep | - | - | - | #1561 | checker 43, parser 4 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/extern_and_c_bindings.rs` | 22 | 825 | 825 | keep | - | - | - | #1561 | checker 16, parser 3 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/fields_and_members.rs` | 56 | 1198 | 1198 | keep | - | - | - | #1561 | checker 51, parser 5 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/generics_and_type_tokens.rs` | 38 | 1047 | 1047 | keep | - | - | - | #1561 | checker 23, parser 9 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/imports_and_stdlib_modules.rs` | 42 | 1155 | 1155 | keep | - | - | - | #1561 | checker 31, parser 7 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/method_decorator_receivers.rs` | 14 | 786 | 786 | keep | - | - | - | #1561 | checker 14, parser 7 | typechecker unit tests for #1790 method-decorator receivers: the receiver spelled like the method, the `mut` marker as part of the function type, the `INCAN-T0110` refusal of `&Owner` / `&mut Owner` and the `INCAN-T0116` chain rules; typechecker only, no emit/driver dependency. `receiver_written_with_ampersand_is_refused` is the proof of `INCAN-T0110`: no behavior fixture spells a receiver with `&`. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/models_enums_and_newtypes.rs` | 51 | 963 | 963 | keep | - | - | - | #1561 | checker 37, parser 2 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/narrowing_and_matching.rs` | 57 | 1206 | 1206 | keep | - | - | - | #1561 | checker 57, parser 3 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/numerics_const_and_static.rs` | 64 | 1042 | 1042 | keep | - | - | - | #1561 | checker 63 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/partials_and_callable_aliases.rs` | 33 | 1117 | 1117 | keep | - | - | - | #1561 | checker 33, parser 12 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/pub_imports_namespaces_and_fields.rs` | 21 | 821 | 821 | keep | - | - | - | #1561 | checker 21, parser 16 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/pub_imports_symbols_and_identity.rs` | 30 | 1041 | 1041 | keep | - | - | - | #1561 | checker 29, parser 5 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/pub_imports_trait_adoptions.rs` | 8 | 710 | 710 | keep | - | - | - | #1561 | checker 8, parser 2 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_constructors_and_fields.rs` | 15 | 974 | 974 | keep | - | - | - | #1561 | checker 14, parser 11 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_generics_and_traits.rs` | 22 | 1375 | 1375 | keep | - | - | - | #1561 | checker 21, parser 12 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_imports_and_types.rs` | 58 | 1277 | 1277 | keep | - | - | - | #1561 | checker 44, parser 5 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_metadata_and_methods.rs` | 27 | 1301 | 1301 | keep | - | - | - | #1561 | checker 23, parser 11 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_supertraits.rs` | 7 | 164 | 164 | keep | - | - | - | #1561 | checker 5 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_trait_import_candidates.rs` | 6 | 155 | 155 | keep | - | - | - | #1561 | checker 6, parser 6 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/rust_trait_qualified_calls.rs` | 9 | 325 | 325 | keep | - | - | - | #1561 | checker 8, parser 7 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. asserts checker facts about `rust::` imports; moves with #1337's interop spec in slice 7, not with the route. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/statements_and_bindings.rs` | 69 | 1175 | 1175 | keep | - | - | - | #1561 | checker 66, parser 1 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/stdlib_surfaces.rs` | 50 | 1439 | 1439 | keep | - | - | - | #1561 | checker 35, parser 12 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/trait_instantiation_and_operators.rs` | 42 | 1363 | 1363 | keep | - | - | - | #1561 | checker 41, parser 11 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/tests/traits.rs` | 48 | 1137 | 1137 | keep | - | - | - | #1561 | checker 44, parser 22 | split of typechecker/tests.rs; typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/typechecker/validate_rust_module.rs` | 2 | 270 | 30 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/typechecker/web_routes.rs` | 2 | 340 | 37 | keep | - | - | - | #1561 | - | typechecker: route-handler signature rules (#1721, #1722); the inline tests cover the path-capture reader and the shape combinator, the checker rows live in loaves/compiler/incan_frontend/src/typechecker/tests/stdlib_surfaces.rs. No emit/driver dependency. |
| `loaves/compiler/incan_frontend/src/vocab_ast_bridge.rs` | 12 | 1896 | 491 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/vocab_desugar_pass/helper_bindings.rs` | 15 | 829 | 455 | keep | - | - | - | #1561 | checker 11 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/src/vocab_desugar_pass/runtime.rs` | 4 | 860 | 78 | keep | - | - | - | #1561 | - | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/checked_string_helper_identity_tests.rs` | 5 | 389 | 389 | keep | - | - | - | #1561 | replacement 4, checker 5, parser 5 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/checked_string_len_identity_tests.rs` | 5 | 158 | 158 | keep | - | - | - | #1561 | replacement 4, checker 5, parser 5 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/declaration_identity_corpus.rs` | 3 | 348 | 348 | keep | - | - | - | #1561 | replacement 2, checker 3, parser 3 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/semantic_core_parity.rs` | 6 | 125 | 125 | keep | - | - | - | #1561 | checker 2, parser 2 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/semantic_core_parity_strings.rs` | 10 | 203 | 203 | keep | - | - | - | #1561 | checker 2, parser 2 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/semantic_digest_invariants.rs` | 9 | 293 | 293 | keep | - | - | - | #1561 | replacement 9, checker 9, parser 9 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |
| `loaves/compiler/incan_frontend/tests/stdlib_module_trait_tests.rs` | 5 | 132 | 132 | keep | - | - | - | #1561 | checker 5, parser 5 | typechecker, Body IR, library manifests, provider plans; no emit/driver dependency. Reviewed at crate level; generated-text hits are Incan source or diagnostics. |

### `loaves/compiler/incan_ir` (176 tests in 19 files: keep 1, retire 175)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/compiler/incan_ir/src/decl.rs` | 1 | 711 | 11 | retire (retire 1) | 0/1 | 1 | - | #1561 | - | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/expr.rs` | 3 | 1167 | 81 | retire (retire 3) | 3/3 | 0 | - | #1561 | legacy_ir 3 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lib.rs` | 5 | 793 | 136 | retire (retire 5) | 0/5 | 5 | - | #1561 | legacy_ir 5 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/decl/helpers.rs` | 4 | 1049 | 209 | retire (retire 4) | 4/4 | 0 | - | #1561 | legacy_ir 4 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/decl/imports.rs` | 2 | 442 | 144 | retire (retire 2) | 1/2 | 0 | - | #1561 | checker 2, parser 2, legacy_ir 2 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/decl/methods.rs` | 2 | 2320 | 164 | retire (retire 2) | 1/2 | 0 | - | #1561 | checker 2, parser 2, legacy_ir 2 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/decl/mod.rs` | 3 | 727 | 238 | retire (retire 3) | 3/3 | 0 | - | #1561 | checker 3, parser 3, legacy_ir 3 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/decl/traits.rs` | 3 | 279 | 45 | retire (retire 3) | 0/3 | 3 | - | #1561 | legacy_ir 3 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/expr/calls.rs` | 21 | 5700 | 1242 | retire (keep 1, retire 20) | 6/20 | 4 | - | #1561 | checker 10, parser 4, legacy_ir 19 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/expr/mod.rs` | 15 | 3180 | 351 | retire (retire 15) | 14/15 | 1 | - | #1561 | checker 3, legacy_ir 15 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/expr/patterns.rs` | 4 | 1199 | 276 | retire (retire 4) | 4/4 | 0 | - | #1561 | checker 4, parser 4, legacy_ir 4 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/mod.rs` | 33 | 5482 | 1359 | retire (retire 33) | 25/33 | 7 | - | #1561 | checker 28, parser 29, legacy_ir 33 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/tests/builtin_str_arguments.rs` | 1 | 94 | 94 | retire (retire 1) | 1/1 | 0 | - | #1561 | legacy_ir 1 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/tests/derive_vocabulary_imports.rs` | 1 | 131 | 131 | retire (retire 1) | 1/1 | 0 | - | #1561 | legacy_ir 1 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/tests/method_decorator_receivers.rs` | 7 | 380 | 380 | retire (retire 7) | 7/7 | 0 | - | #1561 | checker 1, parser 4, legacy_ir 4 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/tests/newtype_automatic_derives.rs` | 4 | 120 | 120 | retire (retire 4) | 3/4 | 1 | - | #1561 | legacy_ir 4 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, derive lists). Retire with `incan_ir` (#654); the typechecker's derive relation that decides a newtype's automatic derives stays covered by typechecker/tests/capability_requirements.rs (keep). |
| `loaves/compiler/incan_ir/src/lower/tests/unary_operand_grouping.rs` | 2 | 149 | 149 | retire (retire 2) | 1/2 | 0 | - | #1561 | legacy_ir 2 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/lower/types.rs` | 13 | 1905 | 343 | retire (retire 13) | 9/13 | 3 | - | #1561 | checker 1, legacy_ir 13 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |
| `loaves/compiler/incan_ir/src/types.rs` | 52 | 1316 | 464 | retire (retire 52) | 2/52 | 49 | - | #1561 | legacy_ir 52 | the Rust-source backend's own lowering (`AstLowering`, `IrProgram`, `IrType`, Rust name spellings). Retire because its only consumers are `incan_emit` and the driver's `backend/ir` re-export, and `replacement/**` and `shadow/**` import nothing from it; flips to keep if #654 keeps the generated-project inspection path or the keep definition is read to include this lowering. Twins belong in Body IR (loaves/compiler/incan_frontend/src/body_ir/tests/). Reviewed at crate level. |

Per-test overrides in `loaves/compiler/incan_ir/src/decl.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `test_visibility_rust_keyword` | retire | `dies` | data-structure invariant of the retiring crate: the Rust keyword spelling of `Visibility` (`pub `, `pub(crate) `). | - | - |

Per-test overrides in `loaves/compiler/incan_ir/src/expr.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `iterator_method_kind_for_receiver_classifies_rfc088_surface` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/iterator_adapter_surface.incn` | - | legacy_ir | The `IteratorMethodKind` classifications die with the crate; the fixture drives every adapter and terminal of the table on `Iterator[int]` values from `list.iter()`. |
| `iterator_method_kind_for_receiver_does_not_capture_iterable_or_plain_structs` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/user_methods_named_like_builtins.incn` | - | legacy_ir | The `None` classifications die with the crate; the fixture's model defines `map` and the call reaches the model's method. An `Iterable[int]` value calling `map` is a checker refusal, not a lowering fact. |
| `result_method_kind_for_receiver_classifies_result_surface` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/result_method_surface.incn` | - | legacy_ir | The `ResultMethodId` classifications die with the crate; the fixture applies each of the eight methods to `Result[int, str]` values. `missing` is a checker refusal. |

Per-test overrides in `loaves/compiler/incan_ir/src/lib.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `checked_c_wrapper_names_do_not_merge_component_boundaries` | retire | `dies` | data-structure invariant of the retiring crate: the Rust and FFI wrapper name spellings of a checked C binding (`Fixture_` + `symbol` versus `Fixture` + `_symbol`). | legacy_ir | - |
| `function_registry_retains_source_identity_without_parsing_the_emitted_name` | retire | `dies` | data-structure invariant of the retiring crate: `FunctionRegistry` maps an opaque projection back to its source name. | legacy_ir | - |
| `function_registry_finds_exact_package_declaration_behind_encoded_key` | retire | `dies` | data-structure invariant of the retiring crate: `FunctionRegistry` finds a canonical identity by package, module path and declaration name behind an encoded key. | legacy_ir | - |
| `function_registry_finds_local_identity_by_retained_source_name` | retire | `dies` | data-structure invariant of the retiring crate: `FunctionRegistry` finds a canonical identity by retained source name. | legacy_ir | - |
| `merged_source_signature_keeps_mutable_parameter_abi` | retire | `dies` | data-structure invariant of the retiring crate: `FunctionSignature::merge_default_source` keeps the source declaration's `Mutable` parameter over the checked callable metadata's `Immutable` one, on hand-built signatures with no program. The source-call half of what it protects, a parameter declared `mut` in source staying mutable at a source-owned call, is carried by loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/receiver_aware_method_kinds_issue1668.incn (`grow(mut table)`, which inserts into its `mut` dict parameter and reads the insertion back). The other half is the `&mut Ui` callback parameter of an imported Rust trait implementation (`eframe::App`), where the inspected callable metadata spells the callback immutably and the source `mut` must win so the borrowed callback value is forwarded rather than cloned: untwinned, a `rust::` program over a Rust trait with a `&mut` callback that no fixture can carry (a Rust crate under `[rust-dependencies]`), recorded as a `rust::` gap that goes with #1337's interop spec, the way the `merge_test_runner_dependencies_*` rows record theirs. | legacy_ir | - |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/decl/helpers.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `adopted_serde_derives_require_canonical_std_serde_json_owner` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/adopted_serde_traits_by_owner` | - | legacy_ir | The forwarded `serde::Serialize`/`serde::Deserialize` derive spellings die with the crate; the fixture adopts `std.serde.json` traits module-qualified and by imported name (the JSON protocol answers) beside another module's trait spelled `Serialize` (its own `to_json` body answers). The negative halves, that a same-named local or third-party trait forwards no derive, are not observable: a superfluous derive changes no output. The `JsonDeserialize` alias spelling is #1712 and lands with its fix. |
| `source_callable_lowering_requires_canonical_std_trait_owner` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/callable_trait_local_versus_std` | - | legacy_ir | `IrTraitBoundOrigin` and the `crate::__incan_std::traits::callable::Callable1` path die with the crate; the fixture's `Callable1`, declared in a user module named `traits.callable` so that only the `std` root separates it from the canonical owner, is an ordinary trait dispatching its own method, and the std `Callable1` admits a closure and a plain function. |
| `aliased_derivable_trait_bound_lowers_to_the_rust_trait_of_its_derive_issue1727` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_derives_collection_adapters_with_imported_clone.incn` | - | legacy_ir | Asserts the recorded bound on the legacy IR (`Eq as Equality` and `Clone as Copyable` lower to the registry's Rust traits, `Iterator` under an alias keeps its generated path); retires with `incan_ir`. The behavior, an aliased `Eq` bound admitting a derived model, is carried by the named fixture (#1727). |
| `phantom_type_params_are_the_parameters_no_field_mentions` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/phantom_type_parameter_model_issue1370.incn` | - | legacy_ir | The phantom-parameter list dies with the crate; the fixture is the #1370 program (`Column[T]` with `T` in no field) constructed, multiplied and rendered, as `cli_issue1370_phantom_type_param_tests.rs` runs it through a build. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/decl/imports.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `renamed_reexport_hop_is_spelled_by_its_declaring_module_issue1710` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/reexport_chain` | - | checker, parser, legacy_ir | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/decl/methods.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `decorated_method_static_signature_keeps_first_projected_surface_parameter` | retire | - | - | checker, parser, legacy_ir | open: asserts the Rust spelling (`ProviderHandle<(&mut i64, &mut i64)>`) and `OwnedMutable` mutability of a decorated method's static signature under a hand-inserted mutable-reference projection over `rust::std::vec::Vec`. The behavior, a decorated method taking a `mut` Rust generic parameter and running, is the same program as `loaves/toolchain/incan-cli/tests/integration_tests.rs::decorated_method_explicit_mutable_rust_generic_keeps_static_and_wrapper_abi` (itself retire/open) and its candidate `decorated_rust_generic_mutable_reference_args.incn` under the `cli/` behavior-candidates group. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `same_spelled_static_and_instance_methods_both_lower_inherently_issue1709` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/same_spelling_type_owned_and_instance_methods.incn` | - | checker, parser, legacy_ir | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/decl/mod.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `explicit_registry_entry_follows_the_source_stdlib_projection_issue1713` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_subjects.incn` | - | checker, parser, legacy_ir | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `explicit_registry_entry_follows_a_declined_projection_issue1713` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_subjects.incn` | - | checker, parser, legacy_ir | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |
| `explicit_registry_entry_refuses_an_unknown_spelling_issue1713` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_registry_subjects.incn` | - | checker, parser, legacy_ir | twinned by the behavior fixture that retires it: the program prints what the lowering decision produces, so the generated-Rust assertion dies with #654 and the observable survives. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/decl/traits.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `sdk_provider_callable_module_retains_public_std_identity` | retire | `dies` | SDK-provider build-mode switch of the retiring crate (`set_sdk_provider_build` with the current module named `traits.callable`): the compiler's own build of the standard library, not a user program. Its ordinary-package half, that a user module named `traits.callable` acquires no callable behavior, is what loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/callable_trait_local_versus_std/ shows: its `traits/callable.incn` declares a `Callable1` that stays an ordinary trait. What the bridge gives a program, the std `Callable1` admitting a closure and a plain function, is shown by the same fixture. | legacy_ir | - |
| `loaf_source_callable_module_restores_internal_std_identity` | retire | `dies` | SDK-provider build-mode switch of the retiring crate (`set_sdk_provider_build` with the current module named `__incan_std.traits.callable`): the compiler's own build of the standard library, not a user program. What the bridge gives a program, the std `Callable1` admitting a closure and a plain function, is shown by loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/callable_trait_local_versus_std/. | legacy_ir | - |
| `normal_oven_internal_stdlib_callable_module_keeps_its_bridge` | retire | `dies` | build-mode switch of the retiring crate (`source_callable_trait` keyed on the current module being named `__incan_std.traits.callable`, the standard library's own source under a normal Oven bake); no user program names that module. What the bridge gives a program, the std `Callable1` admitting a closure and a plain function, is shown by loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/callable_trait_local_versus_std/. | legacy_ir | - |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/expr/calls.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `canonical_path_names_the_overload_a_call_selected` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/overloaded_stdlib_call_selection.incn` | - | - | The overload's `__incan_v1_...` path spelling dies with the crate; the fixture calls both arities of `std.environ.get_as` and shows each reached its own overload. |
| `canonical_path_keeps_its_spelling_when_no_overload_was_selected` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/overloaded_stdlib_call_selection.incn` | - | - | The source-shaped path spelling dies with the crate; the fixture's non-overloaded `get_optional` call reaches its only declaration. |
| `sdk_provider_physical_call_path_restores_std_identity_for_semantic_lookup` | retire | `dies` | SDK-provider build-mode switch of the retiring crate (`set_sdk_provider_build` plus a registry package identity and bootstrap namespace roots): rewrites a physical callee path back to its `std.` spelling while the compiler builds the standard library itself; no user program. | legacy_ir | - |
| `qualified_partial_expands_presets_without_a_callable_snapshot_issue948` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/parent_namespace_and_qualified_partial_issue948` | - | legacy_ir | The `modulelib::hyperquant::index::DEFAULT_SIZE` field-chain spelling dies with the crate; the fixture's provider declares the partial beside its const and the consumer's call shows the preset applied. The residual parameter is passed explicitly: called without it, the generated Rust drops the target's declaration default and fails to compile (E0061, #1760). |
| `source_partial_without_callable_snapshot_defers_to_canonical_defaults_issue701` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/source_partial_keeps_declaration_defaults_issue701.incn` | - | legacy_ir | The `partial_projection_call_args` `None` dies with the crate; the fixture's stored partial over a model constructor applies its preset and the declaration's own defaults fill the rest. |
| `method_type_arg_uses_unique_sdk_provider_nominal_path` | retire | `dies` | SDK-provider build-mode switch of the retiring crate (`set_sdk_provider_build` over an in-memory SDK manifest): the `crate::__incan_std::io::IoError` Rust path a method type argument takes while the compiler builds the standard library itself; no user program. | checker, legacy_ir | - |
| `imported_pub_callable_signature_uses_identity_graph_before_short_name_lookup` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/identity_graph_alias_signature` | - | checker, legacy_ir | The manifest-reading path dies with the crate; the fixture's provider publishes `helpers.cast` (str) as `safe_cast` beside a root-level `cast` (int), and the consumer's `safe_cast("text")` shows the alias took the str declaration. |
| `imported_pub_parent_namespace_routes_to_nested_checked_callable_issue948` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/parent_namespace_and_qualified_partial_issue948` | - | checker, legacy_ir | The `pub::modulelib::hyperquant::default_index` callee path dies with the crate; the fixture imports the directory namespace and calls the nested module's function and partial through it. |
| `compiled_provider_signature_uses_artifact_api_and_preserves_union_owner` | retire | `dies` | data-structure invariant of the retiring crate: reconstructs a callable signature and a module-qualified callee path from a hand-built compiled-provider manifest (no source, in-memory API metadata) through the retiring crate's own manifest readers; no program. | checker, legacy_ir | - |
| `pub_model_field_read_keeps_dependency_owned_union_carrier_issue1697` | keep | - | - | checker, legacy_ir | #1697: asserts the recorded fact, not Rust text: the declared type of a dependency-owned model field lowers to the provider-owned union carrier and retention puts it in the checked type's union position (directly and inside `List`). The replacement route inherits the fact (#1675); re-point the assertion to Body IR when the lowering crate retires. |
| `lower_rusttype_interop_adapter_uses_into_edge_for_rusttype_argument` | retire | - | - | legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rusttype_interop_edges.incn (a `rusttype` over `rust::std::string::String` passed where a `str` is expected adapts through its `into str via` edge). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_rusttype_interop_adapter_uses_from_edge_for_rusttype_target` | retire | - | - | legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rusttype_interop_edges.incn (a `str` passed where the `rusttype` is expected adapts through its `from str via` edge). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_method_call_wraps_args_with_rust_arg_coercion` | retire | - | - | legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_std_collections_argument_coercions.incn (a `str` literal passed to `String.push_str`, which takes a borrowed str). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_rust_named_field_constructor_wraps_fields_with_rust_arg_coercion` | retire | - | - | checker, legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_named_field_constructor.incn (a `rust::std::ops::Range` constructed by named fields, read back); the test's hand-inserted Default-fill fact has no std struct to observe it on. A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_rust_boundary_target_preserves_nested_borrowed_str_refs` | retire | `dies` | data-structure invariant of the retiring crate: the Rust spelling a `List[Ref[str]]` boundary target lowers to (`Vec<&str>`), computed on a hand-built resolved type with no program. It dies where its ten `rust::` siblings in this file stay open because no program reaches it: the nested borrow needs a Rust parameter typed `Vec<&str>` (or `&[&str]`), and `rust::std` has none (std takes string sequences generically, as `IntoIterator<Item: AsRef<OsStr>>` on `Command::args`, or as a slice receiver in `<[&str]>::join`), so the only carrier is a local Rust crate under `[rust-dependencies]`, which a fixture cannot carry (the limit the `cli_rust_interop_tests.rs` #896 row records). The siblings' programs import `rust::std` only and wait for the Cargo-free bake; this one goes with #1337's interop spec. | legacy_ir | - |
| `lower_method_call_threads_arg_shape_hint_from_typechecker` | retire | - | - | legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_std_collections_argument_coercions.incn (`HashMap.get` keeps its lookup argument's shape). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_method_call_applies_required_concrete_borrow_despite_arg_shape_hint` | retire | - | - | legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_std_collections_argument_coercions.incn (`Vec.append` takes its argument by mutable reference). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_rust_import_associated_method_keeps_type_like_receiver` | retire | - | - | checker, parser, legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_std_time_receivers.incn (`Duration.from_secs(1).as_secs()`, the imported type as a type-like receiver). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_nested_rust_associated_method_arg_keeps_type_like_receiver` | retire | - | - | checker, parser, legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_std_time_receivers.incn (`Duration.from_millis(500)` as a method argument keeps its type-like receiver). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_rust_constant_method_receiver_as_value_not_type_like` | retire | - | - | checker, parser, legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_std_time_receivers.incn (`UNIX_EPOCH.checked_add(...)`, the imported constant as a value receiver). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |
| `lower_generic_box_as_ref_preserves_nominal_generic_receiver_args` | retire | - | - | checker, parser, legacy_ir | open: candidate loaves/compiler/incan_test_support/fixtures/behavior_candidates/lowering/rust_box_generic_receiver.incn (the test's own program with a `main`: `Box[Node[T]].as_ref()` handed to a generic function returns the typed value). A `rust::` program; waits for a Cargo-free bake under the compiler suite. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/expr/mod.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `a_package_projects_its_own_inherent_method_while_building_itself` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/package_inherent_methods_issue1174` | - | checker, legacy_ir | `method_belongs_to_an_imported_type` dies with the crate; the fixture's provider calls an inherent method of a sibling module's model while it is baked (#1174's failure mode), and the consumer prints its result. |
| `another_packages_inherent_method_is_still_not_projected` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/package_inherent_methods_issue1174` | - | checker, legacy_ir | The suppression decision dies with the crate; the fixture's consumer calls the dependency's inherent method on the dependency's type directly. |
| `a_dispatched_call_to_another_packages_method_is_not_projected` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/stdlib_trait_dispatch_across_package.incn` | - | checker, legacy_ir | The projection-suppression decision dies with the crate; the fixture is the case the test names, typed `BinaryRead`/`BinaryWrite` calls on a stdlib `BytesIO`, each width reaching the stdlib's own implementation. A standalone bake of a `std.io` program reaches for Rust inspection; `incan check` accepts the fixture, and the same `std.io` surface runs under the compiler suite in the `snapshots_*` areas. |
| `generic_receiver_keeps_trait_abi_dispatch` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/trait_dispatch_receiver_shapes.incn` | - | legacy_ir | The trait-slot versus projection spelling dies with the crate; the fixture dispatches the trait method on a generic receiver bound by a source trait. |
| `nominal_generic_receiver_can_use_source_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/trait_dispatch_receiver_shapes.incn` | - | legacy_ir | The projection decision dies with the crate; the fixture dispatches the trait method through a nominal generic receiver over a bound type parameter. |
| `rust_native_trait_keeps_abi_dispatch_for_concrete_receiver` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/trait_dispatch_receiver_shapes.incn` | - | legacy_ir | The ABI-dispatch decision dies with the crate; the fixture calls `clone()` on a derived model and shows the copy. |
| `a_receiver_typed_as_a_supertrait_adopter_keeps_the_trait_slot_spelling` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/trait_typed_receivers.incn` | - | legacy_ir | The trait-slot spelling dies with the crate; the fixture returns a value typed as the sub-trait and calls a method of its supertrait on it. |
| `a_receiver_typed_as_the_dispatched_trait_keeps_the_trait_slot_spelling` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/trait_typed_receivers.incn` | - | legacy_ir | The trait-slot spelling dies with the crate; the fixture is the case the test names, `display[T](data: DataSet[T])` calling the trait's own method. |
| `concrete_source_trait_can_use_recoverable_projection` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/trait_dispatch_receiver_shapes.incn` | - | legacy_ir | The projection decision dies with the crate; the fixture dispatches a source trait's method on its concrete adopter. |
| `imported_method_chain_dispatches_trait_through_public_library` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/fallible_iterator_through_public_library` | - | legacy_ir | The `fallible_streams::__incan_std::derives::collection::FallibleIterator` trait path dies with the crate; the fixture chains `map` and `collect` on a stream a dependency returns. The consumer imports `FallibleIterator` itself: without the import, the generated Rust leaves the trait out of scope and fails to compile (E0599, #1761). |
| `sdk_provider_dispatches_std_trait_through_local_facade` | retire | `dies` | SDK-provider build-mode switch of the retiring crate (`set_sdk_provider_build`): the `crate::__incan_std::...` trait path a dispatch takes while the compiler builds the standard library itself; no user program. | legacy_ir | - |
| `rust_native_trait_dispatch_keeps_native_method_lookup` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/clone_dispatch_with_std_import.incn` | - | legacy_ir | The native trait path dies with the crate; the fixture imports `Clone` from `std.derives.copying` and calls `clone()` on a derived model. A generic bound spelled with that imported `Clone` is covered by #1727. |
| `stdlib_json_trait_owner_precedes_native_serde_mapping` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/json_protocol_dispatch.incn` | - | legacy_ir | The `crate::__incan_std::serde::json::Serialize` path dies with the crate; the fixture adopts the trait by its imported name and module-qualified and calls `to_json` on both. |
| `unqualified_stdlib_json_trait_precedes_native_serde_mapping` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/json_protocol_dispatch.incn` | - | legacy_ir | The unqualified `Serialize` path dies with the crate; the fixture's `with Serialize` adopter reaches the JSON protocol. |
| `stdlib_json_trait_dispatch_keeps_source_protocol_lookup` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/json_protocol_dispatch.incn` | - | legacy_ir | The `json::Serialize` path dies with the crate; the fixture's `with json.Serialize` adopter reaches the JSON protocol. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/expr/patterns.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `nested_string_literal_pattern_lowers_to_a_guarded_binding_issue1707` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/tuple_str_literal_pattern.incn` | - | checker, parser, legacy_ir | Asserts the recorded pattern fact on the legacy IR (a nested str literal becomes a `__incan_match_str_<n>` binding whose equality test is the arm's guard); retires with `incan_ir`. The behavior, a str literal nested in a tuple pattern selecting its arm, is carried by the named fixture (#1707). |
| `hoisted_literal_tests_conjoin_with_the_arm_guard_issue1707` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/tuple_str_literal_pattern.incn` | - | checker, parser, legacy_ir | Asserts the recorded guard shape on the legacy IR (literal tests ahead of the user's guard; a nested alternation of str literals as one binding tested with `or`); retires with `incan_ir`. The behavior is carried by the named fixture's guarded and alternation arms (#1707). |
| `partial_constructor_pattern_records_its_rest_fields_as_wildcards_issue1708` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_models_and_classes/partial_constructor_pattern.incn` | - | checker, parser, legacy_ir | Asserts the recorded pattern fact on the legacy IR (the checker's rest fields appear as wildcards in the struct pattern, alias keys canonical); retires with `incan_ir`. The behavior, a partial constructor pattern selecting its arm, is carried by the named fixture (#1708). |
| `tuple_pattern_over_a_written_tuple_annotation_binds_its_names_issue1714` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_enums_and_matching/tuple_pattern_bindings.incn` | - | checker, parser, legacy_ir | Asserts the recorded bindings on the legacy IR (a tuple pattern over a `tuple[int, str]` scrutinee binds its names with the element types); retires with `incan_ir`. The behavior is carried by the named fixture (#1714). |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/mod.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `decorator_declared_result_type_follows_the_resolved_declaration_issue1453` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/decorator_declared_surface_issue1453.incn` | - | checker, parser, legacy_ir | The `IrType::Function` surface read off the decorator declaration dies with the crate; the fixture is the test's program with a `main`: the decorator declared after its use and the factory decorator both keep the union surface, the undecorated function is untouched. |
| `retain_declared_decorator_surface_restores_native_carriers_issue1453` | retire | `loaves/toolchain/incan-cli/tests/package_boundary_facade_tests.rs::explicitly_typed_decorator_keeps_the_provider_union_representation_issue1453` | - | legacy_ir | Asserts the positional IrType merge that restores a provider-owned union onto a decorated surface (`retain_declared_decorator_surface`) on hand-built types; retires with `incan_ir`. The behavior, an explicitly typed decorator over a provider's `pub type` union running across the boundary and printing the narrowed value, is carried by the named CLI test (the same #1453 program). That test also reads the baked library's generated text (line 615), which retires with the file's other generated-text reads; its bakes, consumer run and `42` assertion must survive that retirement. The local-alias, declared-after-use and factory halves of #1453 are twinned by loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/decorator_declared_surface_issue1453.incn. |
| `local_trait_shadow_and_nonstdlib_alias_keep_default_fact_provenance` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/local_trait_shadows_imported_trait_issue1592` | - | checker, parser, legacy_ir | The `imported_trait_decls` provenance flags die with the crate; the fixture keeps a local `Same` and an imported module's `Same` apart and each adopter expands the default body of the trait it named. |
| `stdlib_import_facade_reexports_use_the_emitted_namespace` | retire | `dies` | standard-library facade build of the retiring crate (current module `__incan_std.datetime.civil`): the emitted `__incan_std` re-export target path of a `from std... import` in the stdlib's own source; a Rust namespace spelling with no user program. | parser, legacy_ir | - |
| `synthetic_method_partial_wrapper_has_no_source_receiver_binding` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/method_partial_mutating_receiver.incn` | - | parser, legacy_ir | The wrapper's receiver-binding shape dies with the crate; the fixture calls two method partials on a `mut` model and shows the receiver mutated through the `mut self` target. |
| `class_lowering_without_type_info_fails_closed` | retire | `dies` | fail-closed guard of the retiring crate: the test hand-strips or omits the checker's facts (no type info at all for a class) and asserts lowering refuses to guess; no checked program reaches lowering without them, so there is nothing a program can show. | checker, parser, legacy_ir | - |
| `source_function_without_canonical_identity_fails_closed` | retire | `dies` | fail-closed guard of the retiring crate: the test hand-strips or omits the checker's facts (a function's canonical identity removed) and asserts lowering refuses to guess; no checked program reaches lowering without them, so there is nothing a program can show. | checker, parser, legacy_ir | - |
| `source_static_without_canonical_identity_fails_closed` | retire | `dies` | fail-closed guard of the retiring crate: the test hand-strips or omits the checker's facts (a static's canonical identity removed) and asserts lowering refuses to guess; no checked program reaches lowering without them, so there is nothing a program can show. | checker, parser, legacy_ir | - |
| `decorated_function_original_name_collision_uses_distinct_registry_keys` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/decorated_function_beside_original_named_function.incn` | - | checker, parser, legacy_ir | The registry keys die with the crate; the fixture declares `__incan_original_target` beside a decorated `target` and calls both. |
| `source_method_without_canonical_identity_fails_closed` | retire | `dies` | fail-closed guard of the retiring crate: the test hand-strips or omits the checker's facts (a method's canonical identity removed) and asserts lowering refuses to guess; no checked program reaches lowering without them, so there is nothing a program can show. | checker, parser, legacy_ir | - |
| `model_lowering_without_type_info_fails_closed_issue884` | retire | `dies` | fail-closed guard of the retiring crate: the test hand-strips or omits the checker's facts (no checked field visibility for a model, #884) and asserts lowering refuses to guess; no checked program reaches lowering without them, so there is nothing a program can show. | checker, parser, legacy_ir | - |
| `test_lower_simple_function` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/function_model_enum_and_entry.incn` | - | checker, parser, legacy_ir | The declaration count and parameter count die with the crate; the fixture calls the function. |
| `lowering_reports_default_parameter_expression_failures` | retire | `dies` | hand-built AST (a vocab block as a parameter default, never desugared) driven into lowering to assert the error text; the parser and desugarer never produce that input from a program. | checker, legacy_ir | - |
| `test_lower_model` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/function_model_enum_and_entry.incn` | - | checker, parser, legacy_ir | The struct/impl pair dies with the crate; the fixture constructs the model and reads both fields. |
| `test_lower_pub_model_uses_checked_private_field_visibility_issue884` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/pub_model_private_field_refused_issue884` | - | checker, parser, legacy_ir | The `is_type_private` flags die with the crate; the fixture reads a `pub model`'s non-pub field from another module and is refused (INCAN-T0001, `Field 'secret' on 'Vault' is private`). |
| `test_lower_main_entry` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/function_model_enum_and_entry.incn` | - | checker, parser, legacy_ir | `entry_point` dies with the crate; the fixture's first line is printed by `main`. |
| `test_lower_rfc018_assert_is_some_binding_to_helper_let` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/assert_is_some_binds_the_payload.incn` | - | checker, parser, legacy_ir | The `std.testing.assert_is_some` helper call dies with the crate; the fixture binds the payload with `assert value is Some(inner)` and prints it. |
| `test_lower_rfc018_assert_raises_to_canonical_helper_call` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/assert_raises_forms.incn` | - | checker, parser, legacy_ir | The `std.testing.assert_raises` helper call dies with the crate; the fixture's `assert explode() raises ValueError` passes over a block that raises and the program continues (a block that does not raise fails the assertion: checked while writing). |
| `test_lower_imported_assert_raises_injects_error_type_name` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/assert_raises_forms.incn` | - | checker, parser, legacy_ir | The injected `"ValueError"` literal argument dies with the crate; the fixture calls `assert_raises[ValueError](explode, ...)` and the program continues. |
| `test_lower_if_statement` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/statements_and_expressions.incn` | - | checker, parser, legacy_ir | The non-empty body assertion dies with the crate; the fixture prints all three branches. |
| `test_lower_for_loop` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/statements_and_expressions.incn` | - | checker, parser, legacy_ir | The declaration count dies with the crate; the fixture prints the loop's iterations. |
| `test_lower_binary_expressions` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/statements_and_expressions.incn` | - | checker, parser, legacy_ir | The declaration count dies with the crate; the fixture prints the chained arithmetic result. |
| `test_lower_list_literal` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/statements_and_expressions.incn` | - | checker, parser, legacy_ir | The declaration count dies with the crate; the fixture returns the list literal and prints its length and sum. |
| `test_lower_enum` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/function_model_enum_and_entry.incn` | - | checker, parser, legacy_ir | The variant count dies with the crate; the fixture matches a variant of the three-variant enum. |
| `test_inferred_reassign_mutable` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/statements_and_expressions.incn` | - | checker, parser, legacy_ir | The three-statement body shape dies with the crate; the fixture reassigns a `mut` binding and prints it. |
| `test_inferred_reassign_immutable_error` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/immutable_reassignment_refused.incn` | - | checker, parser, legacy_ir | The lowering error text dies with the crate; the fixture's reassignment of a non-`mut` binding is refused at check time (INCAN-T0001). |
| `test_serde_propagation_respects_derives_and_containers` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/serde_derive_propagation.incn` | - | checker, parser, legacy_ir | The `derives` lists on the enum and newtype die with the crate; the fixture serializes the payload, which needs the derive propagated to both. `Deserialize` staying unpropagated is not observable. The enum derives `Eq, Hash`: a `set[Tag]` over an underived enum passes `incan check` and fails to compile (E0277, #1758). |
| `method_kind_for_receiver` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/user_methods_named_like_builtins.incn` | - | legacy_ir | The `MethodKind` classifications die with the crate; the fixture dispatches a model's own `join`/`split`/`replace`/`count`/`index`/`map` beside the str and mut-list builtins of those names. `list.count(v)` and `list.index(v)` have no program: the checker refuses them (`Iterator.count() expects 0 argument(s)`), so those two classifications were unreachable. |
| `method_kind_for_receiver_issue1668_surfaces` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_expressions/receiver_aware_method_kinds_issue1668.incn` | - | legacy_ir | The classifications die with the crate; the fixture calls `contains_key`/`keys`/`values` on a dict and through a `mut` dict parameter, `encode` on `str` and `FrozenStr`, and `decode` on a bytes literal, a bytes value and a `FrozenBytes` const. The two negative classifications (`str.decode`, `bytes.encode`) are checker refusals, not lowering facts. |
| `membership_operators_lower_with_receiver_aware_known_methods` | retire | `loaves/compiler/incan_frontend/src/body_ir/tests/operators_literals_and_assignment.rs::lowers_collection_membership_as_a_helper_call_naming_its_own_container` | - | checker, parser, legacy_ir | Asserts that `in` over a list, set, dict and str lowers to the receiver's own known-method kind and `not in` to its negation. Body IR asserts the same fact per container: the named test (list/set/dict each name their own contains helper), with `lowers_string_membership_as_an_explicit_helper_call_with_its_runtime_requirement` (str) and `lowers_negated_collection_membership_as_its_own_helper_per_container` (`not in`) in the same file. |
| `aliased_stdlib_json_trait_dispatch_names_the_declaration_issue1712` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_newtypes_and_serde/partial_alias_derive.incn` | - | checker, parser, legacy_ir | asserts the recorded dispatch path of a `to_json` call through an aliased `std.serde.json.Serialize` bound names the declaration, not the alias (#1712); the twin runs that program and prints the encoded value. |
| `fn_family_capability_markers_lower_to_the_callable_bound_issue1716` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_traits_and_generics/std_rust_capability_bounds.incn` | - | checker, parser, legacy_ir | asserts the recorded bound shape of the `Fn`, `FnMut` and `FnOnce` markers: the canonical `CallableN` trait with a hidden return-type parameter (#1716); the twin runs each marker over a named function. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/tests/builtin_str_arguments.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `str_over_an_operator_expression_lowers_a_grouped_argument_issue1726` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_collections_and_strings/str_over_arithmetic_expression.incn` | - | legacy_ir | Asserts the recorded shape on the legacy IR (an operator-shaped `str(...)` argument is handed to the emitter as one grouped value in its own type; an atomic argument stays as written); retires with `incan_ir`. The behavior, `str(a + b)` rendering the whole sum, is carried by the named fixture (#1726). |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/tests/derive_vocabulary_imports.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `derive_vocabulary_imports_lower_to_no_ir_import_issue1727` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_stdlib/std_derives_collection_adapters_with_imported_clone.incn` | - | legacy_ir | Asserts the recorded import fact on the legacy IR (an import of a derivable trait's stdlib stub lowers to no IR import; the source-owned protocol and callable imports stay; the expanded adapter default and an aliased `Eq` bound spell the derive-owned Rust traits); retires with `incan_ir`. The behavior, the FallibleIterator adapters driven with derived callables beside `from std.derives.copying import Clone`, is carried by the named fixture (#1727). |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/tests/method_decorator_receivers.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `shared_receiver_slots_are_planned_to_the_wrapper_form` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_method_decorators.incn` | - | parser | asserts that lowering rewrites a `self` method's decorator shapes and replacement to the form the method's wrapper passes the receiver in, and nothing else (#1790); the twin runs that decorator chain. |
| `factory_slot_plans_the_decorator_shape_it_returns` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/self_method_decorator_factory_and_direct_call.incn` | - | parser | asserts that a factory's planned slot rewrites the decorator shape it returns (#1790); the twin runs a factory-produced decorator on a `self` method. |
| `mutable_receiver_slots_leave_the_program_as_written` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/snapshots_functions_and_projections/user_defined_mutable_method_decorators.incn` | - | parser | asserts that a `mut self` chain is lowered as written, since the `mut` marker already says how the receiver is passed (#1790); the twin runs that chain. |
| `direct_call_of_a_planned_replacement_uses_the_planned_signature` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/self_method_decorator_factory_and_direct_call.incn` | - | legacy_ir | asserts that a direct call of a planned replacement carries the planned signature (#1790); the twin calls the replacement directly and prints its result. |
| `closure_checked_against_a_mut_marked_shape_takes_the_argument_mut` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/mut_marked_callable_value_changes_the_argument.incn` | - | legacy_ir | asserts that a closure checked against a `(mut T, ...)` shape lowers that parameter the way a `mut` parameter is passed (#1790); the twin calls a value of that shape and prints the caller's changed argument. |
| `imported_mut_parameter_is_passed_mut_across_the_library_boundary` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli_dependencies/mut_parameter_across_package_boundary` | - | checker, parser, legacy_ir | asserts that a direct call of a library function with a `mut` parameter lowers that argument as `mut` (#1790); the twin calls the dependency's function directly and through a marked function type and prints the consumer's changed argument. |
| `mut_parameter_passing_follows_the_checker_marker` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/mut_parameter_of_a_scalar_alias.incn` | - | legacy_ir | asserts that lowering passes each `mut` parameter the way the checker's marker says, so an `int` or `float` alias parameter is passed by value (#1790); the twin calls such a function directly and as a value of `(int) -> int` and prints the caller's unchanged value. |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/tests/newtype_automatic_derives.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `newtype_automatic_derives_follow_the_underlying_type_issue1754` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/model_field_of_newtype_over_str.incn` | - | legacy_ir | Asserts the derive list lowering gives a newtype (Clone when the underlying type implements it, Debug unless it lacks it, Copy for Copy types). The behavior, a model holding a newtype over str building and reading it back, is carried by the named fixture (#1754). |
| `rust_derive_path_of_an_automatic_derive_is_emitted_once_issue1754` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/rust_derived_clone_path_on_newtype_and_model.incn` | - | legacy_ir | Asserts that a `@rust.derive` path to a derive a newtype already carries (`std::clone::Clone`) joins the derive list once. The behavior, such a newtype building and a model holding it, is carried by the named fixture (#1754). |
| `rust_derive_paths_of_automatic_and_implied_derives_are_emitted_once_issue1754` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/rust_derived_clone_path_on_newtype_and_model.incn` | - | legacy_ir | Asserts that a model, class or enum names each automatic or implied derive once whatever `@rust.derive` path also names it. The behavior, a model with `@rust.derive("std::clone::Clone")` building, is carried by the named fixture (#1754). |
| `ord_derive_adds_its_prerequisites_in_table_order` | retire | `dies` | asserts the textual order of the lowered derive list (`Ord`, `PartialOrd`, `Eq`, `PartialEq`), a generated-Rust spelling that dies with the Rust-source backend (#654); the implication itself is the incan_lang registry table, tested there (keep). | legacy_ir | - |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/tests/unary_operand_grouping.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `prefix_operator_over_an_operator_expression_lowers_a_grouped_operand_issue1763` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/negation_keeps_its_operand_grouping.incn` | - | legacy_ir | Asserts the recorded shape on the legacy IR (an operator-shaped operand of `not`, unary `-` or `~` is handed to the emitter as one grouped value in its own type; an atomic operand stays as written); retires with `incan_ir`. The behavior, `not (1 == 2)` printing `true` and `-(2 + 3)` printing `-5`, is carried by the named fixture (#1763). |

Per-test overrides in `loaves/compiler/incan_ir/src/lower/types.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `foreign_manifest_signatures_use_checked_native_routes` | retire | - | - | checker, legacy_ir | open: asserts, on a hand-built manifest signature, that a provider's exported function type carrying a type of the provider's own dependency lowers through the checker's foreign remapping (`pub::pricing::__incan_provider_rust::catalog::Product`). The behavior needs a provider that itself declares `[dependencies]`, whose bake under the suite goes through the compatibility publisher and reaches Cargo (see *Provider bakes and Cargo* in the family README); the program shape is the `transitive_provider_chain/` candidate under the `dependencies/` behavior-candidates group. Waits for a Cargo-free bake, under the compiler suite, of a provider that declares `[dependencies]`. |
| `exact_binary_float_arithmetic_keeps_its_native_ir_width` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/exact_float_width_arithmetic.incn` | - | legacy_ir | `binary_result_type` dies with the crate; the fixture shows the width through the printed values (`0.1 + 0.2` is `0.3` in f32 and `0.30000000000000004` in f64) for every operator of the table. |
| `lower_trait_default_annotation_uses_defining_module_type_path` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/stdlib_trait_default_names_its_own_module_type.incn` | - | legacy_ir | The `crate::__incan_std::io::ReaderChunks` path dies with the crate; the fixture iterates `BytesIO.chunks(2)`, the `BinaryReader` default whose annotation names its module's `ReaderChunks`, never imported by the program. A standalone bake of a `std.io` program reaches for Rust inspection; `incan check` accepts the fixture, and the same `std.io` surface runs under the compiler suite in the `snapshots_*` areas. The user-module flavor fails to compile: an imported trait's default returning a type of its own module is not found in the adopter's expansion (E0425, #1759). |
| `lower_trait_default_constructor_respects_active_local_binding` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/trait_default_local_shadows_module_type` | - | legacy_ir | `active_trait_default_value_type_path` dies with the crate; the fixture's imported default body binds a local named like a model of the trait's module and returns the local. The map the test consults, `active_trait_default_type_paths`, is filled only when a stdlib trait is imported (`trait_default_type_paths` is written by the stdlib-cache import path alone), so for a user module's trait it is never populated and the local binding shadows nothing: the shadowing this test asserts is vacuous for a user module until #1759 (E0425) is fixed and the map is populated there too. The fixture asserts the observable, the local value coming back, and stays the twin. The sibling fact, the default constructing that model by named fields across modules, fails to compile (E0423, #1759). |
| `lower_resolved_type_qualifies_canonical_public_library_identity_issue892` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/provider_nominal_types_and_unions_issue892` | - | legacy_ir | The `widgets::Widget` Rust path dies with the crate; the fixture constructs and narrows the provider's nominal types in a consumer. |
| `lower_resolved_type_qualifies_generic_public_library_identity_issue892` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/provider_nominal_types_and_unions_issue892` | - | legacy_ir | The `widgets::Envelope<widgets::Widget>` Rust path dies with the crate; the fixture constructs the provider's generic type over the provider's nominal type in a consumer and reads it back. |
| `pub_external_type_normalizes_canonical_union_members_issue892` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/provider_nominal_types_and_unions_issue892` | - | legacy_ir | The `IrType::ExternalUnion` member spelling dies with the crate; the fixture matches the provider's union by its own members in a consumer. |
| `lower_resolved_type_preserves_named_generic_args_for_nominal_types` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/generic_nominal_arguments.incn` | - | legacy_ir | The `NamedGeneric` shape dies with the crate; the fixture constructs `Wrapper[Node]` and reads the nominal argument's field. |
| `lower_resolved_named_type_param_preserves_active_generic_identity` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/generic_nominal_arguments.incn` | - | legacy_ir | The `Generic`/`Struct` split dies with the crate; the fixture's generic functions use `K` as a type parameter beside a nominal `Value`. `first` carries `K with Clone`: without the bound, the index fails to compile (E0308, #1756). |
| `lower_resolved_frozen_collections_freeze_nested_strings` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/frozen_collections_nested_strings.incn` | - | legacy_ir | The `FrozenDict`/`FrozenList` over `&'static` spellings die with the crate; the fixture reads nested const frozen collections by length, index and iteration. Indexing and `contains_key` on a const `FrozenDict`, and `str.join` over a `FrozenList[str]` comprehension, pass `incan check` and fail to compile (E0608/E0277/E0308, #1757). |
| `merge_inferred_ir_type_preserves_existing_generic_placeholders` | retire | `dies` | data-structure invariant of the retiring crate: `merge_inferred_ir_type` keeps a generic placeholder over an inferred struct spelling, on hand-built `IrType` values. | legacy_ir | - |
| `merge_inferred_ir_type_does_not_erase_a_concrete_closure_return` | retire | `dies` | data-structure invariant of the retiring crate: `merge_inferred_ir_type` keeps a concrete closure return over `Unknown`, on hand-built `IrType` values. | legacy_ir | - |
| `merge_inferred_ir_type_preserves_exact_rust_display_types` | retire | `dies` | data-structure invariant of the retiring crate: `merge_inferred_ir_type` keeps an `IrType::RustDisplay` spelling over a union shape, on hand-built `IrType` values. | legacy_ir | - |

Per-test overrides in `loaves/compiler/incan_ir/src/types.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `isinstance_string_storage_identity_is_symmetric_without_widening_union_carriers` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_declarations/isinstance_over_string_storages.incn` | - | legacy_ir | The helper's decision is what `isinstance(value, str)` answers over a union carrying `FrozenStr` and `str` members; the fixture matches both string storages against `str` and an int against `int`. The `union_member_type_matches` asymmetry (String accepts FrozenStr, not the reverse) is a widening rule of the retiring carrier and dies. |
| `external_union_member_matching_is_provider_aware_issue892` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/provider_nominal_types_and_unions_issue892` | - | legacy_ir | The `widgets::Widget` versus `other::Widget` spelling test dies with the carrier. The fixture's two providers each export a `Widget` (`name: str` in `widgets`, `value: int` in `other`); the consumer imports the second as `OtherWidget`, narrows `widgets`' union by `Widget` and `other`'s by `OtherWidget`, and hands each locally constructed Widget to its own union: the two same-spelled models are distinct members of distinct unions, which is the negative half of the test (`other::Widget` is no member of `widgets`' union). |
| `provider_localization_preserves_foreign_external_union_issue892` | retire | - | - | legacy_ir | open: asserts, on a hand-built union owned by `other` whose member is `widgets::Widget`, that localizing `widgets` leaves the foreign union untouched. The program is a provider `other` that imports `widgets`' `Widget` and exports a union carrying it, narrowed in a consumer of both: `other` is then a provider that itself declares `[dependencies]`, whose bake under the suite goes through the compatibility publisher and reaches Cargo (see *Provider bakes and Cargo* in the family README), so the harness cannot bake it Cargo-free; the program shape is the `transitive_provider_chain/` candidate under the `dependencies/` behavior-candidates group. Waits for a Cargo-free bake, under the compiler suite, of a provider that declares `[dependencies]`. loaves/compiler/incan_test_support/fixtures/behavior/lowering_dependencies/provider_nominal_types_and_unions_issue892/ keeps two dependency-free providers' unions apart in one consumer, the same-spelled-member half of #892, not this test's foreign-member half. |

### `loaves/compiler/incan_test_support` (22 tests in 3 files: retire 3, unaffected 19)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/compiler/incan_test_support/src/behavior_fixtures.rs` | 18 | 1671 | 596 | unaffected | - | - | - | #1561 | run 1 | test-support helper for the behavior-fixture family: header grammar, discovery, materialization and observable comparison. The module drives `incan check` / `incan run` through `run_incan`; its unit tests exercise the grammar, discovery and the comparison rules with no compiler call. |
| `loaves/compiler/incan_test_support/src/builtin_stdlib.rs` | 1 | 75 | 16 | unaffected | - | - | - | #1561 | - | test-support helper for the builtin stdlib inventory. |
| `loaves/compiler/incan_test_support/src/emitted_symbol_artifact.rs` | 3 | 421 | 50 | retire | 0/3 | 0 | - | #1561 | - | helpers over emitted symbol projections (RFC 120 physical names). |

### `loaves/kernel/incan_lang` (115 tests in 23 files: keep 115)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/kernel/incan_lang/src/bin/generate_vscode_grammar_keywords.rs` | 2 | 161 | 35 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/errors.rs` | 6 | 327 | 85 | keep | - | - | - | #1561 | checker 1 | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/interop/capabilities.rs` | 2 | 73 | 24 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/interop/coercions.rs` | 6 | 448 | 84 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/interop/metadata.rs` | 21 | 2022 | 396 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/builtins.rs` | 1 | 339 | 12 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/c_abi.rs` | 1 | 465 | 74 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/callables.rs` | 2 | 132 | 25 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/conventions.rs` | 1 | 55 | 13 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/derives.rs` | 1 | 224 | 14 | keep | - | - | - | #1561 | - | language registry (derive vocabulary and the implication table lowering and the typechecker share, #1754); below the emitter. |
| `loaves/kernel/incan_lang/src/lang/highlighting.rs` | 4 | 217 | 56 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/keywords.rs` | 3 | 939 | 25 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/stdlib.rs` | 11 | 1464 | 472 | keep | - | - | - | #1561 | checker 1 | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/surface/types.rs` | 4 | 769 | 149 | keep | - | - | - | #1561 | - | language registry (surface type derive support for Clone, Debug, Eq and Hash, #1754, #1758); below the emitter. |
| `loaves/kernel/incan_lang/src/lang/text_codecs.rs` | 2 | 95 | 36 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/traits.rs` | 1 | 373 | 24 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lang/types/numerics.rs` | 4 | 469 | 79 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/lib.rs` | 5 | 331 | 99 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/numeric_strings.rs` | 6 | 204 | 113 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/numeric_values.rs` | 3 | 222 | 53 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/src/strings.rs` | 2 | 366 | 30 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/tests/lang_registry_guardrails.rs` | 26 | 687 | 687 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_lang/tests/string_len_semantics.rs` | 1 | 12 | 12 | keep | - | - | - | #1561 | - | language registry (stdlib inventory, interop metadata, version); below the emitter. Reviewed at crate level. |

### `loaves/kernel/incan_semantics_core` (128 tests in 13 files: keep 128)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/kernel/incan_semantics_core/src/authority.rs` | 9 | 344 | 177 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/body_ir.rs` | 32 | 4463 | 1080 | keep | - | - | - | #1561 | replacement 1 | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/closure_digest.rs` | 12 | 430 | 226 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/dependencies.rs` | 4 | 317 | 155 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/emitted_symbol.rs` | 7 | 559 | 177 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/executable_representation.rs` | 13 | 1308 | 583 | keep | - | - | - | #1561 | replacement 13 | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/facts.rs` | 12 | 1494 | 408 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/hir.rs` | 2 | 242 | 66 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/namespace.rs` | 7 | 148 | 78 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/receipts.rs` | 12 | 1014 | 431 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/semantic_digest.rs` | 4 | 502 | 84 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/stable_identity.rs` | 7 | 444 | 245 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |
| `loaves/kernel/incan_semantics_core/src/types.rs` | 7 | 435 | 95 | keep | - | - | - | #1561 | - | semantics core (Body IR, receipts, authority); below the emitter. Reviewed at crate level. |

### `loaves/kernel/incan_syntax` (314 tests in 15 files: keep 314)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/kernel/incan_syntax/src/ast/types.rs` | 1 | 260 | 40 | keep | - | - | - | #1561 | - | lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/diagnostics/base.rs` | 2 | 396 | 39 | keep | - | - | - | #1561 | checker 1 | lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/diagnostics/stable.rs` | 10 | 1104 | 345 | keep | - | - | - | #1561 | checker 10 | lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/lexer/mod.rs` | 20 | 909 | 400 | keep | - | - | - | #1561 | checker 20, parser 20 | lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/embedded/tests.rs` | 28 | 915 | 907 | keep | - | - | - | #1561 | checker 24, parser 4 | lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/expressions.rs` | 26 | 769 | 769 | keep | - | - | - | #1561 | checker 20, parser 26 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/fstrings.rs` | 7 | 365 | 365 | keep | - | - | - | #1561 | checker 7, parser 7 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/functions_decorators_and_bindings.rs` | 23 | 556 | 556 | keep | - | - | - | #1561 | checker 16, parser 23 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/modules_and_imports.rs` | 47 | 883 | 883 | keep | - | - | - | #1561 | checker 30, parser 47 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/patterns_and_matching.rs` | 16 | 555 | 555 | keep | - | - | - | #1561 | checker 12, parser 16 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/soft_keywords_and_vocab_blocks.rs` | 20 | 655 | 655 | keep | - | - | - | #1561 | checker 5, parser 20 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/statements_and_blocks.rs` | 19 | 426 | 426 | keep | - | - | - | #1561 | checker 17, parser 19 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/type_declarations.rs` | 58 | 1134 | 1134 | keep | - | - | - | #1561 | checker 35, parser 58, formatter 2 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/types_and_bounds.rs` | 23 | 463 | 463 | keep | - | - | - | #1561 | checker 14, parser 23 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |
| `loaves/kernel/incan_syntax/src/parser/tests/vocab_scoped_symbols.rs` | 14 | 824 | 824 | keep | - | - | - | #1561 | parser 14 | split of parser/tests.rs; lexer, parser and diagnostics catalog; below the emitter, cannot reach codegen. Reviewed at crate level. |

### `loaves/toolchain/incan-cli` (642 tests in 46 files: keep 117, re-point 366, retire 34, unaffected 125)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/toolchain/incan-cli/src/bin/generate_feature_inventory.rs` | 1 | 85 | 23 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/commands/build.rs` | 11 | 1317 | 620 | keep (keep 8, retire 3) | 0/3 | 3 | - | #1561 | replacement 6, checker 3, parser 1 | build command over checked facts (contract step, library publication); the rustc-failure classification tests belong to the generated build. |
| `loaves/toolchain/incan-cli/src/commands/cache.rs` | 2 | 131 | 17 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/commands/debug.rs` | 1 | 178 | 22 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/commands/init.rs` | 16 | 755 | 316 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/commands/lifecycle.rs` | 4 | 924 | 104 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/commands/representation_inspect.rs` | 4 | 503 | 126 | keep | - | - | - | #1561 | checker 4 | representation inspection from checked facts. |
| `loaves/toolchain/incan-cli/src/commands/tools_boundary_tests.rs` | 1 | 116 | 116 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/commands/workspace.rs` | 1 | 428 | 45 | unaffected | - | - | - | #1561 | - | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/lib.rs` | 37 | 3389 | 1069 | unaffected | - | - | - | #1561 | codegen 1, replacement 9 | CLI surface (argument parsing, scaffolding, lifecycle, cache); no compiler semantics. |
| `loaves/toolchain/incan-cli/src/test_runner/execution.rs` | 21 | 3539 | 618 | keep (keep 10, retire 11) | 0/11 | 11 | - | #1561 | text 1, checker 3, parser 3 | `incan test` today lowers Incan tests into a Rust libtest harness; the harness-shape tests retire with it, the discovery and session tests stay. |
| `loaves/toolchain/incan-cli/src/test_runner/mod.rs` | 15 | 2148 | 492 | keep | - | - | - | #1561 | checker 3 | test collection, parametrize expansion, marker selection and scheduling. |
| `loaves/toolchain/incan-cli/tests/behavior_cli_dependencies_tests.rs` | 1 | 22 | 22 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the cli_dependencies area as a program, baking its in-fixture providers first with no Cargo authority (the root is deliberately not registered in OvenCompilerSuiteTargetCapabilities), and compares its observables; the route changes under it, the fixtures do not. |
| `loaves/toolchain/incan-cli/tests/behavior_cli_tests.rs` | 1 | 17 | 17 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the cli area as a program and compares its observables; the route changes under it, the fixtures do not. |
| `loaves/toolchain/incan-cli/tests/behavior_driver_tests.rs` | 1 | 18 | 18 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the driver area as a program and compares its observables; the route changes under it, the fixtures do not. |
| `loaves/toolchain/incan-cli/tests/behavior_harness_tests.rs` | 1 | 21 | 21 | re-point | - | - | - | #1561 | - | runs the harness area's self-proof fixtures as programs; the route changes under it, the fixtures do not. |
| `loaves/toolchain/incan-cli/tests/behavior_lowering_tests.rs` | 3 | 34 | 34 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the three lowering_* areas as a program (one libtest case per leaf area), baking a project fixture's in-fixture providers first with no Cargo authority, and compares its observables, which stay the same when the Rust-source backend is removed (#654). |
| `loaves/toolchain/incan-cli/tests/behavior_ownership_tests.rs` | 4 | 43 | 43 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the four ownership_* areas as a program (one case per area) and compares its observables, which stay the same when the Rust-source backend is removed (#654). |
| `loaves/toolchain/incan-cli/tests/behavior_smoke_tests.rs` | 1 | 16 | 16 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the smoke area as a program and compares its observables; the route changes under it, the fixtures do not. |
| `loaves/toolchain/incan-cli/tests/behavior_snapshots_tests.rs` | 8 | 64 | 64 | re-point | - | - | - | #1561 | - | runs every behavior fixture of the snapshots_* areas (one libtest case per leaf area) as a program and compares its observables; the route changes under it, the fixtures do not. |
| `loaves/toolchain/incan-cli/tests/canonical_item_imports.rs` | 3 | 229 | 229 | re-point (keep 1, re-point 2) | - | - | - | #1561 | run 2 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7; the one `check`- or `inspect`-only test is keep (override). |
| `loaves/toolchain/incan-cli/tests/cli_catalog_forms_tests.rs` | 1 | 113 | 113 | re-point | - | - | - | #1561 | run 1 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/cli_codegraph_and_inspection_tests.rs` | 16 | 2016 | 2016 | re-point (keep 8, re-point 7, retire 1) | 0/1 | 1 | required | #1561 | run 7 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. `inspect codegraph`/`inspect bindings`/`check`-only tests are keep; `inspect rust` asserts the generated-project report and generated Rust text and retires unless #654 keeps that inspection path (overrides). |
| `loaves/toolchain/incan-cli/tests/cli_decorator_and_partial_tests.rs` | 11 | 1113 | 1113 | re-point (re-point 10, retire 1) | 1/1 | 0 | - | #1561 | codegen 1, run 11 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/cli_float_display_tests.rs` | 1 | 86 | 86 | re-point | - | - | - | #1561 | run 1 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/cli_interop_target_tests.rs` | 9 | 868 | 868 | re-point (keep 1, re-point 8) | - | - | - | #1561 | run 8 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7; the one `check`- or `inspect`-only test is keep (override). |
| `loaves/toolchain/incan-cli/tests/cli_issue1370_phantom_type_param_tests.rs` | 1 | 93 | 93 | re-point | - | - | - | #1561 | run 1 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/cli_issue1668_stdlib_gaps_tests.rs` | 2 | 235 | 235 | re-point | - | - | - | #1561 | run 2 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/cli_language_regression_tests.rs` | 25 | 2101 | 2101 | re-point (re-point 25) | - | - | required | #1561 | codegen 5, run 25 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Tests that also read generated Rust (`target/incan/<project>/src/main.rs`, `--emit-rust`) stay re-point on their build or run assertion and lose the generated-text assertion in slice 7 (overrides). |
| `loaves/toolchain/incan-cli/tests/cli_layering_guardrails.rs` | 2 | 189 | 189 | unaffected | - | - | - | #1561 | codegen 2, legacy_ir 2 | layering baseline of what the CLI may reach; names codegen types without using them. |
| `loaves/toolchain/incan-cli/tests/cli_provider_boundary_tests.rs` | 19 | 1343 | 1343 | re-point | - | - | - | #1561 | codegen 2, run 19 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Tests that read the generated .rs retire. |
| `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs` | 13 | 1504 | 1504 | re-point (re-point 12, retire 1) | 0/1 | 0 | required | #1561 | codegen 7, text 1, run 13 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Tests that also read generated Rust (`target/incan/<project>/src/main.rs`) stay re-point on their build or run assertion and lose the generated-text assertion in slice 7 (overrides); the one library-build test that only asserts generated text retires. |
| `loaves/toolchain/incan-cli/tests/cli_std_environ_tests.rs` | 4 | 473 | 473 | re-point | - | - | - | #1561 | run 4 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/cli_surface_tests.rs` | 26 | 1434 | 1434 | re-point (keep 10, re-point 12, unaffected 4) | - | - | - | #1561 | run 12, checker 1 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. `check`/`explain`/`tools metadata api` tests are keep; `init`, `tools doctor`, `lock` and the fixture-handoff helper test are unaffected (overrides). |
| `loaves/toolchain/incan-cli/tests/cli_workspace_and_lock_tests.rs` | 26 | 1905 | 1905 | re-point (keep 2, re-point 19, unaffected 5) | - | - | required | #1561 | codegen 1, run 20 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. `workspace check`/`workspace fmt` tests are keep; `lock`/`workspace inspect`-only tests are unaffected; one build test also reads generated Rust and loses that assertion in slice 7 (overrides). |
| `loaves/toolchain/incan-cli/tests/example_capability_coverage.rs` | 1 | 279 | 279 | keep | - | - | - | #1561 | replacement 1 | examples cover the stable capability registry. |
| `loaves/toolchain/incan-cli/tests/integration_tests.rs` | 203 | 12563 | 12563 | re-point (keep 30, re-point 161, retire 8, unaffected 4) | 4/8 | 1 | required | #1561 | codegen 8, text 7, run 164, checker 14, parser 29 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Checker-only and lexer tests stay; generated-text and IrCodegen tests retire; `fmt`- and `check`-only CLI tests are keep and `--help`/`--version`/`lock`-only ones unaffected (overrides). |
| `loaves/toolchain/incan-cli/tests/layering_guard.rs` | 9 | 359 | 359 | unaffected | - | - | - | #1561 | - | crate and stdlib layering guards. |
| `loaves/toolchain/incan-cli/tests/package_boundary_facade_tests.rs` | 7 | 646 | 646 | re-point (re-point 7) | - | - | - | #1561 | run 7, checker 1 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Tests that read the generated .rs retire. |
| `loaves/toolchain/incan-cli/tests/package_executable_representation.rs` | 6 | 724 | 724 | re-point | - | - | - | #1561 | run 6, checker 3 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Tests that read the generated .rs retire. |
| `loaves/toolchain/incan-cli/tests/repository_path_tests.rs` | 5 | 61 | 61 | unaffected | - | - | - | #1561 | - | repository path resolution for the compiler command. |
| `loaves/toolchain/incan-cli/tests/rfc031_pub_import_integration_tests.rs` | 83 | 8173 | 8173 | re-point (keep 27, re-point 47, retire 9) | 1/9 | 6 | required | #1561 | codegen 10, text 4, run 50, checker 33, parser 12 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. Tests that build and also read generated Rust stay re-point and lose the generated-text assertion in slice 7; tests whose only backend assertion is generated text (`--emit-rust` after `incan check`, a planned build's Rust) retire; `check`-only and `fmt`-only tests are keep (overrides). |
| `loaves/toolchain/incan-cli/tests/script_target_diagnostics.rs` | 1 | 44 | 44 | re-point | - | - | - | #1561 | - | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/std_encoding_algorithm_modules.rs` | 1 | 128 | 128 | re-point | - | - | - | #1561 | run 1 | runs `incan` and asserts output, exit code or diagnostics; the route changes in slice 7. |
| `loaves/toolchain/incan-cli/tests/toolchain_installer_tests.rs` | 30 | 2728 | 2728 | unaffected | - | - | required | #1561 | run 4 | installer, archive packager and release manifest. |
| `loaves/toolchain/incan-cli/tests/vocab_guardrails.rs` | 3 | 575 | 575 | unaffected | - | - | - | #1561 | text 1 | source audits (semantic string audit, stringly vocab checks). |

Per-test overrides in `loaves/toolchain/incan-cli/src/commands/build.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `classify_signature_mismatch_for_rust_extern_context` | retire | `dies` | classifies a rustc `error[E0308]` line in the generated Cargo build's stderr back to the `@rust.extern` item; there is no rustc stderr to classify once nothing is generated (#654). The helpers under test are `#[allow(dead_code)]` with no production caller; the tests pin nothing reachable today. | - | maps a rustc failure of the generated build back to source |
| `classify_unresolved_backing_item_for_rust_extern_context` | retire | `dies` | classifies a rustc `error[E0425]` line in the generated Cargo build's stderr as an unresolved `@rust.extern` backing item; there is no rustc stderr to classify once nothing is generated (#654). The helpers under test are `#[allow(dead_code)]` with no production caller; the tests pin nothing reachable today. | - | maps a rustc failure of the generated build back to source |
| `wraps_rust_extern_failure_back_to_incan_declaration_span` | retire | `dies` | renders a rustc `error[E0425]` of the generated build as a diagnostic at the `@rust.extern` declaration span; the rustc failure it wraps dies with the generated build (#654). The helpers under test are `#[allow(dead_code)]` with no production caller; the tests pin nothing reachable today. | - | maps a rustc failure of the generated build back to source |

Per-test overrides in `loaves/toolchain/incan-cli/src/test_runner/execution.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `merge_test_runner_dependencies_promotes_dev_deps_into_dependencies` | retire | `dies` | merges `[dependencies]` and `[dev-dependencies]` into the generated test-runner crate's Cargo dependency set (`promoted_oven_test_dependencies`); the generated crate dies with #654. Its user-visible half — a test file using a `[rust-dev-dependencies]` crate through `incan test` — is untwinned: no CLI test uses `rust-dev-dependencies`; it goes with #1337's interop spec (slice 7). | - | libtest harness of the generated test crate |
| `merge_test_runner_dependencies_unifies_normal_and_dev_features` | retire | `dies` | unions Cargo feature lists and refuses a version conflict inside the generated test-runner crate's Cargo dependency set; the generated crate dies with #654. Its user-visible half — a normal/dev version conflict surfacing as an `incan test` failure naming the crate — is untwinned and goes with #1337's interop spec (slice 7). | - | libtest harness of the generated test crate |
| `parse_libtest_outcomes_detects_ok_and_failed` | retire | `dies` | parses libtest's `test ... ok\|FAILED` lines from the generated test crate's transcript; the transcript dies with the crate (#654). `incan test` reporting a pass and a failure is proved by the re-point `integration_tests.rs::e2e_generated_harness_success_reports_the_incan_test_identity_issue996` and `e2e_failure_skip_and_assert_reporting_share_one_project`. | - | libtest harness of the generated test crate |
| `parse_libtest_outcomes_normalizes_prefixed_names` | retire | `dies` | strips the generated runner crate's name from libtest's qualified test names in the transcript; the crate and its transcript die with #654. The `.incn::fn` identity in the report is carried by `e2e_generated_harness_success_reports_the_incan_test_identity_issue996` (re-point). | - | libtest harness of the generated test crate |
| `successful_native_batch_preserves_a_passing_harness_result_issue996` | retire | `dies` | maps the libtest summary line (`test result: ok. 1 passed`) of the generated test crate to a passed harness result; the transcript dies with #654. The user-visible half, a passing Incan test reported under its own identity (#996), is the re-point `integration_tests.rs::e2e_generated_harness_success_reports_the_incan_test_identity_issue996`. | - | libtest harness of the generated test crate |
| `extracts_unindented_libtest_panic_payloads_from_a_batch` | retire | `dies` | extracts assertion payloads from the `thread '...' panicked at` lines of the generated test crate's transcript; the transcript dies with #654. The assertion message reaching the `incan test` report (`AssertionError: custom boom`) is the re-point `integration_tests.rs::e2e_failure_skip_and_assert_reporting_share_one_project`. Teardown payload lines are carried by `e2e_fixture_teardown_failure_scenarios_share_one_project` (re-point). | - | libtest harness of the generated test crate |
| `runner_crate_name_is_derived_from_batch_suffix` | retire | `dies` | derives the generated test-runner crate's name (`test_runner_<hash>`) from the batch directory suffix; the crate dies with #654. | - | libtest harness of the generated test crate |
| `native_test_outputs_do_not_alias_distinct_execution_groups` | retire | `dies` | names the native test binary of one generated execution group so two groups of the same file do not overwrite each other's receipt-verified output; the generated crate and its outputs die with #654. Report correctness across normal and xfail groups of one file is carried by `e2e_markers_parametrize_timeout_and_collection_errors_share_projects` (re-point). | - | libtest harness of the generated test crate |
| `batch_suffix_is_path_independent_but_content_sensitive` | retire | `dies` | hashes a batch's sources into the generated test-runner crate directory suffix (`batch_<hash>`); a data-structure invariant of the generated crate directory, which dies with #654. | - | libtest harness of the generated test crate |
| `inject_file_test_harness_emits_tests_module` | retire | `dies` | asserts the injected `mod __incan_file_tests` text with one `fn incan_harness_<i>_<name>` per case and the `set_current_dir` guard; generated Rust text, dies with #654. The cwd guard is carried by `e2e_test_runner_preserves_fixture_cwd_for_file_and_batch_runs` (re-point). | generated_text | libtest harness of the generated test crate |
| `inject_file_test_harness_wraps_async_tests_and_fixtures` | retire | `dies` | asserts the injected harness text for an async test with a yield fixture (`__incan_async_block_on`, `__incan_run_teardown`); generated Rust text, dies with #654. Async tests and fixture teardown through `incan test` are the re-point `integration_tests.rs::e2e_fixture_lifetime_success_scenarios_share_one_project` and `e2e_fixture_teardown_failure_scenarios_share_one_project`. | - | libtest harness of the generated test crate |

Per-test overrides in `loaves/toolchain/incan-cli/tests/canonical_item_imports.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `an_import_does_not_reach_across_modules` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_codegraph_and_inspection_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `inspect_bindings_projects_checked_declaration_facts` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `diagnostic_facts_keep_related_spans_and_type_payloads_across_cli_and_codegraph` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `inspect_rust_reports_current_generated_rust_files` | retire | `dies` | `incan inspect rust --format json`: asserts the generated-project report (`rust_files` with a `crate_root`, `generated.project_path` under `target/lib`) and `#[doc = ...]` text in the generated crate root; `inspect rust` output dies with #654. | - | `incan inspect rust` over an executable and a library project; the report and the generated Rust it points at die with #654. |
| `inspect_codegraph_exports_multifile_imports_and_public_symbols` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `inspect_codegraph_distinguishes_sibling_binding_stable_identities_issue1629` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `inspect_codegraph_exports_checked_registry_facts` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `inspect_codegraph_attaches_facade_paths_to_checked_registry_facts` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `inspect_codegraph_tolerant_directory_keeps_parseable_facts_and_diagnostics` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |
| `inspect_codegraph_strict_directory_rejects_semantic_diagnostics` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_decorator_and_partial_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `test_facade_reexport_preserves_declared_source_import_alias_target_issue57` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/facade_reexport_alias_target` | - | codegen, build_run | twinned by the InQL #57 program run to completion: the facade's `col`, `count` and `count_expr` resolve through the modules that imported their same-named builders under an alias. The retired test only asserted that `--emit-rust` succeeded. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_interop_target_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `codegraph_projects_checked_c_bindings_and_explicit_unsafe_calls` | keep | - | - | - | `incan inspect codegraph`/`inspect bindings`/`check` only; representation inspection from checked facts, no generated Rust. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_language_regression_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `module_qualified_stdlib_type_annotation_emits_and_runs_issue1437` | re-point | - | - | codegen, build_run | bakes and runs the program and also asserts `--emit-rust` text; the run stays, the generated-text assertion drops in slice 7. |
| `build_union_widening_converts_generated_wrappers_issue741` | re-point | - | - | codegen, build_run | builds through `incan build` (the build must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; the build assertion stays, the generated-text assertion drops in slice 7. |
| `build_pub_helper_wraps_union_call_result_as_option_payload_issue745` | re-point | - | - | codegen, build_run | builds through `incan build` (the build must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; the build assertion stays, the generated-text assertion drops in slice 7. |
| `build_pub_method_accepts_dependency_owned_union_alias_payload_issue755` | re-point | - | - | codegen, build_run | builds through `incan build` (the build must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; the build assertion stays, the generated-text assertion drops in slice 7. |
| `build_locked_map_err_string_literal_closure_issue880` | re-point | - | - | codegen, build_run | builds through `incan build` (the build must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; the build assertion stays, the generated-text assertion drops in slice 7. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_rust_interop_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `rust_std_io_trait_interop_borrows_receivers_and_propagates_results_issues878_888` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `rust_trait_object_method_arguments_borrow_by_metadata_issue832` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `rust_concrete_reference_arguments_borrow_by_metadata_issue861` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `rust_std_result_and_contextual_f32_interop_compile_together_issues801_802` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `cold_library_build_preserves_rust_string_compound_assignment_issue896` | retire | - | - | generated_text, build_run | open (twin-h-cli): the library build is the only non-text assertion and the `str_concat` selection under cold Rust metadata is generated text. The surviving behavior, `+=` and `+` with a `rust::`-returned `String` operand, needs a `[rust-dependencies]` path into the checkout (`development_support_crate_dir("incan_std_core")`) that a fixture cannot carry. A `rust::std::string::String.from(...)` stand-in does not type as `str` (`from` is generic), so no in-fixture substitute exists. Question (with Q1): does Rust-crate interop behavior get a twin in this corpus (a fixture would need a Rust crate with a `String`-returning function and the inspection bake of Q1), or does it move with #1337's spec? If the latter, this row is `dies` with the cold-metadata half named. |
| `rust_method_into_bound_keeps_string_argument_inferable_issue804` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `build_metadata_free_into_bound_tokenizer_encode_issue804` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `comprehension_over_rust_iterator_consumes_it_by_value_issue1490` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_surface_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `fixture_handoff_copy_rejects_symlinks` | unaffected | - | - | - | test-support helper (fixture hand-off copy); no compiler invocation. |
| `check_json_reports_parser_diagnostics` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_json_reports_typechecker_diagnostics` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_json_reports_tooling_diagnostics` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_json_reports_import_diagnostics` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `explain_reports_known_and_unknown_diagnostic_codes` | keep | - | - | - | `incan explain` only; diagnostics catalog surface, no generated Rust. |
| `requires_incan_allows_compatible_project_commands` | unaffected | - | - | - | `incan lock` / `workspace inspect` only; lock orchestration over Oven, not the Rust backend. |
| `init_creates_project_scaffold_with_expected_content` | unaffected | - | - | - | CLI surface (argument parsing, scaffolding, lifecycle, doctor); no compiler semantics. |
| `tools_doctor_reports_text_and_json` | unaffected | - | - | - | CLI surface (argument parsing, scaffolding, lifecycle, doctor); no compiler semantics. |
| `tools_metadata_api_reports_docstring_drift` | keep | - | - | - | `incan tools metadata api` only; API metadata from checked facts, no generated Rust. |
| `tools_metadata_api_reports_public_import_aliases` | keep | - | - | - | `incan tools metadata api` only; API metadata from checked facts, no generated Rust. |
| `check_json_reports_parser_and_typechecker_warnings_without_failing` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_json_reports_warnings_alongside_errors_when_typechecking_fails` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_rejects_statement_tuple_unpack_of_non_tuple_without_leaking_generated_rust` | keep | - | - | - | `incan check` only; checker surface (diagnostics through the CLI), no generated Rust. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/cli_workspace_and_lock_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `workspace_inspect_reports_deterministic_scope_and_stale_member_locks` | unaffected | - | - | - | `incan lock` / `workspace inspect` only; lock orchestration over Oven, not the Rust backend. |
| `workspace_lock_scopes_command_feature_flags_to_the_invoking_member_issue1414` | unaffected | - | - | - | `incan lock` / `workspace inspect` only; lock orchestration over Oven, not the Rust backend. |
| `workspace_root_library_without_a_script_publishes_the_canonical_lock_issue997` | unaffected | - | - | - | `incan lock` / `workspace inspect` only; lock orchestration over Oven, not the Rust backend. |
| `rooted_workspace_cold_lock_and_selected_member_preserve_identity_issues908_909_931` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `workspace_lock_concurrent_publishers_leave_one_parseable_root_lock` | unaffected | - | - | - | `incan lock` / `workspace inspect` only; lock orchestration over Oven, not the Rust backend. |
| `workspace_fmt_fans_out_in_member_order_without_changing_single_project_semantics` | keep | - | - | - | `incan workspace fmt` only; formatter surface fanned out per member, no generated Rust. |
| `workspace_check_fans_out_with_one_member_scoped_json_report` | keep | - | - | - | `incan workspace check` only; checker surface fanned out per member, no generated Rust. |
| `a_plain_build_of_a_toolchain_loaf_selects_its_rust_binaries_and_names_the_interim_receipt_inputs_issue1698` | unaffected | - | - | build_run | `incan build` dispatch for a toolchain Loaf (`[[rust.bin]]`, #1698): asserts the stored-plan route is taken and its refusals; no Incan compilation, not the Rust backend. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/integration_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `build_explicit_mutable_rust_generic_reaches_codegen_through_normal_cli_path` | retire | - | - | codegen, generated_text, build_run | open: the build succeeds and the assertions are the projected ABI in `main.rs` (`ProviderHandle<(&mut i64, &mut i64)>`), generated text. A behavior twin exists and passes standalone and under a root admitted to the explicit bake: the same program run to completion, printing its marker line (this program's `rust::std::vec::Vec` import launched no Cargo itself, but the bake that records its authority is the same explicit bake). Waits for a Cargo-free bake under the suite: a program with a `rust::` import needs source-current project inspection authority, which only an explicit `incan oven bake --project .` of its project records, and that bake runs Rust inspection through Cargo (`cargo metadata` of the inspection workspace and the toolchain's `std` sources, `rustc --print` probes, `cargo check`; ten launches per fixture), which under the suite needs the root registered with `explicit_bake_cargo` and is ruled out. Candidate: `loaves/compiler/incan_test_support/fixtures/behavior_candidates/cli/rust_generic_mutable_reference_args.incn`. |
| `decorated_method_explicit_mutable_rust_generic_keeps_static_and_wrapper_abi` | retire | - | - | codegen, generated_text, build_run | open: the build succeeds and the assertions are the shared ABI spelling in `main.rs`, generated text. A behavior twin exists and passes standalone and under a root admitted to the explicit bake: the same program run to completion (`7`, then its marker line). Waits for a Cargo-free bake under the suite: a program with a `rust::` import needs source-current project inspection authority, which only an explicit `incan oven bake --project .` of its project records, and that bake runs Rust inspection through Cargo (`cargo metadata` of the inspection workspace and the toolchain's `std` sources, `rustc --print` probes, `cargo check`; ten launches per fixture), which under the suite needs the root registered with `explicit_bake_cargo` and is ruled out. Candidate: `loaves/compiler/incan_test_support/fixtures/behavior_candidates/cli/decorated_rust_generic_mutable_reference_args.incn`. |
| `test_cli_fmt_preserves_block_decl_docstrings_and_export_doc_surface` | keep | - | - | parser | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_fmt_accepts_assert_identity_bool_literals` | keep | - | - | - | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_fmt_wraps_long_parenthesized_logical_expression_chain` | keep | - | - | - | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_fmt_preserves_fstring_escaped_newline_roundtrip` | keep | - | - | - | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_fmt_applies_rfc053_vertical_spacing_contract` | keep | - | - | parser | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_fmt_keeps_two_blank_lines_between_static_and_function` | keep | - | - | parser | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_fmt_keeps_trailing_comment_after_multiline_function` | keep | - | - | parser | `incan fmt` only; formatter surface, no generated Rust. |
| `test_cli_check_accepts_trailing_comma_in_multiline_function_params` | keep | - | - | - | `incan --check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `test_compound_assign_float_with_int_rhs` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_valid_fixtures` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_invalid_fixtures` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_help_is_banner_free` | unaffected | - | - | - | CLI argument surface (`--help`, `--version`, an unknown flag); never reaches the compiler. |
| `test_version_is_single_line_and_banner_free` | unaffected | - | - | - | CLI argument surface (`--help`, `--version`, an unknown flag); never reaches the compiler. |
| `test_parse_error_is_banner_free` | unaffected | - | - | - | CLI argument surface (`--help`, `--version`, an unknown flag); never reaches the compiler. |
| `test_imported_static_initializer_does_not_deadlock_issue680` | re-point | - | - | generated_text, build_run | Builds and runs (prints `ok` within 30 s with a static reached directly and through a facade, #680) and also reads the generated Rust; the run assertion stays, the generated-text assertion drops in slice 7. Re-point, not retire: the run half has no other coverage. |
| `lexer_token_surface_cases` | keep | - | - | - | lexer only |
| `test_python_like_numeric_ops_compile` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_hello_world_codegen` | retire | `dies` | IrCodegen text of `examples/hello.incn` (`fn main()`, `println!`, the message). The test returns early because that path no longer exists (the example is `examples/simple/hello.incn`), so it proves nothing today; the example itself is a case of the re-point `examples` fixture root, run by `make examples`. | codegen, generated_text, checker, parser | IrCodegen over examples/hello.incn, a path that no longer exists (the test skips itself); the example is run by the `examples` fixture root. |
| `test_method_alias_codegen_rewrites_to_target_method` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/method_alias_calls_target.incn` | - | codegen, parser | twinned by a program whose model declares `mean = avg` and prints both calls; the retired assertion (the alias lowering to the target's canonical projection, no wrapper) was generated text. |
| `test_check_web_route_uses_proc_macro_passthrough` | keep | - | - | - | `incan --check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `explicit_legacy_sdk_inventory_blocks_lock_preheat` | unaffected | - | - | - | `incan lock` / `workspace inspect` only; lock orchestration over Oven, not the Rust backend. |
| `test_check_cyclic_explicit_call_site_generics_cross_module_succeeds` | keep | - | - | - | `incan --check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `test_rfc041_rusttype_interop_typechecks_end_to_end` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_rfc041_rusttype_with_methods_typechecks` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_rfc041_rust_coercion_codegen_smoke` | retire | - | - | codegen, checker, parser | open: IrCodegen text only (`Duration::from_secs_f32`). A behavior twin exists and passes standalone and under a root admitted to the explicit bake: a program passing a float literal to `rust::std::time::Duration.from_secs_f32` and printing `as_millis()` (`1500`). Waits for a Cargo-free bake under the suite: a program with a `rust::` import needs source-current project inspection authority, which only an explicit `incan oven bake --project .` of its project records, and that bake runs Rust inspection through Cargo (`cargo metadata` of the inspection workspace and the toolchain's `std` sources, `rustc --print` probes, `cargo check`; ten launches per fixture), which under the suite needs the root registered with `explicit_bake_cargo` and is ruled out. Candidate: `loaves/compiler/incan_test_support/fixtures/behavior_candidates/cli/rust_f32_argument_coercion.incn`. |
| `test_rfc041_structural_coercion_codegen_smoke` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/annotated_collection_literals.incn` | - | codegen, generated_text, checker, parser | twinned by a program that binds the annotated `Option[int]`, `List[str]` and `Dict[str, float]` literals and reads them back; the retired assertion was the Rust expressions they lowered to. |
| `test_rfc009_numeric_resize_and_decimal_codegen_smoke` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/numeric_resize_policies_and_decimal.incn` | - | codegen, generated_text, checker, parser | twinned by a program that prints each resize policy's result and the decimal literal; the retired assertion was the casts and stdlib helper calls they lowered to. |
| `build_lib_imported_static_decorator_receiver_materializes_string_arg_issue671` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli/imported_static_decorator_receiver` | - | generated_text, build_run | twinned by the #671 program run to completion: the decorator factory called on the imported static receiver with a str argument leaves the decorated function callable. The retired test built `--lib` and asserted how the str argument materialized in generated text. |
| `test_model_with_decorator` | keep | - | - | parser | lex/parse/typecheck only |
| `test_class_with_traits` | keep | - | - | parser | lex/parse/typecheck only |
| `test_trait_supertraits_compile_source` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_trait_constructor_rejected_in_full_pipeline` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_method_with_mut_self` | keep | - | - | parser | lex/parse/typecheck only |
| `test_generic_instance_method_full_pipeline` | keep | - | - | checker, parser | lex/parse/typecheck only |
| `test_match_with_case` | keep | - | - | parser | lex/parse/typecheck only |
| `test_list_comprehension` | keep | - | - | parser | lex/parse/typecheck only |
| `test_generic_type` | keep | - | - | parser | lex/parse/typecheck only |
| `test_yield_expression` | keep | - | - | parser | lex/parse/typecheck only |
| `test_fixture_decorator` | keep | - | - | parser | lex/parse/typecheck only |
| `test_rust_crate_import` | keep | - | - | parser | lex/parse/typecheck only |
| `test_rust_from_import` | keep | - | - | parser | lex/parse/typecheck only |

Per-test overrides in `loaves/toolchain/incan-cli/tests/package_boundary_facade_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `explicitly_typed_decorator_keeps_the_provider_union_representation_issue1453` | re-point | - | - | build_run | Twin of `loaves/compiler/incan_ir/src/lower/mod.rs::retain_declared_decorator_surface_restores_native_carriers_issue1453`. The test reads the baked library's generated text at line 615 (`!generated.contains("pub enum __IncanUnion")`), which retires with this file's other generated-text reads; the provider and library bakes, the consumer run and its `42` assertion must survive that retirement, because that run is what the incan_ir row depends on. |

Per-test overrides in `loaves/toolchain/incan-cli/tests/rfc031_pub_import_integration_tests.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `compiled_provider_preserves_shared_rust_interop_contracts_issues834_835_961` | retire | - | - | codegen, generated_text, build_run, checker | open (twin-h-cli): the provider bake and the consumer build succeed over a local Rust crate (`receiver_factory`, `[rust-dependencies]`); the assertions are the manifest `rust_abi` type-parameter record and generated provider text (method turbofish, `&mut [f32]` callback, receiver specialization). A twin would be a project fixture carrying a Rust crate plus the provider bake of Q2 and the `rust::` inspection bake of Q1. Question: does Rust-crate interop behavior get a twin in this corpus (fixture carries Rust source), or does it move with #1337's spec? |
| `oven_build_projects_structural_mutable_reference_generics_for_an_unrelated_provider` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `external_pub_consumer_uses_flattened_nested_union_variant_index_issue1198` | re-point | - | - | codegen, build_run | builds or runs through `incan` (the build or run must succeed) and then asserts Rust text read from `target/incan/<project>/src/main.rs`; that assertion drops in slice 7, the build or run assertion stays. |
| `external_pub_consumer_narrows_dependency_owned_union_through_model_field_issue1697` | re-point | - | - | codegen, build_run | #1697: bakes a provider whose `pub model` field is a `pub type` union alias, then bakes and runs a consumer that narrows the field, a binding of the field, a helper result and a `List` field's loop element, asserting the exact run output; also asserts the provider-qualified wrapper in `target/incan/<project>/src/main.rs`, which drops in slice 7 while the run assertion stays. |
| `fmt_dependency_collection_does_not_prepare_persistent_library_artifact` | keep | - | - | - | `incan fmt` only; formatter surface, no generated Rust. |
| `check_reports_unknown_pub_library` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_reports_missing_pub_export` | keep | - | - | checker | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_reports_pub_manifest_load_failure` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_passes_for_pub_imported_manifest_type` | keep | - | - | checker | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_reports_missing_pub_library_artifacts` | keep | - | - | checker | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `check_reports_pub_library_artifact_mismatch` | keep | - | - | checker | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_resolves_a_manifest_declared_package_relative_c_header` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_copies_a_sqlite_error_view_with_an_explicit_bound` | retire | `dies` | `--emit-rust` text half (the `__incan_checked_c_copy_utf8` bounded-copy helper); the `incan check` acceptance in the same test is checker-only and survives. The binding's `header` is a host-specific absolute path (`sqlite_header_path()`) and the check needs the host verifier, so a fixture cannot carry the program. | codegen | `incan check` (checker-only) followed by `--emit-rust`, whose generated Rust text is the assertion; nothing is built or run, so the test retires with the emitter, as its sibling `consumer_check_models_an_accelerate_f32_span_through_a_declared_framework_shim` does. |
| `consumer_check_models_a_sqlite_caller_owned_byte_buffer_through_a_declared_shim` | retire | `dies` | `--emit-rust` text half (`__incan_checked_c_finish_span`, `*mut u8`, `-> usize`); the `incan check` acceptance in the same test is checker-only and survives. The declared shim symbol `incan_sqlite_random_bytes` has no implementation and the header is a host-specific path, so the program cannot run and no behavior fixture is possible. | codegen | `incan check` (checker-only) followed by `--emit-rust`, whose generated Rust text is the assertion; nothing is built or run, so the test retires with the emitter, as its sibling `consumer_check_models_an_accelerate_f32_span_through_a_declared_framework_shim` does. |
| `consumer_check_models_an_accelerate_f32_span_through_a_declared_framework_shim` | retire | `dies` | `--emit-rust` text half (`*const f32`, the `f32` output carrier, the `#[link(... kind = "framework")]` attribute); the `incan check` acceptance in the same test is checker-only and survives. The declared shim symbol `incan_accelerate_sum_f32` has no implementation, so the program cannot run and no behavior fixture is possible. | codegen, generated_text | `incan check` (checker-only) followed by `--emit-rust`, whose generated Rust text is the assertion; nothing is built or run, so the test retires with the emitter. |
| `consumer_check_supports_checked_byte_spans_and_caller_owned_buffers` | retire | `dies` | `--emit-rust` text half (`__incan_checked_c_finish_span`, `.as_ptr()`/`.as_mut_ptr()`); the `incan check` acceptance in the same test is checker-only and survives. The declared shim symbol `fixture_copy_prefix` has no implementation, so the program's `main` cannot run and no behavior fixture is possible. | codegen | `incan check` (checker-only) followed by `--emit-rust`, whose generated Rust text is the assertion; nothing is built or run, so the test retires with the emitter, as its sibling `consumer_check_models_an_accelerate_f32_span_through_a_declared_framework_shim` does. |
| `consumer_check_rejects_unpaired_or_immutable_checked_byte_buffer_arguments` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_rejects_checked_byte_span_escape_and_reuse` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_preserves_checked_c_f32_scalar_identity` | retire | `dies` | `--emit-rust` text half (an `f32` carrier in the emitted binding); the `incan check` acceptance in the same test is checker-only and survives. The declared shim symbol `fixture_absolute_f32` has no implementation, so the program cannot run and no behavior fixture is possible. | codegen | `incan check` (checker-only) followed by `--emit-rust`, whose generated Rust text is the assertion; nothing is built or run, so the test retires with the emitter, as its sibling `consumer_check_models_an_accelerate_f32_span_through_a_declared_framework_shim` does. |
| `consumer_check_preserves_exact_checked_c_scalar_carriers` | retire | `dies` | `--emit-rust` text half (one exact Rust carrier per `c.*` scalar); the `incan check` acceptance in the same test is checker-only and survives. The declared shim symbols (`fixture_i8` ... `fixture_size`) have no implementation, so the program cannot run and no behavior fixture is possible. | codegen | `incan check` (checker-only) followed by `--emit-rust`, whose generated Rust text is the assertion; nothing is built or run, so the test retires with the emitter, as its sibling `consumer_check_models_an_accelerate_f32_span_through_a_declared_framework_shim` does. |
| `consumer_check_supports_checked_f32_spans_for_paired_numeric_pointers` | keep | - | - | - | `incan check` and `inspect bindings` only; representation inspection from checked facts, no generated Rust. |
| `consumer_check_rejects_unpaired_checked_f32_span_arguments` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `sqlite_checked_c_tooling_projects_the_shared_descriptor` | keep | - | - | - | `incan check` and `inspect bindings` only; representation inspection from checked facts, no generated Rust. |
| `consumer_check_reports_checked_c_signature_mismatch_at_the_binding` | keep | - | - | - | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugars_external_vocab_block_via_wasm` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_passes_request_payload_into_external_vocab_desugarer` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_accepts_expression_desugar_output_in_statement_position` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_reports_external_vocab_desugarer_failure` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_build_plans_source_backed_vocab_helper_calls_with_defaults_and_unions_issue729` | retire | - | - | generated_text, build_run, checker | open (twin-h-cli): the consumer build succeeds and the assertions are generated text (`querykit::count(` filled from the helper default, `COUNT_SENTINEL` through the provider path, no re-owned `__IncanUnion`). The desugared `where true:` calls come from a vocab companion crate (`write_vocab_companion_crate_with_source`, a wasm desugarer) a fixture cannot carry, on top of the provider bake of Q2. The same helpers' default, union and string behavior through ordinary calls is the pub sibling's candidate twin; the re-point sibling `consumer_build_plans_vocab_helper_calls_like_ordinary_calls_issue729` proves desugared helper calls build. Question: is that enough for `dies`, or does the vocab lane want a fixture with a companion? |
| `consumer_build_plans_source_backed_pub_helper_calls_with_defaults_and_unions_issue729` | retire | `loaves/compiler/incan_test_support/fixtures/behavior/cli_dependencies/dependency_helper_defaults_and_unions` | - | generated_text, build_run, checker | the consumer build succeeded and the assertions were generated text (`querykit::count(` filled from the helper default, `COUNT_SENTINEL` and `DEFAULT_LABEL` through the provider module path, no re-owned `__IncanUnion`, `.to_string()` arguments); the twin is a project fixture with `deps/querykit` whose `main` prints `adjusted=5`, `order_count=__querykit_count_no_argument__` and `orders=7` (union parameter, omitted-argument default, dependency-owned const default), with the provider baked by the runner (harness-deps). Narrowing the dependency's `Union[IntLiteralExpr, StringLiteralExpr]` in the consumer is accepted by the checker but the legacy route does not compile it, so the twin narrows inside the provider. |
| `consumer_check_passes_scoped_query_surface_artifacts_to_desugarer` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_passes_expr_list_item_metadata_to_desugarer_issue724` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugars_colon_vocab_expression_in_assignment_issue727` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugars_colon_vocab_expression_in_return_issue727` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugars_colon_vocab_expression_preserves_inline_clauses_issue727` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugars_braced_vocab_expression_with_compound_clauses_issue727` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugared_public_field_callee_call_typechecks_as_method_issue727` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `consumer_check_desugared_generic_method_call_uses_expected_return_type_issue735` | keep | - | - | checker, parser | `incan check` / `--check` only; checker surface (diagnostics through the CLI), no generated Rust. |
| `fmt_activates_clean_source_dependency_vocab_before_parsing_issue756` | keep | - | - | - | `incan fmt` only; formatter surface, no generated Rust. |

### `loaves/toolchain/incan-lsp` (93 tests in 13 files: keep 92, retire 1)

| File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
|---|---:|---:|---:|---|---:|---:|---|---|---|---|
| `loaves/toolchain/incan-lsp/src/backend/tests/completions.rs` | 20 | 648 | 648 | keep | - | - | - | #1561 | checker 6, parser 7 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/contract_model_command.rs` | 2 | 81 | 81 | keep | - | - | - | #1561 | - | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/hover_previews.rs` | 14 | 511 | 511 | keep | - | - | - | #1561 | checker 9, parser 12, lsp 1 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/identity_navigation.rs` | 4 | 369 | 369 | keep | - | - | - | #1561 | checker 4, parser 4, lsp 2 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/parse_context_and_receivers.rs` | 3 | 88 | 88 | keep | - | - | - | #1561 | parser 3 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/rust_inspect_workspace.rs` | 3 | 149 | 149 | keep | - | - | - | #1561 | checker 1, parser 1 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/signature_help.rs` | 3 | 104 | 104 | keep | - | - | - | #1561 | parser 3 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/backend/tests/vocab_surfaces.rs` | 7 | 237 | 237 | keep | - | - | - | #1561 | parser 3 | split of incan-lsp/src/backend.rs; language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/call_site_type_args.rs` | 4 | 749 | 48 | keep | - | - | - | #1561 | parser 1 | language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/diagnostics.rs` | 7 | 395 | 168 | keep | - | - | - | #1561 | checker 3 | language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/main.rs` | 3 | 98 | 30 | keep | - | - | - | #1561 | - | language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/src/semantic_tokens.rs` | 14 | 1394 | 284 | keep | - | - | - | #1561 | parser 13 | language server over the checker; no codegen. Reviewed at crate level. |
| `loaves/toolchain/incan-lsp/tests/rfc081_embedded_conformance.rs` | 9 | 730 | 730 | keep (keep 8, retire 1) | 0/1 | 0 | - | #1561 | codegen 1, checker 2, parser 1, formatter 3, lsp 1 | RFC 081 embedded-fragment conformance through desugar, typecheck and formatter; one test asserts the emitter refuses. |

Per-test overrides in `loaves/toolchain/incan-lsp/tests/rfc081_embedded_conformance.rs`:

| Test | Disposition | Twin | Dies | Lanes | Notes |
|---|---|---|---|---|---|
| `every_submode_survives_desugar_typecheck_and_lowering_then_refuses_emission` | retire | - | - | codegen, checker | asserts an emitter refusal; the refusal moves to the replacement route's source profile |

??? note "Unaffected crates (1148 tests in 90 files)"

    Every test in these crates is `unaffected`: the cutover does not touch them. They are listed so the summary reconciles to the whole tree.

    #### `loaves/compiler/incan_oven_facet` (6 tests in 2 files: unaffected 6)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/compiler/incan_oven_facet/src/lib.rs` | 4 | 465 | 235 | unaffected | - | - | - | #1561 | - | Oven facet of the compiler; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_oven_facet/tests/oven_pr_regressions.rs` | 2 | 210 | 210 | unaffected | - | - | - | #1561 | - | Oven facet of the compiler; no emit/driver dependency. Reviewed at crate level. |

    #### `loaves/compiler/incan_provider` (97 tests in 9 files: unaffected 97)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/compiler/incan_provider/src/compiled_sdk.rs` | 1 | 74 | 16 | unaffected | - | - | - | #1561 | - | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/dependency_resolver.rs` | 21 | 1161 | 533 | unaffected | - | - | - | #1561 | checker 20 | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/inventory.rs` | 7 | 1036 | 590 | unaffected | - | - | - | #1561 | checker 3 | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/lock_semantics.rs` | 14 | 2063 | 1320 | unaffected | - | - | - | #1561 | checker 12 | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/requirements.rs` | 11 | 1002 | 275 | unaffected | - | - | - | #1561 | checker 8 | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/sdk_build.rs` | 8 | 869 | 253 | unaffected | - | - | - | #1561 | run 1, checker 1 | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/sdk_store.rs` | 11 | 1178 | 511 | unaffected | - | - | - | #1561 | - | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/src/vocab_extraction.rs` | 14 | 1713 | 296 | unaffected | - | - | - | #1561 | run 3 | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/incan_provider/tests/stdlib_effect_digest.rs` | 10 | 313 | 313 | unaffected | - | - | - | #1561 | - | SDK provider store, lock semantics, vocab extraction; no emit/driver dependency. Reviewed at crate level. |

    #### `loaves/compiler/rust_inspect` (103 tests in 7 files: unaffected 103)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/compiler/rust_inspect/src/cache_tests.rs` | 41 | 2610 | 2610 | unaffected | - | - | required | #1561 | replacement 1 | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/rust_inspect/src/digest/tests.rs` | 17 | 634 | 634 | unaffected | - | - | - | #1561 | - | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/rust_inspect/src/extractor.rs` | 20 | 3243 | 1279 | unaffected | - | - | - | #1561 | - | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/rust_inspect/src/lib.rs` | 2 | 206 | 149 | unaffected | - | - | - | #1561 | - | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/rust_inspect/src/loader.rs` | 15 | 2105 | 630 | unaffected | - | - | - | #1561 | - | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/rust_inspect/src/mir_digest.rs` | 5 | 455 | 235 | unaffected | - | - | - | #1561 | - | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |
    | `loaves/compiler/rust_inspect/src/receiver_contract.rs` | 3 | 101 | 43 | unaffected | - | - | - | #1561 | - | Rust metadata extraction that feeds the checker; no emit/driver dependency. Reviewed at crate level. |

    #### `loaves/kernel/incan_codegraph` (5 tests in 1 file: unaffected 5)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/kernel/incan_codegraph/src/lib.rs` | 5 | 1312 | 174 | unaffected | - | - | - | #1561 | - | codegraph record format. Reviewed at crate level. |

    #### `loaves/kernel/incan_vocab` (6 tests in 1 file: unaffected 6)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/kernel/incan_vocab/src/lib.rs` | 6 | 514 | 205 | unaffected | - | - | - | #1561 | checker 6 | vocab registration contract crate. Reviewed at crate level. |

    #### `loaves/oven/oven_cargo_compat` (144 tests in 10 files: unaffected 144)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/oven/oven_cargo_compat/src/cargo_process.rs` | 3 | 209 | 72 | unaffected | - | - | - | #1561 | run 1 | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/compiler_suite_foundation.rs` | 7 | 1542 | 978 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/harvest.rs` | 13 | 1671 | 807 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/lib.rs` | 68 | 9360 | 4749 | unaffected | - | - | required | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/loaf_bake.rs` | 10 | 1592 | 750 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/loaf_bake/vocab_support.rs` | 3 | 1023 | 290 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/registry_sources.rs` | 2 | 699 | 129 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/rustc_trace.rs` | 5 | 494 | 103 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/selected_graph_projection.rs` | 22 | 3811 | 1478 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_cargo_compat/src/selected_unit_capture.rs` | 11 | 2361 | 783 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |

    #### `loaves/oven/oven_interop` (20 tests in 1 file: unaffected 20)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/oven/oven_interop/src/lib.rs` | 20 | 4394 | 2720 | unaffected | - | - | required | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |

    #### `loaves/oven/oven_model` (162 tests in 9 files: unaffected 162)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/oven/oven_model/src/loaf_registry.rs` | 5 | 685 | 245 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/lock.rs` | 24 | 1893 | 994 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/manifest.rs` | 59 | 3874 | 1234 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/oven_interop.rs` | 8 | 2098 | 603 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/project_lifecycle/env.rs` | 18 | 860 | 476 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/project_lifecycle/toolchain.rs` | 4 | 295 | 58 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/project_lifecycle/version.rs` | 10 | 418 | 104 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/toolchain_layout.rs` | 15 | 875 | 353 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_model/src/workspace.rs` | 19 | 2065 | 604 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |

    #### `loaves/oven/oven_rustc` (296 tests in 17 files: unaffected 296)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/oven/oven_rustc/src/loaf.rs` | 28 | 4710 | 1341 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/loaf_mirror.rs` | 9 | 758 | 429 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/native_test.rs` | 34 | 2881 | 1419 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). A source module (the native test runner) with a `#[cfg(test)]` region, measured by that region. Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/native_test/case_slice.rs` | 5 | 174 | 87 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/native_test/evidence.rs` | 4 | 258 | 74 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/plan/composition.rs` | 8 | 1496 | 686 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/plan/selection.rs` | 1 | 518 | 51 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc.rs` | 94 | 11409 | 6517 | unaffected | - | - | required | #1561 | run 3 | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/compiled_unit.rs` | 7 | 844 | 476 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/direct_compiler/retention.rs` | 3 | 771 | 771 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). A source module (compiler-closure retention) with a `#[cfg(all(test, unix))]` region over a synthetic sysroot, measured by that region. Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/inspection.rs` | 32 | 2657 | 1991 | unaffected | - | - | required | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/runtime_closure.rs` | 8 | 1087 | 509 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/runtime_executor.rs` | 9 | 1341 | 759 | unaffected | - | - | - | #1561 | run 4 | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/runtime_foundation.rs` | 26 | 2152 | 1473 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/selected_unit.rs` | 13 | 1793 | 717 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/substitution.rs` | 10 | 708 | 335 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_rustc/src/rustc/toolchain.rs` | 5 | 656 | 73 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |

    #### `loaves/oven/oven_store` (94 tests in 6 files: unaffected 94)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/oven/oven_store/src/closure_proof.rs` | 1 | 107 | 27 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_store/src/lib.rs` | 13 | 2285 | 550 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_store/src/process.rs` | 10 | 769 | 769 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_store/src/progress.rs` | 6 | 343 | 99 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_store/src/store.rs` | 55 | 6391 | 2080 | unaffected | - | - | required | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |
    | `loaves/oven/oven_store/src/store_mirror.rs` | 9 | 635 | 373 | unaffected | - | - | - | #1561 | - | Oven ring; no compiler-crate dependency (scripts/check_oven_ring.py). Reviewed at crate level. |

    #### `loaves/stdlib/async` (28 tests in 6 files: unaffected 28)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/stdlib/async/rust/src/channel.rs` | 8 | 540 | 115 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/async/rust/src/race.rs` | 5 | 157 | 58 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/async/rust/src/runtime.rs` | 2 | 104 | 33 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/async/rust/src/sync.rs` | 4 | 663 | 125 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/async/rust/src/task.rs` | 4 | 201 | 58 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/async/rust/src/time.rs` | 5 | 184 | 85 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |

    #### `loaves/stdlib/core` (88 tests in 9 files: unaffected 88)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/stdlib/core/rust/src/collections.rs` | 28 | 605 | 242 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/src/conversions.rs` | 6 | 73 | 41 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/src/frozen.rs` | 1 | 445 | 16 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/src/iter.rs` | 11 | 365 | 96 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/src/num.rs` | 26 | 1008 | 290 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/src/strings.rs` | 5 | 405 | 42 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/src/version.rs` | 9 | 249 | 89 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/tests/errors.rs` | 1 | 32 | 32 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/core/rust/tests/string_len.rs` | 1 | 10 | 10 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |

    #### `loaves/stdlib/data` (8 tests in 2 files: unaffected 8)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/stdlib/data/rust/src/collections.rs` | 1 | 144 | 17 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/data/rust/src/json.rs` | 7 | 708 | 108 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |

    #### `loaves/stdlib/derive` (8 tests in 2 files: unaffected 8)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/stdlib/derive/incan_web_macros/src/lib.rs` | 6 | 451 | 133 | unaffected | - | - | - | #1561 | parser 6 | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |
    | `loaves/stdlib/derive/incan_web_macros/tests/route_runtime.rs` | 2 | 197 | 197 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |

    #### `loaves/stdlib/interop` (1 test in 1 file: unaffected 1)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/stdlib/interop/vocab_companion/src/lib.rs` | 1 | 188 | 17 | unaffected | - | - | - | #1561 | checker 1 | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |

    #### `loaves/stdlib/testing` (3 tests in 1 file: unaffected 3)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/stdlib/testing/rust/src/lib.rs` | 3 | 275 | 80 | unaffected | - | - | - | #1561 | - | stdlib runtime crate; no compiler-crate dependency. Reviewed at crate level. |

    #### `loaves/third_party/ra_ap_proc_macro_api` (2 tests in 1 file: unaffected 2)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/third_party/ra_ap_proc_macro_api/src/legacy_protocol/msg.rs` | 2 | 430 | 244 | unaffected | - | - | - | #1561 | - | vendored crate. Reviewed at crate level. |

    #### `loaves/toolchain/oven-cli` (77 tests in 5 files: unaffected 77)

    | File | Tests | Lines | Test lines | Disposition | Twins | Dies | Split | Owner | Signals | Notes |
    |---|---:|---:|---:|---|---:|---:|---|---|---|---|
    | `loaves/toolchain/oven-cli/src/commands/oven.rs` | 58 | 6842 | 3321 | unaffected | - | - | required | #1561 | - | Oven CLI; bakes and harvests, no compiler semantics. Reviewed at crate level. |
    | `loaves/toolchain/oven-cli/src/commands/oven/case_partition.rs` | 6 | 379 | 171 | unaffected | - | - | - | #1561 | - | Oven CLI; bakes and harvests, no compiler semantics. Reviewed at crate level. |
    | `loaves/toolchain/oven-cli/src/commands/oven/harvest.rs` | 3 | 437 | 69 | unaffected | - | - | - | #1561 | - | Oven CLI; bakes and harvests, no compiler semantics. Reviewed at crate level. |
    | `loaves/toolchain/oven-cli/src/commands/oven/suite_environment.rs` | 2 | 899 | 32 | unaffected | - | - | - | #1561 | - | Oven CLI; bakes and harvests, no compiler semantics. Reviewed at crate level. |
    | `loaves/toolchain/oven-cli/src/commands/tools.rs` | 8 | 1869 | 414 | unaffected | - | - | - | #1561 | run 1 | Oven tools command; the build_run hit is a Cargo config hint, not an Incan build. |
