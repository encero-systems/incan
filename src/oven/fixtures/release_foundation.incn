"""Minimal standard-provider runtime fixture for the release Loaf envelope."""

from std.fs import Path
from std.json import JsonValue
from std.testing import assert_true


def main() -> None:
    """Verify the checked filesystem provider can observe the fixture working directory."""
    assert_true(Path(".").exists())
