//! The generated project, the IR and emission crates' shim and the shadow comparison under their old path, for the
//! callers that still spell `crate::backend::…`.

pub use incan_driver::backend::*;
