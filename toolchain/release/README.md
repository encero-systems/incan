# `release`

Ring: **toolchain**

Archive packaging, release manifest, support workspace, install scripts.

## Moves here from

- `workspaces/release/`
- `scripts/ (release and packaging scripts)`

## May depend on

none

This directory is a layout skeleton. It holds no code yet; `src/` is a placeholder for the conventional crate root.
