[project]
name = "incan_stdlib_web"
version = "0.5.0"
private = true

[build]
source-root = "../.."

[sdk]
profile = "minimal"
components = ["stdlib-data", "stdlib-async"]

[rust-dependencies]
incan_web_macros = { path = "../../../../incan_web_macros" }
