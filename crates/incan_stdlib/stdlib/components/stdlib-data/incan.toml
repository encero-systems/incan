[project]
name = "incan_stdlib_data"
version = "0.5.0"
private = true

[build]
source-root = "../.."

[rust-dependencies]
incan_stdlib = { path = "../../..", features = ["ordinal"] }

[sdk]
profile = "minimal"
components = ["stdlib-system", "stdlib-codecs"]
