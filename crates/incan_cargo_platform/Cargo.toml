[package]
name = "incan_cargo_platform"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Frozen fork of Cargo's cfg-expression grammar, so Oven can decide target-gated dependency edges without Cargo."
