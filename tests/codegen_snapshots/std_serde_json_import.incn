# RFC 024: Verify module-level JSON derive metadata resolves and compiles.

from std.serde import json

@derive(json)
model Config:
    host: str
    port: int
    debug: bool

def main() -> None:
    print(Config(host="localhost", port=8080, debug=true).to_json())
